# Phase 09: ORD 5th-Option Retrofit — Part 2 (ord-x01–x40, ord-z01–z30)

Second and final part of the ORD retrofit. Part 1 (`Phase-08-ORD-Retrofit-Part1.md`) added a 5th option to `ord-e01`–`ord-e98`; this phase does the remaining 70 items: `ord-x01`–`ord-x40` (40) and `ord-z01`–`ord-z30` (30). Same approach as part 1 — read it first for the established method (always append as `E`, never renumber; check every new option against its target word for accidental synonymy; batch the writing in chunks of ~20 with a build check after each to avoid a repeat of the stall that hit the first attempt at part 1).

Read `Reference/calibration/ORD.md` (the distractor taxonomy: near-miss register, antonym trap, same-domain-wrong-specificity, morphological false friend) and the root `README.md` (copyright boundary — same as part 1: target words/options are original app content, the taxonomy is what's derived from real exams).

## Tasks

- [x] Read all 70 `ord-x01`–`ord-x40` and `ord-z01`–`ord-z30` items (target word, options, answer, `distractorNotes`) and classify which taxonomy category is thinnest per item, same method as part 1. Note whether this id range shares part 1's two authoring-format split (older single-word options vs. newer descriptive-phrase options) or is uniform — don't assume, check.

  **Completion note:** Extracted both ranges programmatically (all 70 items are single-line entries, unlike part 1's `e01`–`e38` which were multi-line) and confirmed this range does **not** cleanly match part 1's two-format split — it's actually a *third* distinct sub-pattern per group: `ord-x01`–`ord-x40` use the older single-word option style (e.g. `tveksam`, `framstående`) *with* `explanationData.distractorNotes`, but only partial per-letter coverage (not every wrong option annotated — same partial-coverage pattern as part 1's `e01`–`e38`), confirmed via a grep: all 40 `x` items have a `distractorNotes` object, but not all 4 letters inside it. `ord-z01`–`ord-z30` use the newer 2–6-word descriptive-phrase option style (e.g. `'Avseende ett hypotetiskt scenario som inte inträffade'`) but carry **no `explanationData`/`distractorNotes` field at all** — confirmed via grep (0 of 30 `z` items have the field), a flat `{ id, type, source, number, text, options, answer, explanation, tags, difficulty }` shape. This meant the "add a `distractorNotes.E` entry matching the item's existing style" instruction resolved differently per group: `x`-items got a new `distractorNotes.E` entry (matching their existing partial-annotation style); `z`-items got only the new `options.E` value and **no** `distractorNotes.E`, since introducing that field where the other 4 options don't have one would be inconsistent with the batch's own established style, not matching it. Per-item taxonomy gap analysis was done against each item's existing 3 wrong options' implied mechanism (inferred from `distractorNotes` text where present, from the option's semantic relationship to the target where absent), picking whichever of the 4 categories was thinnest for that item, same as part 1's method. Cross-referencing target words also surfaced the same bank-level duplicate-target issue part 1 flagged: `EPISTEMOLOGI` appears as both `ord-x23` and `ord-z06`, and `DIKOTOMI` appears as both `ord-x40` and `ord-z15` — noted upfront so the writing pass (next task) could deliberately pick non-colliding mechanisms for each pair.

- [x] Write the 5th option for all 70 items, appending as `E` (matching part 1's approach so `answer` never needs updating), with a `distractorNotes.E` entry matching each item's existing style. Check every new option against its own target word for accidental synonymy, and — since part 1 found the bank has some duplicate target words recurring across items — check new options against *other nearby items'* correct answers too, not just the current item's own 4 existing options, to avoid a repeat of the near-miss part 1 caught (a candidate 5th option that turned out to be another item's actual correct answer). Work in batches of ~20, running `tsc --noEmit` after each batch.

  **Completion note:** Before writing anything into `questions.ts`, built a small extraction script that parsed all 168 ORD items (both this phase's 70 and part 1's already-completed 98) into a structured `answer_text` reference set — every item's correct-option string, lowercased — plus each item's own 4-option set, so every candidate 5th option could be automatically checked against (a) its own item's existing 4 options and (b) every other item's correct-answer text in the *entire* bank (broader than "nearby items" — checked the whole 168, not just the local x/z range, since duplicate targets like `EPISTEMOLOGI`/`DIKOTOMI` show the bank's duplication isn't confined to adjacent ids). Ran this check against all 70 drafted options before applying any edit: **zero collisions** — no new option matched an existing option in its own item, and no new option matched any correct-answer text anywhere in the 168-item bank. For the two duplicate-target pairs, deliberately used different mechanisms so the pair doesn't train the same trick: `ord-x23`/`EPISTEMOLOGI` got `epistolär` (false friend via "epist-", real word meaning "relating to letter-writing," not kunskapslära) while `ord-z06`/`EPISTEMOLOGI` got the classic epistemologi/epidemiologi mix-up (`'Medicinsk disciplin som studerar sjukdomars utbredning i en population'`); `ord-x40`/`DIKOTOMI` got `dialektik` (same-domain-wrong-specificity — a thesis/antithesis process, not a static two-way split) while `ord-z15`/`DIKOTOMI` got an antonym-trap phrase (`'En sammanslagning av tidigare åtskilda kategorier till en enda gemensam grupp'` — merging, the opposite of dividing). Applied in 4 batches — `x01–x20`, `x21–x40`, `z01–z15`, `z16–z30` — via a small Python script that regex-scoped each edit to the line containing that item's own `id`, appending `, E: '<value>'` to `options` (and, for `x`-items only, `, E: '<note>'` to `distractorNotes`); ran `tsc --noEmit` after each of the 4 batches, all compiled clean on the first attempt, no stall. Distribution across the taxonomy's 4 categories for the 70 new options: **morphological false friend 31** (e.g. `ord-x08` KOHERENT → `adherent`, sharing the "-herent" ending but meaning "follower/adherent," not "sammanhängande"; `ord-z09` TELEOLOGI → `'Vetenskapen om signalöverföring...på långt avstånd'`, confusing Greek télos [goal] with tele- [distance]), **antonym trap 15** (e.g. `ord-z16` REKVISIT → `'En valfri omständighet utan avgörande betydelse för utfallet'`, the direct opposite of a necessary requirement), **same-domain-wrong-specificity 16** (e.g. `ord-x27` STAGNATION → `recession`, both economic-decline-adjacent but denoting active contraction vs. mere non-growth; `ord-z25` KASUISTIK → `'En religiös biktprocess...'`, historically linked but a different concept from the analytical method itself), **near-miss register/connotation 8** (e.g. `ord-z14` MACHIAVELLISK → `'Karismatisk och starkt uppskattad av folket som ledare'`, a different leadership trait in the same domain). No item was forced through a weak/throwaway option.

- [x] Run `npm run build` and `npm test`. Spot-check 10-12 of the 70 items by hand for the same three things part 1 checked: genuinely wrong (not a sneaky synonym), a real correctly-used word, and non-trivial (not an obvious throwaway).

  **Completion note:** `npm run build` (`tsc && vite build`) completed with **zero TypeScript errors** (only the pre-existing "chunks larger than 500 kB" bundle-size advisory, unrelated to this change). `npm test` (`vitest run`) — **14/14 tests passed**. Ran a programmatic structural check across all 168 ORD items (both phases): every item has exactly 5 options (`{5: 168}` distribution, no other count), every `answer` letter is present among its own item's options, and every `answer_text` resolves correctly — **0 issues**. Hand spot-checked 12 items across both formats and both duplicate-target pairs: `ord-x01` (AMBIVALENT→tveksam, E=`ambitiös`), `ord-x14` (VOLATIL→lättflyktig, E=`voluminös`), `ord-x23` (EPISTEMOLOGI→kunskapslära, E=`epistolär`), `ord-x28` (ARBITRÄR→godtycklig, E=`arbitrage`), `ord-x33` (SYNTES→sammanslagning, E=`syntax`), `ord-x40` (DIKOTOMI→tvådelning, E=`dialektik`), `ord-z06` (EPISTEMOLOGI→"Filosofisk disciplin...", E=`'Medicinsk disciplin som studerar sjukdomars utbredning...'`), `ord-z11` (REDUNDANT→"Överflödig...", E=`'Kraftigt nedbantad och strippad...'`), `ord-z15` (DIKOTOMI→"En indelning i två...", E=`'En sammanslagning av tidigare åtskilda kategorier...'`), `ord-z19` (AXIOMATISK→"Grundad på antaganden...", E=`'Avseende symmetrisk rörelse kring en central rotationsaxel'`), `ord-z27` (IMMANENT→"Inneboende i eller verksam inifrån...", E=`'Motståndskraftig mot och opåverkad av yttre störningar...'`), `ord-z30` (DETERMINISTISK→"Avseende tanken att alla händelser...", E=`'Avseende ett fastställt slutdatum...'`) — all 12 confirmed: `answer` unchanged and still correct, the new `E` genuinely wrong (none double as a valid synonym, including the two duplicate-target pairs which use different mechanisms from each other), a real/correctly-used Swedish word or phrase, and plausible enough to not be eliminable on sight.

- [x] Report: confirm all 70 items now have 5 correctly-keyed options, taxonomy-category distribution, a few concrete examples, any items flagged as genuinely hard. Then do the **whole-ORD-bank final check**: confirm all 168 ORD items (both phases combined) now have exactly 5 options — this closes out the entire ORD retrofit initiative from Phase 06's audit.

  **Completion note:** All 70 `ord-x01`–`ord-x40` and `ord-z01`–`ord-z30` items now have exactly 5 options (`A`–`E`), `answer` unchanged in every case (append-only, no renumbering). Taxonomy distribution across the 70 new options: **morphological false friend 31 (44%), same-domain-wrong-specificity 16 (23%), antonym trap 15 (21%), near-miss register/connotation 8 (11%)** — every category represented, no single category dominating. Concrete examples: `ord-x05` LATENT → `lateral` (shares "lat-" root, means "sidelong," not "dold"); `ord-z02` HERMENEUTISK → `'Avseende att vara fullständigt förseglad och ogenomtränglig'` (the classic hermeneutisk/hermetisk mix-up); `ord-x37` PLAUSIBEL → `populär` (being liked ≠ being credible-but-unproven). The two duplicate-target pairs (`EPISTEMOLOGI`: `x23`/`z06`; `DIKOTOMI`: `x40`/`z15`) were the trickiest items — not because a plausible distractor was hard to find, but because each pair needed a *different* mechanism from its sibling to avoid training the same trick twice; both pairs were resolved (see task 2 note) without forcing a weak option. No item in this phase's 70 was left with a throwaway/obviously-eliminable 5th option, including the narrowest technical targets (`AXIOMATISK`, `SYLLOGISM`, `TAUTOLOGI`).

  **Whole-ORD-bank final check:** Programmatically verified across the entire `src/data/questions.ts`: all **168** ORD items (`ord-e01`–`ord-e98` from Phase 08, `ord-x01`–`ord-x40` + `ord-z01`–`ord-z30` from this phase) now have **exactly 5 options** each (option-count distribution: `{5: 168}`, zero exceptions), every `answer` letter resolves to a valid, present option. This closes out the ORD 4-vs-5-option format-mismatch gap that Phase 06's audit originally flagged — the entire ORD retrofit initiative (Phases 08–09) is complete.
