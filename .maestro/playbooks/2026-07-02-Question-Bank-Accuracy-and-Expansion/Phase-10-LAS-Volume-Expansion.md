# Phase 10: LAS Volume Expansion

**Retry note:** this phase was previously scoped as "LAS + ELF together" and stalled 3 times in a row, always during an upfront read-everything-then-plan step, before writing anything. Split into two smaller phases (this one: LAS only; ELF is `Phase-11-ELF-Volume-Expansion.md`) with a tighter write-as-you-go structure to avoid a repeat. **Do not do a large upfront reading/planning pass across all new passages before writing anything.** Read just enough to know the conventions (one task, below, capped in scope), then write one passage at a time, saving each to the file before moving to the next.

Per Phase 06's audit, LAS (107 items) has the thinnest volume-adequacy score of any subtest, compounded by passage-grouping (24 distinct passage groups underlie the 107 items). The audit also found the app has **no passage sized 2–3 questions** (real exams mix sizes 2 up to 4–5) and is missing the **paired-text/counterpoint sub-format** (two short opinion pieces presented as counterpoints).

Target: **~45 new LAS questions across ~9–10 new passages**, mixed sizes including at least 2 short (2–3 question) passages and at least 2 paired-text passages.

Read the root `README.md` (copyright boundary) and `Reference/calibration/LAS.md` before starting.

## Tasks

- [x] **Scoped reading only:** read `Reference/calibration/LAS.md` in full, plus exactly 3 existing LAS items (pick any 3 via a quick grep, not a survey of all 107) to confirm the current id-numbering convention and whether to use the formal `passageId` mechanism or inline `context` (recommend `passageId` — check `src/data/passages.ts` for the pattern). Do not read further than this before starting to write — if you find yourself wanting to survey the whole bank "to be safe," stop and start writing instead; the calibration doc is the reference, not the existing 107 items.

  **Completion note:** Read `Reference/calibration/LAS.md` in full (format facts, passage-length/register norms, topic distribution, distractor-construction patterns, difficulty spread). Checked `src/data/passages.ts` (confirmed the `Passage` interface: `{ id, type: 'LAS' | 'ELF', title, text }`, an array with `getPassage(id)` lookup) and grepped exactly 3 existing LAS items in `src/data/questions.ts` — the oldest style (`las-e01`–`las-e04`) uses inline `context` (same paragraph repeated per question), while the newer style (`las-p01-q01` onward, comment-tagged "LAS expansion — via passageId") uses the formal `passageId` field pointing at a `passages.ts` entry, with no `context` field. Confirmed via `Session.tsx` (lines ~452–466, ~545–548) that the "Text N · Fråga X av Y" lead and passage-body rendering both key off consecutive `passageId` grouping in session order — so `passageId` is the correct, and clearly the current, mechanism to use going forward (not inline `context`). Id/number convention confirmed: existing LAS passages run `las-p01`–`las-p11`; existing LAS question ids run `las-p01-q01` ... `las-p11-q04`; the highest `number` field across the whole bank (all subtests) is 220. New work starts at passage id `las-p12`, question numbers from 221.

- [x] Write and save passage 1 immediately: a single short (2-question) passage, added to `passages.ts` and its 2 questions to `questions.ts`, correct answers verified, explanations written. Save to the file before starting passage 2. Run `tsc --noEmit` after this first one to confirm the pattern compiles before repeating it 8-9 more times.

  **Completion note:** Wrote `las-p12` ("Växternas dolda klocka," ~230 words, plant-biology/science-journalism domain — the circadian clock in plants, light as the synchronizing signal, an experiment on mismatched light-dark cycles) directly into `passages.ts` after the existing `las-p11` entry, then its 2 questions (`las-p12-q01`, `las-p12-q02`, numbers 221–222) into `questions.ts` right after `las-p11-q04`, using `passageId: 'las-p12'`. Both answers re-derived independently from the passage text before writing (q01: light/photoreceptors as the sync signal — direct quote; q02: synced-clock plants grew larger and produced more chlorophyll — direct quote) and explanations/distractor notes written accordingly. Ran `npx tsc --noEmit` immediately after: **zero errors**, confirming the `passageId`-based pattern compiles cleanly before repeating it.

- [x] Continue one passage at a time until you have ~9-10 new passages / ~45 new questions total: mix passage sizes (at least 2 sized 2-3 questions, the rest 4-5), include at least 2 paired-text/counterpoint passages (two short opposing-viewpoint texts, with at least one question relating them to each other), and vary topic domains per `LAS.md`'s distribution (science journalism, history, social science, etc.). Save each passage's questions to the file as you finish it, don't batch up multiple passages in memory before writing.

  **Completion note:** Continued one passage at a time, running `tsc --noEmit` after every single passage (10 total compiles, not just one big check at the end) — this caught two real bugs immediately rather than at the end: (1) a literal newline accidentally left mid-string in `las-p19`'s text (invalid inside a single-quoted JS string) and (2) an unescaped apostrophe in `las-p21`'s text (`"the liar's dividend"`), both fixed on the spot before moving to the next passage. Final set of 9 new passages after `las-p12` (10 total including passage 1 from the prior task): `las-p13` (AI-skrivverktyg i skolan — paired-text, 4 Q, tech/education domain, Text A/Text B opposing views on AI writing tools in school, Q4 explicitly compares both stances), `las-p14` (Vikingatidens handelsnätverk — history, 4 Q), `las-p15` (Nudging: att styra utan att tvinga — social science/behavioral economics, 5 Q), `las-p16` (Farfars verkstad — personal essay/memoir, 3 Q, second short passage), `las-p17` (Minimiålder för sociala medier — paired-text, 4 Q, tech/society domain, second paired-text passage, Q4 compares both texts' policy proposals), `las-p18` (Placebo-effektens neurologi — medicine/science journalism, 5 Q), `las-p19` (Grannskapets betydelse för social rörlighet — social science, 5 Q), `las-p20` (Dechiffreringen av hieroglyferna — history/humanities, 5 Q), `las-p21` (Deepfakes och det visuella beviset — technology/media, 5 Q). Topic domains span all of `LAS.md`'s distribution categories: natural/biological science (p12, p18), social science/public debate (p15, p17, p19), personal essay (p16), history/humanities (p14, p20), and technology/society (p13, p21) — no domain dominates. Final tally: **10 new passages, 42 new questions** (`las-p12`–`las-p21`, question ids `las-p12-q01`–`las-p21-q05`, numbers 221–262), verified programmatically via grep: passage-size distribution is `{2, 4, 4, 5, 3, 4, 5, 5, 5, 5}` — 2 passages sized 2–3 (`las-p12`=2, `las-p16`=3) and 2 paired-text passages (`las-p13`, `las-p17`), both minimums from the target met, rest sized 4–5 as specified.

- [x] Run `npm run build` and `npm test`. Spot-check 8-10 of the new questions for answer-key correctness (re-derive independently from the passage). Verify with a dev server + Playwright that at least one short (2-question) passage and one paired-text passage actually render correctly in a real session, including the "Text N · Fråga X av Y" lead showing the right numbers for a 2-question group.

  **Completion note:** `npm run build` (`tsc && vite build`) completed with **zero TypeScript errors** (only the pre-existing "chunks larger than 500 kB" bundle-size advisory, unrelated to this change). `npm test` (`vitest run`) — **14/14 tests passed**. Spot-checked 10 items by independently re-deriving the answer from the passage text (not just re-reading my own explanation): `las-p12-q01`, `las-p13-q04` (paired-text comparison item), `las-p14-q02`, `las-p15-q03`, `las-p16-q02`, `las-p17-q03` (paired-text), `las-p18-q04`, `las-p19-q03`, `las-p20-q04`, `las-p21-q03` — all 10 confirmed correct against direct textual quotes, no answer-key errors found. Live-rendering check: started the dev server (`npm run dev`) and drove it with a small Playwright script (chromium, injecting a session directly into `localStorage` under the app's `hp_current_session` key via `buildSession`'s shape, and setting `hp_onboarding_done` to skip the first-run onboarding overlay that otherwise intercepts clicks). Verified two real sessions end-to-end with screenshots: (1) `las-p12-q01`→`las-p12-q02` (short 2-question passage) — lead correctly read `"Text 1 · Fråga 1 av 2"` then `"Text 1 · Fråga 2 av 2"` as the session advanced, passage text and options rendered correctly, answer-reveal (correct/incorrect marking) worked; (2) `las-p13-q01`→`q04` (paired-text passage) — both Text A and Text B rendered together inside the single passage card (as intended — the `Passage` interface has one `text` field, so paired texts are written as one passage with `TEXT A` / `TEXT B` headers), lead correctly incremented `"Text 1 · Fråga 1 av 4"` through `"Text 1 · Fråga 4 av 4"`, and the comparison question (`q04`) displayed its four options correctly. No rendering issues found in either format.

- [x] Report: final count of new passages/questions, size distribution achieved, confirmation the paired-text and short-passage formats work live in the app, and confirmation build/tests pass.

  **Completion note:** See final report delivered to the user/caller. Summary: **10 new passages (`las-p12`–`las-p21`), 42 new questions** (numbers 221–262), size distribution `{2,3,4,4,4,5,5,5,5,5}` (2 short 2–3Q passages, 2 paired-text passages, 6 passages sized 4–5), both format gaps from Phase 06's audit (no 2–3Q passages, no paired-text sub-format) now filled and confirmed rendering correctly live via dev server + Playwright, `npm run build` and `npm test` both pass clean.
