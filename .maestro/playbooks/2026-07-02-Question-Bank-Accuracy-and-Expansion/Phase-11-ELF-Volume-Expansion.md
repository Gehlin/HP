# Phase 11: ELF Volume Expansion

Second half of what was originally one combined "LAS+ELF" phase, split after 3 stalls (see `Phase-10-LAS-Volume-Expansion.md`'s retry note for why). **Do not do a large upfront reading/planning pass before writing anything** — read just enough to confirm conventions, then write one passage at a time, saving each before moving to the next.

Per Phase 06's audit, ELF (100 items) has the thinnest volume-adequacy score alongside LAS. Same missing-format gap: no passage sized 2–3 questions (existing app passages are all 4–6). Unlike LAS, there's no real ELF exam content anywhere in the source corpus to calibrate against (UHR never publishes it) — work from `Reference/calibration/ELF.md`'s structural facts only (10q/22min, passage-based, comes after MEK) plus your own knowledge of good English-reading-comprehension item design. Don't strain to imitate a "real style" the corpus doesn't actually document.

Target: **~45 new ELF questions across ~9–10 new passages**, mixed sizes including at least 2 short (2–3 question) passages.

Read the root `README.md` (copyright boundary — nothing to derive from for ELF specifically, since there's no real content, but the general principle of writing fresh original content still applies) before starting.

## Tasks

- [x] **Scoped reading only:** read `Reference/calibration/ELF.md` in full, plus exactly 3 existing ELF items (quick grep, not a survey) to confirm the id-numbering convention, the English-passage/Swedish-question/Swedish-options structure, and whether to use `passageId` or inline `context`. Don't read further before starting to write.

  **Completion note:** Read `Reference/calibration/ELF.md` in full (10q/22min, last block of the verbal provpass, structurally identical to LÄS's allocation, no real content exists in the corpus to calibrate against). Read the root `README.md` copyright boundary. Grepped exactly 3 existing ELF items plus enough surrounding context to confirm structure: **correction to this task's stated assumption** — the actual codebase convention is **English passage + English question text + English options** throughout (`elf-e01`–`elf-e20` and `elf-p01-q01` onward), not Swedish question/options as this task's title assumed. This makes sense for ELF specifically (it tests English reading comprehension directly, unlike LÄS which tests Swedish reading with Swedish questions) and matches every one of the ~100 existing ELF items with no exception found, so the new content follows the existing all-English convention rather than the task doc's assumption. Confirmed via `src/data/passages.ts`: `Passage` interface is `{ id, type: 'LAS' | 'ELF', title, text }` with a `getPassage(id)` lookup; existing ELF passages run `elf-p01`–`elf-p08`. Confirmed via `src/data/questions.ts`: the oldest style (`elf-e01`–`elf-e04`, `elf-e05`–`elf-e20`) uses inline `context` (full passage text repeated per question); the newer style (`elf-p01-q01` onward, comment-tagged "ELF expansion — via passageId") uses the formal `passageId` field referencing a `passages.ts` entry — this is the mechanism to use going forward, matching Phase 10's LAS convention exactly. Id/number convention: new passages start at `elf-p09`; the highest `number` field across the entire question bank (all subtests, confirmed by grep across the whole file, not just ELF) is 262 (the last one used by Phase 10's LAS expansion), so new ELF numbers start at 263. Current bank size confirmed: LAS = 149 items (post-Phase-10), ELF = 100 items (pre-this-phase).

- [x] Write and save passage 1 immediately: a short (2-question) English passage + 2 Swedish questions/options, correct answers verified, explanations written, added to `passages.ts`/`questions.ts`. Run `tsc --noEmit` to confirm the pattern compiles before repeating.

  **Completion note:** Wrote `elf-p09` ("The Bystander Effect," ~330 words, social-psychology domain — the 1964 case, diffusion of responsibility, pluralistic ignorance, the practical training implication) directly into `passages.ts` after the existing `elf-p08` entry. Wrote its 2 questions (`elf-p09-q01`, `elf-p09-q02`, numbers 263–264) into `questions.ts` right after `elf-p08-q04`, using `passageId: 'elf-p09'`, question text and options in English per the corrected convention noted in task 1 (not Swedish as originally assumed). Both answers re-derived independently from the passage text before writing (q01: diffusion of responsibility named directly, with the passage explicitly ruling out "simple indifference" as a distractor-trap; q02: the training practice's stated purpose is to counteract diffusion of responsibility by pointing at one specific person). Ran `npx tsc --noEmit` immediately after: **zero errors**, confirming the `passageId`-based pattern compiles cleanly before repeating it for the remaining passages.

- [x] Continue one passage at a time to ~9-10 new passages / ~45 new questions: mix sizes (at least 2 sized 2-3 questions, rest 4-5), vary topics and register (university-entry-level English, not beginner or highly technical academic-journal level), save each passage as you finish it.

  **Completion note:** Continued one passage at a time, running `tsc --noEmit` after every single passage (9 more compiles after passage 1, 10 total) — **zero errors at every step**, unlike Phase 10's LAS work, no syntax bugs surfaced this time (likely because ELF passages are single continuous English paragraphs without the `TEXT A`/`TEXT B` embedded-quote complexity that caused Phase 10's two bugs). Final set of 9 more passages after `elf-p09` (10 total including passage 1 from the prior task): `elf-p10` (Grid-Scale Battery Storage — energy/technology, 3 Q, second short passage), `elf-p11` (The Origins of Vaccination — medical history, 5 Q), `elf-p12` (Cognitive Dissonance — psychology, 5 Q), `elf-p13` (The Gig Economy — labour economics/society, 5 Q), `elf-p14` (The Problem of Space Debris — science/technology, 5 Q), `elf-p15` (Universal Basic Income — economics/politics, 5 Q), `elf-p16` (The Science of Habit Formation — psychology/neuroscience, 5 Q), `elf-p17` (Ocean Plastic Pollution — environment, 5 Q), `elf-p18` (The Fermi Paradox — astronomy/philosophy of science, 5 Q). Topics deliberately avoid overlap with existing ELF passages (sleep, biodiversity economics, attention economy, gene editing, urban heat, risk perception, AI creativity, AI/LLMs, democratic recession) and with LAS's Swedish-language topics (e.g. avoided "nudging" and "placebo," both already covered in LAS, in favour of UBI, habit formation, gig economy, vaccination history, cognitive dissonance, space debris, ocean plastics, and the Fermi paradox instead) — register kept at university-entry level throughout (no untranslated jargon left unexplained, no beginner-level vocabulary). Final tally: **10 new passages, 45 new questions** (`elf-p09`–`elf-p18`, question ids `elf-p09-q01`–`elf-p18-q05`, numbers 263–307), verified programmatically via grep: passage-size distribution is `{2, 3, 5, 5, 5, 5, 5, 5, 5, 5}` — exactly 2 short passages (`elf-p09`=2, `elf-p10`=3) filling the missing-format gap Phase 06 flagged, rest sized 5, no duplicate ids found across the whole bank (`passages.ts` and `questions.ts` both checked).

- [x] Run `npm run build` and `npm test`. Spot-check 8-10 new questions for answer-key correctness. Verify with dev server + Playwright that at least one short (2-question) passage renders correctly live, including correct "Text N · Fråga X av Y" labeling.

  **Completion note:** `npm run build` (`tsc && vite build`) completed with **zero TypeScript errors** (only the pre-existing "chunks larger than 500 kB" bundle-size advisory, unrelated to this change). `npm test` (`vitest run`) — **14/14 tests passed**. Spot-checked 9 items via a sub-agent that independently re-derived each answer from the passage text (not from the pre-written explanation field): `elf-p09-q02`, `elf-p10-q02`, `elf-p11-q03`, `elf-p12-q03`, `elf-p13-q04`, `elf-p14-q05`, `elf-p15-q05`, `elf-p16-q03`, `elf-p18-q04` — all 9 confirmed correct against direct textual quotes, no answer-key errors found. Live-rendering check: started the dev server (`npm run dev`) and drove it with a Playwright script (chromium), injecting a 2-question session (`elf-p09-q01`, `elf-p09-q02`) directly into `localStorage` under `hp_current_session` via `buildSession`'s shape, and setting `hp_onboarding_done` to `'1'` (the exact string the app checks for, confirmed by reading `Onboarding.tsx`'s `isOnboardingDone()`) to skip the onboarding overlay that otherwise intercepts clicks. Note: this app uses `BrowserRouter`, not hash routing, so the session route is `/session` not `/#/session` — navigating to the wrong path silently rendered the Home page instead of erroring, initially causing a false "Q1 lead: null" reading before being corrected. Verified the full 2-question passage end-to-end with screenshots: lead read `"Text 1 · Fråga 1 av 2"` on question 1 (`HP-träning · #263`), the passage text ("The Bystander Effect") rendered correctly above the question, all 4 options displayed; after clicking an option and advancing, the lead correctly updated to `"Text 1 · Fråga 2 av 2"` with the progress indicator showing `"2 / 2"` and question `HP-träning · #264` with its own 4 options. No rendering issues found — the short-passage format works identically to the existing 4-6 question passages.

- [x] Report: final count of new passages/questions, size distribution achieved, confirmation the short-passage format works live, and confirmation build/tests pass. Then do a combined check across this phase and Phase 10 together: confirm LAS is now ~152 items and ELF ~145 items (107+45, 100+45), closing out the volume-expansion work Phase 06 flagged as highest-priority-after-DTK.

  **Completion note:** Final tally delivered to the user/caller. Summary: **10 new ELF passages (`elf-p09`–`elf-p18`), 45 new ELF questions** (numbers 263–307), size distribution `{2,3,5,5,5,5,5,5,5,5}` (2 short 2–3Q passages, 8 passages sized 5), the missing 2–3Q format gap from Phase 06's audit now filled and confirmed rendering correctly live via dev server + Playwright, `npm run build` and `npm test` both pass clean, no duplicate ids anywhere in `passages.ts` or `questions.ts`. **Combined check across Phase 10 + Phase 11:** ELF is now exactly **145 items** (100 + 45), matching this task's estimate exactly. LAS is now **149 items**, not the ~152 this task's own estimate guessed — this is not a discrepancy to fix, it is simply because Phase 10 actually delivered 42 new LAS questions rather than the full 45 it targeted (107 + 42 = 149, correctly recorded in Phase 10's own completion notes at the time); both banks are within their respective phases' "~45 new questions" target range, and both now have the previously-missing 2–3Q short-passage format, closing out the volume-expansion work Phase 06 flagged as highest-priority-after-DTK.
