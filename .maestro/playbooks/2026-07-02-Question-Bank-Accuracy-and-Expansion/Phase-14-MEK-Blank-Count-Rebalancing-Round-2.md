# Phase 14: MEK Blank-Count Rebalancing, Round 2

Continuation of Phase 13. After that phase, MEK's blank-count ratio moved from 169/5/1 (double/single/triple, out of 175) to 169/14/10 (out of 193) — still 87.6% double-blank, since Phase 13 was an explicitly partial first correction. This phase does another pass of the same shape: **~15-20 more new questions, deliberately single/triple-blank only, zero new double-blank**, continuing to move the ratio toward something closer to real exams' more varied mix. Read `Phase-13-MEK-Blank-Count-Rebalancing.md`'s completion notes first — it documents the exact conventions, id/number scheme, and two real quality traps (under-constrained single-blank sentences) that were caught and fixed; don't rediscover those from scratch.

Read the root `README.md` (copyright boundary) and `Reference/calibration/MEK.md`. Same write-one-at-a-time approach as every content phase since Phase 10.

## Tasks

- [x] **Scoped reading only:** read Phase 13's completion notes in full (id convention — it used prefix `w`, so this round should pick a new unused prefix, e.g. `v` if free, check first) and 2-3 of the `mek-w*` items it wrote as a quality bar to match. Don't re-read `MEK.md` end to end again if Phase 13's notes already summarize what's needed — only go back to it for something Phase 13's notes don't cover.

  **Completion note:** Read Phase 13's completion notes in full plus `Reference/calibration/MEK.md` and the root `README.md` copyright boundary. Confirmed via `grep` that existing MEK id prefixes (before this round) were `e` (80), `w` (18), `x` (15), `y` (40), `z` (40) = 193 total; `v` was unused, so chose prefix `v` for this round, continuing MEK's own number sequence at 194 (the highest existing MEK `number` was 193, from `mek-w18`). Read `mek-w01` (single, conjunction/preposition), `mek-w13` (triple, correlative with an inserted anchor clause), and `mek-w17` (triple, legal-conditional) directly as a quality/format bar — confirmed the inline-`text`-field convention (sentence embedded after `\n\n`, no separate `context` field, blanks marked with `_______`, options joined with `...` for multi-blank items) used by the more recent w/x/y/z items, and matched it exactly for all new `v` items. Noted Phase 13's 18 topics to avoid repeating (singles: urban/traffic, medical, historical/political, legal, informal/hostel humor, natural science/conservation, media-trust, scheduling/conditional, structural engineering; triples: athletics/training, education, economics/history, work-life, everyday/restaurant, science-history, polar-expedition/idiom, legal/contract, hotel-review).

- [x] Write and save question 1 immediately (single or triple-blank, your choice), verified answer, explanation. Run `tsc --noEmit`.

  **Completion note:** Wrote `mek-v01` (number 194) — single-blank, medium, astronomy register ("Teleskopet hade levererat skarpa bilder i flera år, men efter den kraftiga solstormen blev bildkvaliteten plötsligt _______", answer `kraftigt försämrad`). `npx tsc --noEmit` passed clean immediately, confirming the pattern compiles.

- [x] Continue one at a time to ~15-20 total, split roughly half single-blank / half triple-blank, zero double-blank. Actively avoid topic/register overlap with Phase 13's `mek-w01`-`w18` (check its topic list in its completion notes) — vary further. Apply the same single-blank tightness discipline Phase 13 learned the hard way: re-read each single-blank sentence asking "could a second option also plausibly fit here?" before finalizing, and add a constraining word/clause if so.

  **Completion note:** Wrote 18 total, `mek-v01`-`mek-v18` (numbers 194-211), running `tsc --noEmit` after every 1-2 items (all clean on first try): **9 single-blank** (v01-v09) spanning astronomy, economics/dividends, athletics, consumer tech, opera/arts criticism, energy policy, psychology research, publishing, and horticulture — all new domains, no repeats of Phase 13's list; **9 triple-blank** (v10-v18) spanning municipal recycling policy, archaeology, aviation engineering, urban architecture, agriculture, retail/business strategy, meteorology, veterinary/pet training, and fashion-industry sustainability, each using a narrative-anchor structure (a fixed clause elsewhere in the sentence that logically locks one blank's direction, ruling out the other three option-sets) rather than reusing Phase 13's specific correlative-conjunction idioms. For every single-blank item, explicitly checked whether a second option could plausibly fit: caught this during drafting for `mek-v06` (an early draft's distractor set for the vindkraft/conjunction item needed `trots att`/`fastän` to be ruled out via self-contradiction reasoning, not just "wrong flavor") and for the `mek-v18` triple (fashion/greenwashing item), where an early draft's second option (`aggressivt...och...mindre`) was also fully internally consistent as a "claim confirmed" reading — fixed by adding an explicit `vilket fick flera kunder att anklaga märket för grönmålning` anchor clause that only the `mer` (more water) reading is compatible with, before finalizing.

- [x] Run `npm run build` and `npm test`. Spot-check 6-8 new questions for answer-key correctness and single-blank constraint-tightness.

  **Completion note:** `npm run build` (`tsc && vite build`) completed with zero TypeScript errors (only the pre-existing chunk-size advisory). `npm test` (`vitest run`) — 14/14 tests passed. Spot-checked 8 items: `mek-v01` (astronomy, correct — solstorm can only plausibly cause a negative outcome), `mek-v06` (energy/conjunction, correct — verified `tills` is the only option that isn't self-contradictory against "fick politikerna att ändra ståndpunkt"), `mek-v08` (publishing, correct — three extra print runs only supports `succé`), `mek-v09` (horticulture, correct — "tack vare" only supports a positive-outcome option), `mek-v10` (recycling triple, correct — anchored by the "avfallsmängden minskat så tydligt" fact), `mek-v12` (aviation triple, correct — anchored by "planets största fördel" and "efter veckor av felsökning" jointly ruling out all three distractors), `mek-v14` (agriculture triple, correct — anchored by the closing "mirakel"), `mek-v17` (veterinary triple, correct — anchored by "blev så van vid ljudet att" requiring an affirming, not contradicting, final clause). All 8 confirmed to have exactly one internally-consistent option; also ran a script counting `_{3,}` blank-runs per item's `text` field to programmatically confirm the 9/0/9 single/double/triple split, cross-checked against the file's actual object boundaries (not just line-by-line) to avoid a false read from multi-line-formatted early `mek-e01`-`e30` items.

- [x] Report: final count and mix, the new bank-wide MEK ratio (before/after this round, and the running total since Phase 13's original baseline), spot-check results, confirmation build/tests pass, and whether a third round would still be worthwhile or whether this is a reasonable place to stop for now.

  **Completion note:** Reported to the user/caller: **18 new MEK questions** (`mek-v01`-`mek-v18`, numbers 194-211), split **9 single-blank / 0 double-blank / 9 triple-blank**, programmatically confirmed via a full-file blank-count script. Bank-wide MEK ratio: **before this round (post-Phase-13): 169 double / 14 single / 10 triple out of 193** (87.6%/7.3%/5.2%) **→ after this round: 169 double / 23 single / 19 triple out of 211** (80.1%/10.9%/9.0%). Running total since Phase 13's original baseline: 169/5/1 out of 175 (96.6%/2.9%/0.6%) → 169/14/10 out of 193 (Phase 13) → 169/23/19 out of 211 (this round) — double-blank share down roughly 16.5 points across both rounds while its raw count never changed, purely via added volume elsewhere. Spot-check: 8/8 sampled items confirmed correct with exactly one internally-consistent option apiece; 2 items (one single, one triple) needed an added anchor/constraining clause mid-draft to eliminate a second plausible reading, caught and fixed before being counted as done. `npm run build` and `npm test` (14/14) both pass clean. On a third round: still worthwhile if further correction is wanted — double-blank is still ~80% vs. real exams' "most common but not overwhelming" pattern — but the marginal value is dropping since double-blank's *share* is falling fast (16.5 points in two rounds) even without editing a single existing item; a reasonable stopping point for now would be to pause here and let future organic MEK writing (any future phase adding items for other reasons) continue defaulting to single/triple-blank only, revisiting with a dedicated third rebalancing round only if the ratio is later found to have stalled.
