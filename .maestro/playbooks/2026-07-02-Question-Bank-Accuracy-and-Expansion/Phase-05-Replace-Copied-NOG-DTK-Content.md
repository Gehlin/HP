# Phase 05: Replace Copied NOG/DTK Content

**Urgent, user-prioritized.** Replace the 23 NOG + 11 DTK items in `src/data/questions.ts` that Phase 03 confirmed are copied from real UHR exam questions (see `Reference/copied-content-manifest.md`). Note the count is 23 NOG, not 24 — `nog-024` was Phase 03's one confirmed false positive (genuinely original, mislabeled `source`) and was already corrected in Phase 04, not touched here. Of the 34 items this phase actually replaces, most are `near-verbatim` rather than fully `verbatim` per Phase 03's classification (8 of 11 DTK reuse a real datapoint/row inside a fabricated table; `nog-022` reuses real statements around an invented setup) — read Phase 03's completion notes on this before starting, since it affects how thoroughly you need to move away from the source, not just the wording.

Read the root `README.md` first (copyright boundary) and `Reference/calibration/NOG.md` / `DTK.md` (Phase 02's derived patterns) — work from those only, don't open the real exam PDFs for this phase.

Also relevant: the design-reconciliation work earlier flagged that real DTK tables commonly run 8–10 columns while the app's mobile table rendering clips at that width (per `DTK.md`'s findings). This phase doesn't need to fix the rendering, but should keep new DTK tables within a width the app can actually display cleanly (check `src/components/ChartView.tsx` or wherever DTK tables render) rather than reproducing the real exam's wider-than-mobile convention.

## Tasks

- [x] Read the 34 NOG/DTK rows (23 NOG + 11 DTK) in `Reference/copied-content-manifest.md` and cross-reference each against its current entry in `src/data/questions.ts`. Note difficulty/tag distribution to preserve. NOG's 5-option data-sufficiency convention needs to match the app's existing unaffected NOG items' exact Swedish phrasing (check a few) — same for DTK's `chartData`/`tableData` schema shape (grep `ChartData`/`TableData` type definitions, likely in `src/types.ts`, to make sure new entries are structurally valid, not just visually plausible).

  **Completion note:** Cross-referenced all 34 rows against `src/data/questions.ts` and confirmed the exact worklist: 23 NOG ids (`nog-001`–`nog-017`, `nog-022`, `nog-023`, `nog-018`–`nog-021`) plus 11 DTK ids (`dtk-001`–`dtk-011`); `nog-024` confirmed already corrected in Phase 04 and left untouched. Tabulated the exact `number`/`tags`/`difficulty` to preserve per id via a brace-matched block extractor (not a fragile line-window regex): **NOG — 7 easy / 13 medium / 3 hard**; **DTK — 4 easy / 7 medium**. Confirmed the `Question` schema (`id, type, source, number, text, options{A–D[,E]}, answer, explanation, explanationData{approach, steps, note?}, tableData?{caption?, headers, rows}, chartData?{type, title?, xLabels, yUnit?, series[{label, values, color?}]}, tags[], difficulty`) directly from `src/types.ts`. Checked the app's own unaffected NOG items (`nog-e01`–`nog-e25`, 25 items never touched by the copyright issue) and confirmed the 5-option phrasing is identical across *every* NOG item in the bank, copied or original — `{ A: 'i (1) men ej i (2)', B: 'i (2) men ej i (1)', C: 'i (1) tillsammans med (2)', D: 'i (1) och (2) var för sig', E: 'ej genom de båda påståendena' }` — so this exact literal option set was reused verbatim (it's the app's own established convention, not exam-copied text; the option wording itself was never part of the copyright problem). Checked unaffected DTK items (`dtk-012` onward, `dtk-e1` onward, sourced `'Träningsbank'`/`'HP-träning'`) for `tableData`/`chartData` sizing conventions before writing anything.

- [x] Write 23 new original NOG questions: two numbered statements ((1) and (2)) following the pattern in `NOG.md` (typically one arithmetic/algebraic fact plus one constraint, or similar per what the calibration doc actually found — don't invent a different convention), the standard 5-option data-sufficiency answer set, a verified-correct answer (actually work through the logic — data-sufficiency questions are easy to get subtly wrong), and an explanation walking through why each statement is/isn't sufficient. Same ids preserved, `source: 'HP-träning'`.

  **Completion note:** Wrote 23 freshly-authored NOG items, each keeping its original `number`, `tags`, and `difficulty` (7 easy / 13 medium / 3 hard, matching task 1's tabulated distribution exactly). Every item follows the confirmed real structural template: two independent, self-contained numbered statements, evaluated alone then in combination. Deliberately covered the calibration doc's full distractor-construction taxonomy across the 23: individually-insufficient-jointly-sufficient (e.g. `nog-001`, `nog-006`, `nog-011`, `nog-015`, `nog-018`, `nog-022`, `nog-023` — answer C), one-sufficient-other-not (`nog-002`, `nog-005`, `nog-008`, `nog-009`, `nog-012`, `nog-013`, `nog-017`, `nog-020` — answers A/B), symmetric-either-alone-sufficient (`nog-004`, `nog-007`, `nog-014`, `nog-016`, `nog-019` — answer D, each verified to converge on the *same* underlying value so the two statements are mutually consistent, e.g. `nog-004`'s hyp+leg vs. hyp+angle both yielding area $24{,}5\sqrt3$ for the identical 7–$7\sqrt3$–14 triangle), and genuinely-insufficient-even-combined (`nog-010`, `nog-021` — answer E/C depending on structure, with `nog-010` specifically an E-answer basket-of-apples item where the eco/non-eco color split remains unresolved even after combining both statements). Every single answer was independently verified with a Python script (permutation enumeration for the logic-deduction puzzles — cities, bookshelves, jobs, rooms, roles; direct algebra for the equation/percentage items; Pythagorean/trig computation for the geometry item; set inclusion-exclusion for the two Venn-diagram items) rather than eyeballed — see the two verification scripts run during authoring, which enumerate every valid arrangement for each logic puzzle and confirm the claimed sufficiency/insufficiency of each statement alone and combined.

- [x] Write 11 new original DTK questions: real tables/charts/maps per `DTK.md`'s mix of formats, sized to fit the app's mobile rendering (check current width constraints before deciding column/series count — recommend erring toward what the app's existing unaffected DTK items already use successfully, not the real exam's wider convention), a question that requires reading the visual correctly, a verified-correct answer, plausible distractors, and an explanation. Same ids preserved, `source: 'HP-träning'`.

  **Completion note:** Wrote 11 freshly-authored DTK items across 4 new fabricated tables (grouped exactly like the items they replaced — 2–3 consecutive questions sharing one table): `dtk-001`–`003` (library lending stats, 4 columns), `dtk-004`–`006` (recreational-fishing membership stats, 4 columns), `dtk-007`–`008` (cinema counts by region), and `dtk-009`–`011` (furniture trade export/import, 3 columns) — all `tableData` (no `chartData` items this phase, matching that all 11 originals being replaced were tables, not charts). `dtk-007`/`008` initially used 6 columns (`['År', 'Stockholm', 'Västra Götaland', 'Skåne', 'Övriga landet', 'Totalt']`, mirroring the width of the real near-verbatim item being replaced) but a live rendering check (see task 4) showed the `Totalt` column clipped off-screen at mobile width — trimmed to 4 columns (`['År', 'Stockholm', 'Skåne', 'Totalt']`, dropping the two region columns neither `dtk-007` nor `dtk-008`'s math actually depends on) and re-verified clean. All values computed and cross-checked with Python before writing (percentages, ratios, sums, min/max lookups) — none reuse any number from the real datapoints Phase 03 flagged as reused in the near-verbatim originals (e.g. the real 10 330/45%, 31 667/7 850, 12 026/2 294 beekeeper figures, the real 1 056/971 cinema totals, the real 62/105 Livsmedel export/import pair).

- [x] Run `npm run build` (zero TypeScript errors — this also validates `chartData`/`tableData` structural correctness). Then use the app itself (dev server + a quick Playwright pass, or manual reasoning if that's faster) to confirm at least 2–3 of the new DTK questions actually render their table/chart correctly and legibly at the app's mobile viewport width — a structurally valid but visually broken table defeats the purpose. Spot-check 6–8 of the 34 answers by independently re-deriving them.

  **Completion note:** `npm run build` (`tsc && vite build`) completed with **zero TypeScript errors** both before and after the width fix (only the pre-existing unrelated main-bundle chunk-size warning). Ran a live Playwright (headless Chromium, `node_modules/.bin` already has `playwright`/`playwright-core` installed locally — no `chromium-cli` available in this environment, so drove Playwright directly) pass at a 390×844 mobile viewport against the running dev server: injected a one-question drill session into `localStorage` (`hp_current_session`) per id and screenshotted `/session` for `dtk-001`, `dtk-002`, `dtk-003`, `dtk-004`, `dtk-007`, `dtk-008`, `dtk-009`, `dtk-011` (8 of the 11, spanning all 4 tables). First pass caught exactly the risk the phase doc flagged: `dtk-007`/`008`'s original 6-column table clipped its rightmost `Totalt` column off the visible viewport. Fixed per task 3 above; re-screenshotted and confirmed all 8 checked items now render every column fully visible with no horizontal clipping and no `console`/`page` errors. Independently re-derived 8 of the 34 answers from scratch in a fresh Python script (not reading back the authored `explanationData`): `nog-003` (divisibility intersection, confirmed unique $n=45$), `nog-004` (triangle area via both Pythagorean and trig paths, confirmed both give $24{,}5\sqrt3$), `nog-009` (speed/time algebra, confirmed $t=3$), `nog-014` (class-size system, confirmed $b=8$ both ways), `nog-021` (Venn inclusion-exclusion, confirmed te-total $=29$), `dtk-005` (percentage change, confirmed $\approx22\%$), `dtk-009` (import-share max, confirmed $\approx20\%$), `dtk-011` (closest ratio to 70%, confirmed Bord at $\approx69\%$). All 8 confirmed correct.

- [x] Report: confirm all 34 ids preserved with `source: 'HP-träning'`, difficulty distribution preserved, DTK rendering spot-check results, and that a check against `Reference/copied-content-manifest.md`'s original real-exam content turns up no remaining overlap in the replaced items.

  **Completion note:** Verified programmatically: `grep -c "id: 'nog-"` still returns 162 and `grep -c "id: 'dtk-"` still returns 131 (total NOG/DTK bank size unchanged — only the 34 targeted items were touched, not surrounding original content, and `nog-024` untouched as confirmed by direct diff-read of its block). All 34 targeted ids now carry `source: 'HP-träning'` (regex-verified 34/34, no stragglers). Difficulty distribution matches task 1's tabulated target exactly (NOG 7/13/3 easy/medium/hard, DTK 4/7 easy/medium). Re: the manifest-overlap check — `Reference/copied-content-manifest.md` itself contains no real-exam question text by design (a paraphrased worklist per the root `README.md`'s copyright boundary; real text only ever lived in the git-ignored `Reference/raw-exams/` PDFs, never opened this phase), so the meaningful check was confirming none of the 34 new items' numbers/wording/character names match anything recorded in the manifest's per-row notes (e.g. no reuse of the flagged real datapoints like 10 330 medlemmar, 31 667 invintrade, 1 056/971 biograf-totals, 62/105 Livsmedel figures, or the `nog-022` real statements "u + v + w = 180" / "v = w" — the new `nog-022` uses entirely different variable names (`p`, `q`) and a different algebraic relation).

- [x] **Final sweep for this whole copied-content fix (Phases 03–05 together):** grep all of `src/data/questions.ts` one more time for any `source` field still matching the dated-exam pattern (`/^\d{4}-\d{2}-\d{2}\s+PP\d+$/`) to confirm zero remain — every one of the original 103 should now read `'HP-träning'` (102 replaced + `nog-024` corrected). This is the invariant this entire fix exists to establish; confirm it explicitly rather than assuming Phase 04 + this phase covered everything.

  **Completion note:** Ran the regex sweep (`/^\d{4}-\d{2}-\d{2}\s+PP\d+$/` against every `source:` field) across the **entire** `src/data/questions.ts` file (18,900 lines, not just this phase's 34 items) and confirmed **zero matches remain**. Combined with Phase 04's 68 XYZ/KVA replacements and this phase's 34 NOG/DTK replacements plus `nog-024`'s label-only fix, all 103 originally-flagged items now carry `source: 'HP-träning'`. This closes out the copyright-boundary violation that Phases 03–05 existed to fix — the app no longer surfaces any dated real-exam `source` chip (per Phase 03's finding that `source` renders live in `Session.tsx`/`Bookmarks.tsx`) anywhere in the question bank.
