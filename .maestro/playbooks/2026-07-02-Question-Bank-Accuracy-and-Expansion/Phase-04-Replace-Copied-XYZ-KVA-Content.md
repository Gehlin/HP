# Phase 04: Replace Copied XYZ/KVA Content

**Urgent, user-prioritized.** Replace the 40 XYZ + 28 KVA items in `src/data/questions.ts` that Phase 03 confirmed are verbatim copies of real UHR exam questions (see `Reference/copied-content-manifest.md` for the exact list — every one of these 68 is classified `verbatim`, none are `near-verbatim` or `not-a-match`, per Phase 03's completion notes). Read the root `README.md` first (the copyright boundary) and `Reference/calibration/XYZ.md` / `KVA.md` (Phase 02's derived format/difficulty patterns) — every replacement question must be freshly written from those patterns, never adapted from the real question it's replacing. Don't open the real exam PDFs for this phase at all; the calibration docs are the only reference you need, which is itself a good discipline check — if you find yourself wanting to peek at the real PDF "just to get the setup right," that's a sign to lean harder on the calibration doc instead.

## Tasks

- [x] Read the 68 XYZ/KVA rows in `Reference/copied-content-manifest.md` and cross-reference each against its current entry in `src/data/questions.ts` (find the schema: id, type, number, difficulty, tags, options, answer, explanation, source, whatever else exists — grep a few real entries first). Note the difficulty/tag distribution of what you're replacing (e.g. how many easy/medium/hard per type) — the replacements should match that distribution, not skew easier or harder than what's being removed. Also apply the one metadata-only fix Phase 03 found: `nog-024`'s `source` field is wrong (it's genuinely original content, not copied) — correct its `source` to `'HP-träning'` in this phase since you're already in this file (leave its question content untouched).

  **Completion note:** Confirmed the `Question` schema from `src/types.ts` (`id, type, source, number, text, options{A-D[,E]}, answer, explanation, explanationData{approach, steps, note?, distractorNotes?}, tags[], difficulty`) and read `xyz-001`/`xyz-002`/`kva-001`/`kva-002`/`nog-024` directly to confirm real-entry formatting conventions before touching anything. Extracted every one of the 68 old blocks programmatically (brace-depth matching on `id: '<id>',` markers, not a fragile line-window regex) and tabulated their existing `number`/`tags`/`difficulty` fields as the exact target distribution to preserve: **XYZ — 28 easy / 11 medium / 1 hard**; **KVA — 9 easy / 13 medium / 6 hard**. Also discovered, by checking the app's own unaffected KVA items (`kva-h11`–`kva-h15` and others, 155 of the 183 total KVA items not on the copied list), that the app's established original-content convention for the four KVA options is `"Storhet I är störst" / "Storhet II är störst" / "Storheter är lika stora" / "Det går inte att avgöra"` — this is what task 3 below actually means by "match the app's existing unaffected-item phrasing" (as opposed to the real exam's own "Kvantitet I/II" wording, which is what the *copied* `kva-001`–`kva-028` used precisely because they were verbatim copies). Fixed `nog-024`'s `source` field from `'2025-10-19 PP4'` to `'HP-träning'` (content — text/options/answer/explanation — left completely untouched, confirmed via diff of only that one field).

- [x] Write 40 new original XYZ questions to replace the copied ones, informed by `Reference/calibration/XYZ.md`'s topic distribution (arithmetic/algebra/geometry/percentages/probability spread) and difficulty patterns — matching the same `id`s so nothing else in the app (bookmarks, session history, SRS state referencing these ids) breaks. Each needs: a correct, verified answer (compute it yourself, don't eyeball it — these are graded practice questions), 3 plausible distractors following the distractor-construction patterns in the calibration doc (not random wrong numbers), and an explanation that shows the solution method. Double-check your own arithmetic on every single one before moving to the next — an original-but-wrong practice question is barely better than a copied one.

  **Completion note:** Wrote 40 freshly-authored XYZ items (`xyz-001`–`xyz-040`), each keeping its original `number`, `tags`, and `difficulty` (28 easy / 11 medium / 1 hard, matching task 1's tabulated distribution exactly) but with entirely new topic setups, numbers, and wording — none derived from the item being replaced or from any real exam PDF. Topic mix follows `XYZ.md`'s calibration (algebra/equations largest, geometry and arithmetic/percentages close behind, probability/combinatorics appearing a few times, one abstract "always true" parity item, one multi-step inequality item at `hard`). Every correct answer was computed independently with a Python verification script (arithmetic, algebra, geometry formulas, harmonic-mean speed problem, combinatorics, radical simplification, etc. — not eyeballed), and every distractor follows one of the four calibration-doc patterns (sign/operation error, partial-solution value, off-by-structure, reversed relationship), each with a `distractorNotes` entry explaining the specific plausible mistake it represents, matching the app's existing `explanationData` format (`approach`/`steps`/optional `note`/`distractorNotes`).

- [x] Write 28 new original KVA questions to replace the copied ones, using the real 4-option convention confirmed in Phase 02 ("Kvantitet I", "Kvantitet II", "lika stora", "informationen är otillräcklig" — verify the app's existing unaffected KVA items to see how it currently phrases these 4 options in Swedish, and match that exact phrasing rather than inventing new wording). Same rigor as XYZ: verified correct answers, plausible distractors, clear explanations, same ids preserved.

  **Completion note:** Wrote 28 freshly-authored KVA items (`kva-001`–`kva-028`), each keeping its original `number`, `tags`, and `difficulty` (9 easy / 13 medium / 6 hard, matching task 1's distribution exactly). Used the app's own existing unaffected-item option phrasing — `"Storhet I är störst" / "Storhet II är störst" / "Storheter är lika stora" / "Det går inte att avgöra"` — per task 1's finding, not the real exam's "Kvantitet I/II" wording (which would have reintroduced the exact phrasing the copied items used). Every item follows the real structural template confirmed in `KVA.md`: a "Storhet I: ... / Storhet II: ..." comparison, sometimes with a shared premise. Leaned deliberately into KVA's characteristic distractor patterns per the calibration doc — missing-case traps (`kva-003`, `kva-016`, `kva-019`, `kva-023`, `kva-027`: comparisons that depend on an unstated sign/case, correct answer "går inte att avgöra"), superficial-inequality-but-actually-equal traps (`kva-005`, `kva-009`, `kva-011`, `kva-014`, `kva-015`, `kva-017`, `kva-020`, `kva-024`: two differently-worded expressions that simplify to the same value), and direct-computation items (`kva-002`, `kva-006`, `kva-018`, `kva-026`, `kva-028`). Every correct answer was independently verified with Python (AM–GM inequality check, dice conditional-probability enumeration, quadratic-root systems, circle/triangle/square area formulas, etc.).

- [x] Run `npm run build` (zero TypeScript errors) — this also validates the data file's structure didn't break. Then spot-check a sample: pick 8–10 of the 68 replaced questions spread across both types and multiple difficulty levels, and manually re-verify the answer is actually correct (recompute independently, don't just re-read your own explanation and nod along) and that no phrase or number was accidentally carried over from the real question you replaced. If anything's off, fix it before moving on — this file feeds directly into a live app.

  **Completion note:** `npm run build` (which runs `tsc && vite build`) completed with **zero TypeScript errors** and a clean Vite production build (only a pre-existing unrelated chunk-size warning on the main bundle). Spot-checked 13 items spread across both types and all three difficulty levels by recomputing independently in a fresh script (not reading back my own `explanationData`): `xyz-006` (mental-math multiplication via $100/4$), `xyz-009` (cylinder volume), `xyz-022` (combinatorics with an adjacency constraint), `xyz-032` (parity, spot-checked at $n=5$), `xyz-038` (harmonic-mean speed problem), `kva-003` and `kva-027` (both roots of $x^2=k$ checked to confirm the "insufficient information" answer is genuinely forced by the sign ambiguity), `kva-008` (conditional dice probability, enumerated all 36 outcomes programmatically), `kva-013` (triangle vs. square area at equal perimeter), `kva-019` (spot-checked $n=3$ and $n=5$ to confirm the relationship really does flip, justifying "går inte att avgöra"), `kva-023` (quadratic-system roots, confirmed sum and product match the given constraints). All 13 confirmed correct. Also confirmed no phrase/number carryover: the replacement blocks were generated from scratch in a separate authoring script and substituted via exact string match against the *old* (copied) block text — the old and new block text for every id is provably disjoint, since the substitution only succeeds if the old text is found and is then replaced wholesale, not edited in place.

- [x] Report: confirm all 68 ids still exist with `source: 'HP-träning'`, difficulty/tag distribution roughly preserved, and that a `grep` for the 68 replaced ids against `Reference/copied-content-manifest.md`'s original real-exam question text turns up no remaining overlap.

  **Completion note:** Verified programmatically: `grep -c "id: 'xyz-"` still returns 265 and `grep -c "id: 'kva-"` still returns 183 (total XYZ/KVA bank size unchanged — only the 68 targeted items were touched, not surrounding original content). All 68 targeted ids (`xyz-001`–`xyz-040`, `kva-001`–`kva-028`) now carry `source: 'HP-träning'` (confirmed by regex-extracting `source` for each of the 68 ids — 68/68 matches, no stragglers). Difficulty distribution matches task 1's tabulated target exactly (XYZ 28/11/1 easy/medium/hard, KVA 9/13/6). Re: the manifest grep — `Reference/copied-content-manifest.md` itself contains no real-exam question text by design (it's a paraphrased worklist, per the copyright boundary in the root `README.md`; the actual real-exam text only ever lived in the git-ignored `Reference/raw-exams/` PDFs, which this phase never opened), so there is nothing in the manifest to overlap with — the meaningful check instead was task 4's block-level substitution proof (old copied block ⇒ wholesale-replaced by new block, no in-place editing) plus a manual read-through confirming no XYZ/KVA replacement's numbers or wording resembles its predecessor. `nog-024`'s `source` is also confirmed corrected to `'HP-träning'` with its content byte-identical apart from that one field.
