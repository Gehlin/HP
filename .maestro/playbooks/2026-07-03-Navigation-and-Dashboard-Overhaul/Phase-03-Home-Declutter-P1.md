# Phase 03: Home Declutter (P1)

**User-confirmed:** P1 ("Ett tydligt nästa steg") from Phase 01's exploration — keep the hero score card exactly as prominent as it is today, merge the continue-session and weak-area-drill cards into one focused "Dagens pass" card, and collapse the 8-section grid to the 3 weakest sections plus a "Visa alla 8 delprov" expander. Look at `Working/declutter-1-collapsed.png` and `Working/declutter-1-expanded.png` from Phase 01 for the exact visual target. Phase 01's mockup source (`Declutter.tsx`) was deleted along with the design-preview harness in Phase 02 — it's recoverable from git history (`git log --diff-filter=D -- src/design-preview/Declutter.tsx`) if useful as a starting reference, but the real implementation goes directly into `Home.tsx`.

Read the root `README.md` for full context, and `Phase-02-Build-Top-Bar.md`'s completion notes — it already flagged that the streak currently shows twice (once in the new `AppHeader` bar, once in Home's own header pill) as something to resolve in this phase, and that the green nav band sitting directly above the green hero card needs a visual gap/separator check now that both exist.

## Tasks

- [x] Read the current `Home.tsx` in full (post-Phase-02, since its padding already changed) and the git history of `src/design-preview/Declutter.tsx` for reference. Identify every existing feature Home currently surfaces (hero score, continue-session card, weak-area-drill card, section grid, any notification/pacing hooks) so nothing gets silently dropped while restructuring — the goal is decluttering the *presentation*, not removing functionality.

  **Completion note:** `Home.tsx` (287 lines post-Phase-02) read in full. The Declutter.tsx git-history recovery came up empty — `git log --diff-filter=D -- src/design-preview/Declutter.tsx` returns nothing because the harness was created *and* deleted inside the still-uncommitted Phase 01/02 working tree (no stash either), so the mockup source is genuinely gone; `Working/declutter-1-{collapsed,expanded}.png` + the Phase 01 P1 notes served as the spec, and they were sufficient. Feature inventory (everything that must survive): (1) greeting header — time-of-day greet + first name from `hp_user_name` + capitalized sv-SE date; (2) terracotta **streak pill** (`loadStats().streak`) — the Phase-02-flagged duplicate; (3) **hero score card** — normerat via `estimateSectionedScore`, delta chip vs oldest `hpScoreHistory()` entry, gold progress bar, `Mål` from `hp_goal`, "Provet om N dagar · {date}" with `Ange provdatum →` fallback (stopPropagation → `/profil`), whole card taps to `/progress`; (4) **continue-session card** — conditional on `loadSession()` without `endTime`; resumes via `saveSession({...resumeSession, endTime: undefined })` → `/session`, shows section code/name + "N av M frågor klara"; (5) **weak-area card** — conditional on `weakTypeSummary()` + `buildWeakAreaSession(20)`; starts fresh via `buildSession(ids, null, true, 'drill', true)` → `/session`, shows top-3 weak types + question count; (6) VERBAL/KVANTITATIV header row; (7) **8-item 2-col grid** — per-type accuracy from history, conic-gradient ring, each tile `navigate('/practice', { state: { defaultType } })`. Pacing hook re-confirmed a *notification* (`maybeShowPacingNotification` — Home imports nothing from `notifications.ts`), so nothing to carry over there.

- [x] Resolve the duplicate-streak issue Phase 02 flagged: decide whether Home's own header streak pill should be removed now that `AppHeader` shows it (recommended, avoids redundancy) or serves a different enough purpose to keep both (state the reasoning either way, don't just pick one silently).

  **Completion note — removed Home's pill; the `AppHeader` gold chip is now the single streak display.** Reasoning: the two showed the *same* number from the *same* accessor (`loadStats().streak`) within ~110px of each other vertically, and neither was tappable — pure duplication, no differentiated purpose (the pill wasn't e.g. a link to a streak detail view). The bar's chip is the keeper because it's global (visible on every page including mid-session logo-only mode, where a finished pass is exactly when the streak moves) while the pill was Home-only. The P1 mockup *does* show the pill, but only because it was rendered under Direction A's bar, which had no streak chip — with Direction C chosen, keeping it would contradict the phase's whole "fewer competing elements" goal. Side effect: Home's header simplifies to greeting + date (the `stats` state and `loadStats`/`GameStats` imports dropped), and the freed top-right space keeps the greeting row calmer than the mockup, not busier.

- [x] Build the "Dagens pass" card: merge the continue-session card (resume the in-progress section) and the weak-area-drill card (start a session targeting weak delprov) into one card with a primary action (whichever is more actionable "today" — check Phase 01's mockup for which one led) and a quieter secondary row/link for the other, rather than two full-width cards stacked. Preserve both underlying behaviors (resuming a specific in-progress session vs. starting a fresh weak-area-targeted one) — this is a visual merge, not a feature cut.

  **Completion note:** Built per the mockup and the P1 spec in `design-options.md` ("the primary action — continue, or the weak-area drill when nothing is unfinished — gets the full row + play button"). One white card under a "DAGENS PASS" small-caps label: **primary row** = the resume action when an unfinished session exists (46px `#E7EDE7` section-code tile, "Fortsätt: {section}" 16px/700, "N av M frågor klara", 40px green play circle — exact same resume behavior: `saveSession({...resumeSession, endTime: undefined})` → `/session`); when nothing is unfinished the weak-area drill **promotes to primary** (terracotta ↗ tile + `#BF5A33` play circle, "Öva på svaga delprov", "MEK · NOG · KVA · 20 frågor" — same `buildSession(weakAreaIds, null, true, 'drill', true)`). **Secondary row** (only when a resume leads *and* a weak drill exists): a quiet 12.5px text row under a `#F0EAE0` inner hairline — "Fokusträning · svaga delprov" in gray + weak-type codes in terracotta 700 + a terracotta → — wired to the identical `startWeakDrill` handler. Card renders nothing when neither behavior applies (fresh user), same conditionality as the two old cards. Both behaviors Playwright-verified: secondary row → `/session` with a *fresh* drill (`{type:'drill', n:20, studyMode:true, answered:0}`); primary → `/session` with the *same* session (`id: seed-current`, 8 answers intact, `endTime` cleared); the promoted-primary and no-card states verified too (`03-home-weak-primary.png`, `03-home-fresh.png`).

- [x] Collapse the 8-section grid to the 3 lowest-scoring sections (by whatever accuracy metric Home already uses per section) followed by a "Visa alla 8 delprov" expander that reveals the remaining 5 in place (no navigation away, matching the mockup). Confirm each of the 3 shown sections still links to its practice session correctly, and that the expanded state also works (all 8 tappable, same as today).

  **Completion note:** The 2-col grid became a single column of slim rows under a "DELPROV / svagast först" header (per the mockup: bold code in a fixed 44px column, gray name, VERBAL/KVANT tag in the category color, same 32px conic-gradient accuracy ring as before), ranked by the exact metric Home already computed (per-type correct/total over all history; untried sections score 0 and therefore surface first — reasonable, "never practiced" *is* weakest). Collapsed = first 3; the dashed expander toggles `Visa alla 8 delprov ▾` / `Visa färre ▴` via local state — no navigation. **One deliberate deviation from the mockup:** its expanded frame re-sorts all 8 into canonical ORD/XYZ/LÄS/… order, which would make the 3 visible rows jump around on tap and silently falsify the persistent "svagast först" label; the implementation instead keeps the full weakest-first ordering, so the top 3 stay put and the remaining 5 append below — this is also the literal reading of this task's "reveals the remaining 5 in place". Playwright: collapsed shows exactly MEK/NOG/KVA (ascending) with the seeded profile, expand → 8 rows with the first 3 unchanged, URL unchanged, **all 8 rows individually clicked → each lands on `/practice`** (state-carried `defaultType` unchanged from the old grid), collapse returns to 3.

- [x] Add a visual gap/separator between the new green `AppHeader` band and the green hero score card directly below it (Phase 02 flagged these are both green and currently sit flush) — a thin gap, a subtle shadow/border, or a background-color sliver of paper between them, whatever reads cleanly; check a live screenshot before deciding it's resolved, don't assume a small CSS tweak worked without looking.

  **Completion note — paper sliver, verified with before/after screenshots.** Looked before touching anything: at rest the two greens are actually *not* flush (the greeting sits between them on ~120px of paper), so the real defect only appears on scroll — the fixed band's existing shadow is green-on-green and invisible, so the instant the hero card slides underneath, band and card fuse into one undifferentiated green mass (captured live before the fix, hero delta-chip half-swallowed by the tabs row). Fix: `borderBottom: '2px solid #F1ECE3'` on the `AppHeader` element — a sliver of the paper background color that is literally invisible at rest (paper border against paper page) but renders a crisp full-width seam the moment any content, green or otherwise, passes beneath the band. Re-screenshotted at scroll offsets 60 and 120 (`03-band-vs-hero-scrolled{,-60}.png`) plus a zoomed crop of the boundary: the band now terminates cleanly above the hero at every position. Box-sizing is border-box (Tailwind preflight), so the bar stays exactly 97/54px and Phase 02's `.pt-topnav*` clearances needed no change; the 2px comes out of the flexible tabs row.

- [x] Run `npm run build` and `npm test`. Use Playwright to screenshot the final Home page (both collapsed and expanded grid states) at the real mobile viewport, and click through the "Dagens pass" card's primary and secondary actions to confirm they still navigate/behave correctly. Compare against `Working/declutter-1-collapsed.png`/`-expanded.png` for visual fidelity to what was approved.

  **Completion note:** `npm run build` (tsc + vite + PWA precache, 80 entries) and `npm test` (14/14, srs) both pass. Playwright at 402×874 @2x against a dev server, same seeded profile as Phases 01/02 (Elin, mål 1,60, exam 2026-10-25, streak 12, 3 history sessions tuned so MEK 40% < NOG 47% < KVA 53% are weakest, unfinished 8/20 ORD pass): **34/34 checks green**, covering every inventoried feature (greeting, hero + delta + goal + days-until, streak-once, Dagens pass primary/secondary with session-payload assertions, 3-row collapse with correct weakest ordering, expand/collapse in place, all 8 rows → `/practice`, weak-drill-as-primary state, fresh-user state). Fidelity vs `declutter-1-*.png`: side-by-side match on structure, spacing, card anatomy, tag/ring treatment, expander styling — differences are the intended ones only (Direction C band instead of the mockup's Direction A bar, no header streak pill, expanded list stays weakest-first, live seeded numbers instead of the mockup's static ones). Screenshots: `03-home-collapsed.png`, `03-home-expanded.png`, `03-home-weak-primary.png`, `03-home-fresh.png`, `03-session-weak-drill.png`, `03-session-resumed.png`, `03-band-vs-hero-scrolled{,-60}.png`.

- [x] Report: confirm every original Home feature is still present and reachable, confirm the streak-duplication decision and why, confirm the green-band/hero-card separation looks right, and confirm build/tests pass.

  **Completion note:** Reported. All seven inventoried features present and reachable (streak intentionally consolidated into the AppHeader chip — the one deliberate change, decided explicitly rather than silently dropped); both session-starting behaviors verified down to the localStorage session payloads; band/hero separation resolved with the invisible-at-rest paper-sliver border and confirmed against live scrolled screenshots; build + tests green (14/14).
