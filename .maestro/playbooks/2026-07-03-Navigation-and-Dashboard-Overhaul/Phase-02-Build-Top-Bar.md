# Phase 02: Build & Wire the Top Bar (Direction C)

**User-confirmed:** Direction C ("Grönt band") from Phase 01's exploration. Read `Working/design-options.md` and look at `Working/nav-c-hem.png`/`nav-c-ova.png`/`nav-00-overview.png` (the "C" rows) for the exact visual target — a solid `#224A3A` masthead, inverted cream logo tile (green "H"), cream serif wordmark "HP Träning", a gold streak chip on the right, and an editorial tabs row below (active = gold `#E4C66A` text + underline, inactive cream @55% opacity), collapsing to a logo-only identity row (no tabs) on `/session`/`/resultat`/`/granska`/`/exam/*`.

Also confirmed in Phase 01: the existing `ScrollToTop` in `App.tsx` works for forward navigation but fails on browser-back and the app's own back-chevron (`PageHeader.tsx`'s `navigate(-1)`) because native `history.scrollRestoration` re-applies its saved offset after the effect runs. Fix identified: set `history.scrollRestoration = 'manual'`. Include this fix in this phase since you're touching `App.tsx` anyway.

This phase replaces the app's navigation chrome everywhere. Read `src/design-preview/NavBars.tsx` (Phase 01's harness — the Direction C implementation already exists there, working, and can be adapted/ported rather than rebuilt from scratch) before writing new code.

## Tasks

- [x] Port Direction C from `src/design-preview/NavBars.tsx` into a new real component (e.g. replace `src/components/TopNav.tsx`'s contents, or rename to something less confusing like `AppHeader.tsx`/`NavBar.tsx` if that's cleaner — check what's referenced where first with a grep before renaming). It needs: the logo tile linking to `/`, the 4 tabs (Hem/Öva/Statistik/Profil) with active-state styling, a live streak count (pull from wherever the app already tracks streak — check `Home.tsx`/`Profil.tsx` for the existing accessor rather than reinventing), and the logo-only collapsed mode for `/session`/`/resultat`/`/granska`/`/exam/*` (same path list the old bottom bar hid on).

  **Completion note:** Grep first: `TopNav` was referenced only from `App.tsx` (import + one render), so a rename was cheap — `git mv src/components/TopNav.tsx src/components/AppHeader.tsx`, rewritten as the Direction C masthead (a file named "TopNav" that was a bottom bar deserved to die with the bottom bar). Ported 1:1 from `NavBarC`/`LogoTile` in the harness: green `#224A3A` band + soft green drop shadow, 54px row 1 (inverted cream 31px logo tile with serif green "H" wrapped in a `<Link to="/">` with `aria-label`, 19px cream serif "HP Träning" wordmark, gold streak chip on `rgba(255,255,255,0.12)`), 43px editorial tabs row under a `rgba(251,247,238,0.12)` hairline (active = gold `#E4C66A` + 26×2.5px underline + `aria-current="page"`, inactive cream @55%). Real-app wiring the harness faked: tabs are `navigate()` buttons using the old bar's exact active logic (`/` exact, others `startsWith` — so e.g. `/exam-select` correctly lights nothing), `logoOnly` derived from the same `['/session','/resultat','/granska','/exam/']` prefix list, streak read live via the existing `loadStats().streak` from `src/utils/gamification.ts` (same accessor Home uses; re-read every render, which happens on each route change — exactly when a finished session can have moved it). One harness bug fixed in the port: the harness set `height: 97px` *and* `paddingTop: env(safe-area-inset-top)` — border-box, so on a notched phone the safe-area would have eaten the bar's content; real component uses `height: calc(97px|54px + env(safe-area-inset-top))`. Exports `APP_HEADER_HEIGHT` (97) / `APP_HEADER_LOGO_ONLY_HEIGHT` (54) documented as the numbers `.pt-topnav*` must stay in sync with.

- [x] Update `App.tsx`: replace the old bottom `<TopNav />` render with the new top bar component rendered at the top of the tree (before `<Routes>`, not after), and set `history.scrollRestoration = 'manual'` (in the same `ScrollToTop` component or a one-time effect near it — either is fine, just make sure it actually runs before any navigation happens, e.g. at module load or in a top-level effect with an empty dependency array).

  **Completion note:** `<AppHeader />` now renders right after `<ScrollToTop />`, before `<Suspense>`/`<Routes>`; the old post-Routes `<TopNav />` render and import are gone. `window.history.scrollRestoration = 'manual'` set at **module scope** of `App.tsx` (guarded by `'scrollRestoration' in window.history`) — runs at import time, before React even mounts, so no navigation can beat it. Also took Phase 01's optional suggestion: `ScrollToTop`'s reset moved from `useEffect` to `useLayoutEffect`, killing the one-frame flash of stale scroll on route change. Live-verified `history.scrollRestoration === 'manual'` in the running app via Playwright.

- [x] Update `src/index.css`'s `.pt-topnav`/`.pb-bottomnav` utilities (or equivalent — confirm current names first) to reflect the flip: `.pt-topnav` needs to become real bar-clearance (bar height + safe-area-inset-top, matching Direction C's ~97px full mode — check the exact height from the harness) instead of today's generic 20px breathing room, and `.pb-bottomnav` needs to shrink to just `env(safe-area-inset-bottom)` (or be removed if nothing needs bottom clearance anymore — check whether anything else relies on it, like a floating action button). Sweep every page currently using these classes (grep across `src/pages/`) and confirm each still renders correctly — pay special attention to `Home.tsx`, which hardcodes its own padding inline rather than using the shared classes (per Phase 01's finding) and needs the same fix applied directly.

  **Completion note:** Names confirmed (`.pt-topnav` / `.pb-bottomnav`, plus unrelated `.pb-safe` left alone). New values: `.pt-topnav = calc(119px + safe-area-top)` (97px bar + 22px gap, matching the harness's visual rhythm) and `.pb-bottomnav = max(28px, safe-area-bottom)` — kept rather than removed because 24 page containers use it and content shouldn't end flush at the viewport edge. Added a **third** utility the phase doc didn't anticipate: `.pt-topnav-collapsed = calc(76px + safe-area-top)` (54px band + 22px gap) — needed because the logo-only paths used to have *no* bar at all and now have a 54px one, so their pages need clearance too, and full-bar clearance (119px) there would leave a 65px dead gap. Sweep results: the 22 pages using `pt-topnav pb-bottomnav` (grep across `src/pages/`) needed **zero edits** — the class semantics flip covers them. Hand-edits: `Home.tsx` (inline `max(58px,…) 18px calc(100px+…)` → `pt-topnav pb-bottomnav` classes + inline 18px horizontal only), `ExamStart.tsx` (× 2 containers → `pt-topnav-collapsed`, it's on `/exam/*`), `Granska.tsx` (`pt-14` → `pt-topnav-collapsed pb-bottomnav`), `Resultat.tsx` (`pt-16` → `pt-topnav-collapsed`; green band on green page blends into a seamless branded header, see `Working/02-resultat-logo-only.png`), `Session.tsx` (its own fixed header `top-0` → `top-[calc(54px+env(safe-area-inset-top,0px))]` so it stacks under the band, and its scroll area `pt-[72px]` → `pt-[calc(126px+…)]`). The "anything else relying on bottom clearance" check found exactly one: `InstallBanner.tsx` floated at `bottom-20` to clear the old 84px bottom bar → now `bottom-[max(16px,env(safe-area-inset-bottom))]`. (`Session`'s `bottom-24` keyboard hint clears Session's *own* footer, not the nav — untouched.)

- [x] Confirm the logo-only mode's height doesn't cause a layout jump when navigating into/out of `/session` etc. — Phase 01 found Direction A had zero jump but B/C both change height between full and logo-only mode; decide whether that's acceptable (a de facto height change happens today too, since the old bottom bar just disappears entirely on those paths, which is arguably a *bigger* jump) or worth smoothing with a transition. Don't over-engineer this — a quick opinion and a one-line CSS transition if easy, not a big animation system.

  **Completion note — acceptable, smoothed with one line.** Opinion: the 97→54px change is a non-issue in practice because (a) the bar is `position: fixed`, so it never reflows page content — the "jump" is only the band visually shortening, (b) every full↔logo-only crossing coincides with a full page swap (into `/session`, out to `/resultat` → Hem) where the entire viewport changes anyway, and (c) the old behavior was an 84px bar blinking out of existence entirely. Still cheap to smooth: `transition: 'height 180ms ease'` + `overflow: hidden` on the header (the tabs row unmounts instantly, the band settles over 180ms). Verified `height 0.18s` in the live computed style. Nothing more built, per the don't-over-engineer instruction.

- [x] Delete the Phase 01 design-preview harness (`design-preview.html`, `src/design-preview/`) now that Direction C has been ported into the real component — it was explicitly scaffolding, not meant to ship.

  **Completion note:** `design-preview.html` and `src/design-preview/` (`main.tsx`, `NavBars.tsx`, `Declutter.tsx`) deleted after the port. `grep -rn "design-preview\|TopNav" src/ index.html` confirms zero remaining references (the only hit is a provenance mention in `AppHeader.tsx`'s doc comment). Note for the Home-declutter phase: `Declutter.tsx` (the P1 mockup source) went down with the harness — it's recoverable from git history at this phase's parent commits if wanted, though `Working/declutter-1-*.png` + the Phase 01 notes are probably a sufficient spec.

- [x] Run `npm run build` and `npm test`. Then use Playwright to click through at least 8-10 different pages (including the logo-only paths) at the real mobile viewport, confirming: the top bar renders correctly and consistently, the logo click navigates to `/`, active-tab styling matches the current route, no page has broken/overlapping padding, and the scroll-restoration fix actually works now (repeat Phase 01's back-navigation test — scroll down, navigate forward, hit back, confirm it's now at the top). Screenshot the results into this phase's `Working/` folder (reuse the folder from Phase 01).

  **Completion note:** `npm run build` (tsc + vite, PWA precache regenerated) and `npm test` (14/14, srs) both pass. Playwright against a dedicated dev server, 402×874 @2x, same seeded profile as Phase 01 (Elin, mål 1,60, streak 12, 3 history sessions + unfinished 8/20 ORD pass built from the real question bank). **Full-bar sweep — 14 pages** (`/`, `/practice`, `/progress`, `/profil`, `/theory`, `/settings`, `/exam-select`, `/score`, `/matematik`, `/bookmarks`, `/srs`, `/verbalt`, `/kvantitativt`, `/ord-guide`): bar at top:0, height exactly 97, logo + wordmark + 4 tabs present on all; active tab correct on the 4 tab routes and *no* tab active on the 10 sub-pages; overlap probe (first visible text leaf outside the header vs. bar bottom) ≥ 119px on every page — zero overlaps. **Logo-only sweep — all 4 paths** (`/session`, `/resultat`, `/granska`, `/exam/2026-04-18-pp3`): height exactly 54, zero tabs, logo present; `/session`'s own counter/timer header verified sitting at exactly y=54 below the band. **Interactions:** tab click Hem→Öva lands on `/practice`, active state follows to Statistik, logo click from `/settings` lands on `/`. **Scroll-restoration re-test (Phase 01's exact failing repros, 402×650):** `history.scrollRestoration === 'manual'` ✓; browser back from `/progress` (scrolled 5570) → Hem at **y=0** ✓; PageHeader chevron (`navigate(-1)`) from `/theory` back to `/profil` (was scrolled 504) → **y=0** ✓; browser back from `/score` → `/progress` at **y=0** ✓ (this leg landed at 1615 in Phase 01); forward navs still land at 0. 33/33 functional checks green (script's own naive "overlap" heuristic initially measured the padded root container at y=0 — false alarm, replaced with the text-leaf probe above). Screenshots: `Working/02-{home,practice,progress,profil,theory,settings}.png` + `02-{session,resultat,exam}-logo-only.png`.

- [x] Report: confirm all pages checked render correctly, confirm the scroll-restoration fix verified working on back navigation, and flag anything that looked off during the sweep.

  **Completion note:** All 18 checked routes render correctly with the new masthead (14 full-bar + 4 logo-only); scroll restoration verified fixed on all three of Phase 01's failing POP repros (browser back × 2 + PageHeader chevron), all landing at y=0. Two things flagged for later phases, neither a defect: (1) the streak now appears **twice on Hem** — gold chip in the bar *and* Home's terracotta header pill — expected until the P1 declutter phase reworks Home's header (same duplication Phase 01 noted for Direction B); (2) on Hem the green band sits ~120px above the green hero card — the "brand column" caveat from `design-options.md`, also relieved by the declutter phase. Offline banner (`z-[300]`) and onboarding overlay (`z-[200]`) still layer above the bar (`z-50`) as before.
