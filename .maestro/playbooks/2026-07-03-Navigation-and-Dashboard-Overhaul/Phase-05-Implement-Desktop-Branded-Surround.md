# Phase 05: Implement Desktop Layout — Direction 2 (Branded Surround)

**User-confirmed:** Direction 2 from Phase 04's exploration. Look at `Working/04-dir2-home-1440.png` and `Working/04-dir2-home-768.png` for the exact visual target: content stays in its existing mobile-first column (~700px wide based on the screenshots, centered), everything outside that column gets a dark-green (`#224A3A`-family) background with a large, faint, repeated watermark of the logo "H" mark. Every page in the app needs this, not just Home — it's a global wrapper concern, not a per-page redesign.

Read the root `README.md`, `Phase-04-Desktop-Layout-Exploration.md`'s completion notes (the harness code — `src/design-preview/` — still exists with Direction 2's implementation; port from there, don't rebuild from scratch), and Phase 02's completion notes (for how the mobile-first component code and shared layout classes are currently structured, since this phase wraps around all of it without changing it).

## Tasks

- [x] Read the Direction 2 implementation in `src/design-preview/` (whatever component/CSS produced `04-dir2-home-*.png`) and port it into a real, reusable layout wrapper — e.g. a `DesktopFrame`/`AppShell` component that wraps the app's existing content column, applied once at the top level (`App.tsx`) rather than duplicated per-page. Confirm the mobile viewport (~390-402px) is completely unaffected by this wrapper — at mobile width the surround should be invisible/zero-width, only appearing once the viewport is wider than the content column's max-width.

  **Completion note:** Direction 2 lived in `Direction2()` in `DesktopLayouts.tsx` + the `.d2-*` rules in `preview.css`. Ported 1:1 into `src/components/DesktopFrame.tsx` (frame div + two `aria-hidden` watermark "H" spans + a `.desktop-frame-sheet` that the routed pages render inside) with the CSS moved to `src/index.css` as `.desktop-frame*`: the 165° `#1A3A2D→#224A3A→#26523F` gradient + gold radial glow, the 26px `rgba(251,247,238,0.05)` dot motif (frame `::before`), the `min(46vh,480px)` serif watermarks at `calc(50% ± 288px)` shown from 900px up, and the sheet's gold-hairline + `0 0 80px` depth shadow from 600px up. One new token: `--content-max-width: 528px` in `:root` as the single source of truth for the column (the harness hardcoded 528 in four places) — consumed by the sheet, `AppHeader`, `Session`'s fixed bars and `FormulaDrawer`. Mobile inertness is structural, not conditional: the sheet is `max-width` + `margin: 0 auto` + `min-height: 100vh` with the paper background, so at ≤528px it spans the whole viewport and covers the surround completely — no media query needed for the "off" state (shadows/watermarks additionally gate at 600px/900px). No transform anywhere in the wrapper, so pages' `position: fixed` children (modals, drawers, Session's bars) keep the viewport as containing block. Proven pixel-inert at 400px, see task 3.

- [x] Wire the wrapper into `App.tsx` around the routed content (and `AppHeader`, if the nav bar should also sit inside the constrained column at desktop width — check the Direction 2 screenshots to confirm whether the green nav band itself stays full-width or also constrains; from the screenshots it looks like the nav band's dark green blends with the surround so it may be fine full-width, verify against the actual screenshot rather than guessing).

  **Completion note:** `<DesktopFrame>` wraps `<AppHeader />` + the `<Suspense><Routes>` block in `AppInner`; the viewport-level overlays (offline banner, onboarding, key-guide modal, `InstallBanner`) stay outside — they're full-viewport by design. Header question settled against the actual screenshots, not the blend guess: in both `04-dir2-home-768.png` and `-1440.png` the band **constrains to the column** (surround + watermark visibly continue left/right of it, separated by the sheet's gold hairlines — the harness gave the header `maxWidth: 528`). So `AppHeader` now caps at `--content-max-width`, centered via `left: 50%` + `translateX(-50%)` — the harness's exact technique; at ≤528px that computes to the old full-bleed geometry. Same cap applied to the three other pieces of fixed full-width chrome the harness never exercised (it only rendered Home): `Session`'s counter/timer sub-header, `Session`'s bookmark/action footer, and `FormulaDrawer` — without it they'd smear paper bars across the green surround at desktop widths. (`FormulaDrawer`'s `animate-slide-up` keyframes animate `transform` while Tailwind 4's `-translate-x-1/2` uses the standalone `translate` property, so they compose instead of clobbering.) `AchievementToast`, the skip-undo toast and `InstallBanner` already self-center ≤448px wide, inside the column — untouched.

- [x] Run `npm run build` and `npm test`. Use Playwright to screenshot at least 8-10 different pages (not just Home) at 400px, 768px, and 1440px, confirming: mobile is pixel-identical to before this phase, the desktop surround appears correctly and consistently across all pages checked, and nothing overlaps/clips/looks broken at any width (check `/session`, `/resultat`, `/granska` too, since their logo-only header mode + the frame need to coexist correctly).

  **Completion note:** `npm run build` (tsc + vite + PWA precache) and `npm test` (14/14, srs) pass. Verification went beyond eyeballing: a deterministic capture harness (seeded localStorage profile dumped to JSON once + `page.clock.install` at a fixed timestamp + `animations: 'disabled'`, determinism proven by two identical pre-change runs) screenshotted **13 pages × 3 widths** — `/`, `/practice`, `/progress`, `/profil`, `/theory`, `/settings`, `/exam-select`, `/score`, `/verbalt`, `/session`, `/resultat`, `/granska`, `/exam/2026-04-18-pp3`. **Mobile: all 13 pages at 400px are byte-identical (`cmp`) to pre-change baselines** — stronger than pixel-identical. Desktop: surround + column + watermarks consistent on all pages at 1440px; at 768px watermarks correctly absent (<900px) with surround + hairlines present; `Theory`'s sticky tab bar constrains inside the sheet; `Resultat`'s green hero blends into the surround with the gold hairlines keeping the column edge legible. Logo-only trio verified: collapsed 54px band, Session's sub-header and footer all cap at the same 528px and stay aligned with the sheet at every width. **One pre-existing quirk found (not a defect of this phase):** Playwright's bundled headless Chromium paints a ghost of Session's footer-button text ("Nästa fråga") inside the top band at 768px — reproduced on the *unmodified* pre-phase code (via `git stash`), absent from the DOM (probed: exactly one such element, correctly positioned), and absent when rendered with real Chrome (`channel: 'chrome'`, see `Working/05-session-768-realchrome.png`). Left alone. Screenshots: `Working/05-<page>-1440.png` (13), `05-{home,session}-{400,768}.png` (4), + the real-Chrome evidence shot.

- [x] Delete the `src/design-preview/` harness and `design-preview.html` now that Direction 2 is ported (same cleanup Phase 02 did last time).

  **Completion note:** `design-preview.html` and `src/design-preview/` (`main.tsx`, `DesktopLayouts.tsx`, `HomeReflow.tsx`, `preview.css`, `seed.ts`) deleted right after the port, *before* the build/test/screenshot pass, so verification ran against the exact state that ships. `grep -rn "design-preview" src/ index.html vite.config.*` finds only provenance mentions in doc comments (`DesktopFrame.tsx`, `AppHeader.tsx`, `index.css`) — same as Phase 02's cleanup. `seed.ts`'s deterministic profile survives as a localStorage dump used by the verification harness (and is recoverable from git history if a later phase wants it).

- [x] Report: confirm mobile is unaffected, confirm desktop surround works consistently across all pages checked (list them), and confirm build/tests pass.

  **Completion note:** Reported. Mobile: byte-identical screenshots at 400px on all 13 checked pages (`/`, `/practice`, `/progress`, `/profil`, `/theory`, `/settings`, `/exam-select`, `/score`, `/verbalt`, `/session`, `/resultat`, `/granska`, `/exam/2026-04-18-pp3`). Desktop: surround correct and consistent on the same 13 pages at 768px and 1440px, including the logo-only trio's collapsed band + Session's fixed bars. Build and tests pass. Only flag: the pre-existing headless-Chromium ghost-paint documented in task 3, invisible in real browsers.
