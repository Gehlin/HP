# Phase 04: Restructure Nav & Profil

Implements the rest of Phase 02's decisions (see the playbook `README.md`'s "Final decisions" section). Depends on Phase 03 being complete (dead routes gone, quant guides re-parented under Theory, the migrated Settings features in Profil).

## Tasks

- [x] Add a 5th tab to `AppHeader.tsx`: **Teori**, linking to `Theory.tsx` (now the complete guide index post-Phase-03). Match the existing tab styling/active-state exactly — this is a mechanical addition to an already-designed component, not a redesign of the bar itself. Confirm the bar still looks good with 5 tabs at mobile width (~390-402px) — if 5 tabs genuinely don't fit comfortably, that's worth a quick live check before assuming it's fine, not after.

  **Completion note:** One entry added to the `TABS` array — `{ path: '/theory', label: 'Teori' }` — between Öva and Statistik (learning next to practicing, the two analytics/identity tabs stay right-adjacent), plus the doc comment's "four editorial text tabs" → "five". Nothing else needed touching: the tabs are `flex: 1` so they self-divide, the active state (gold `#E4C66A` + 26px underline pill, `aria-current="page"`) comes from the shared `startsWith` matcher, and the bar heights/`.pt-topnav` clearances are unchanged. **Fit verified live, not assumed:** Playwright at both 390px and 402px confirmed no header horizontal overflow, no per-tab text clipping (`scrollWidth <= clientWidth` for all 5, incl. the widest, "Statistik"), and the five buttons exactly fill the nav's content width; each tab gets ~75px at 390px, comfortable for 13px labels. All 5 tabs clicked: correct route, exactly one active tab at a time, computed active color `rgb(228,198,106)` (gold), underline present. Screenshot `Working/07-header-5-tabs-390-teori-active.png` visually inspected — the band reads as calm as the 4-tab version.

- [x] Move `Ordbyggaren` (`OrdBuilder.tsx`) to be reachable from `Theory.tsx` (a learning tool, per the user's decision) rather than from Profil.

  **Completion note:** Added an **`OrdBuilderBanner`** to Theory following the exact existing banner convention (`card rounded-2xl p-4` button, 2px green dot per `TYPE_COLOR.ORD`, title + one-liner "Lär dig ORD-delens synonymer med flashcards" written from the page's own intro copy, `ChevronRight`). Placed in **two** spots, mirroring how the other cross-cutting tool (Liggande stolen) already appears on both Översikt and its home tab: (1) the **Översikt tab** after MathGuide/Liggande stolen — this keeps Ordbyggaren at 2 taps from Hem (Teori → banner), same depth it had via Profil, so the move doesn't bury it; (2) the **ORD tab** after the ORD-guide banner, where a student studying ordförståelse actually is. `OrdBuilder.tsx`'s back button retargeted `/practice` → `/theory` to match its new parent. The Profil row is gone (next task). Playwright clicked both banners → `/ord-builder` renders, and back returns to `/theory`.

- [x] Restructure `Profil.tsx` down to: identity card, goal/exam-date card, an INSTÄLLNINGAR group (notification toggle, focus priority, the migrated replay-onboarding), and a KONTO group (export/import, the migrated granular resets, reset-all). Remove the STUDIEVERKTYG section entirely — everything in it has been promoted elsewhere by this point (Teori/Ordbyggaren → the new Teori tab; SRS/exam-simulator/bookmarks → Öva, per the next task).

  **Completion note:** Profil is now exactly the four blocks specified. Removed: the standalone "Sparade frågor" card (the removal Phase 03 explicitly deferred here), the whole STUDIEVERKTYG heading + 5-row card (Teori & guider → the Teori tab; Repetitionskö/Provsimulatorn → Öva; Ordbyggaren → Teori; **HP-poängprediktor keeps its pre-existing second entrance** — Statistik's HP-prognos card, `Progress.tsx:691`, which Phase 01 already documented — so nothing became unreachable). Renamed the ÖVNING group **INSTÄLLNINGAR** and moved "Visa introduktion igen" into it from KONTO per this task's spec (it's a preference-ish action, not a data operation), leaving KONTO as pure data ops: Exportera / Importera / Rensa specifik data / Återställ all data (danger-styled, last). Dead code went with the rows: five now-unused icons (Book/Clock/Timer/Type/Chart), the `SettingsRow` `badge` prop (only the removed Repetitionskö row used it), and the now-unused `useNavigate` import — `dueCount`/`bookmarkCount` stay, the granular-reset modal badges still need them. Playwright: no STUDIEVERKTYG/row remnants in the rendered page, both group labels present with the right rows in the right groups (DOM-checked, not just text-searched), granular modal still opens. Screenshot `Working/09-profil-minimal.png` — the page now fits identity + goal + 2 groups in one screen.

- [x] Promote SRS (`SrsQueue`), the exam simulator (`ExamSelect`), and bookmarks into `Practice.tsx` (Öva) as visible entry points — not buried in a sub-menu, but alongside the existing mode selector/section grid so they're one tap from Öva. Match Practice's existing visual conventions (card style, spacing) rather than inventing a new pattern.

  **Completion note:** Extended Phase 03's Repetitionskö link card into a **three-card link cluster** directly under the mode selector, all sharing the established card shell (`rounded-2xl p-4 mb-3`, colored dot, 18px stroke icon, title + status line, trailing → to signal navigation rather than mode-arming): **Repetitionskö** (→ `/srs`, gold dot, live due count — unchanged), **Provsimulatorn** (→ `/exam-select`, terracotta dot, stopwatch icon, "Gamla högskoleprov och provpass med tid"), **Sparade frågor** (→ `/bookmarks`, green dot, bookmark icon, live count or "Inga sparade frågor än" — always visible, unlike the old card). The three are one `.map()` over a link array, not three copies of the markup. **Consolidation:** the old conditional "Bokmärkta frågor" quick-drill card (only rendered when bookmarks existed, with its own Bläddra/Drill buttons) is removed along with its now-dead `startBookmarkDrill` — `/bookmarks` is bookmarks' single home from Öva, and its own "Öva alla sparade frågor" CTA covers the drill launch (Playwright drove it into a live `/session`). `Bookmarks.tsx`'s two back buttons retargeted `/profil` → `/practice` to match the new parent. Playwright: all three cards visible with empty-state lines on a fresh profile, each click lands on the right page, bookmark count goes live ("2 bokmärkta frågor") when bookmarks exist, and the simulator still launches (Öva → Provsimulatorn → Starta Full HP-dag → `/exam/full-hp`). Screenshot `Working/10-practice-promoted-tools.png`.

- [x] Run `npm run build` and `npm test`. Use Playwright to click through the full nav: confirm all 5 tabs work and show correct active states, confirm Teori/Ordbyggaren reachable from the new tab, confirm Profil now only shows identity/goal/settings (no stray STUDIEVERKTYG remnants), confirm SRS/exam-simulator/bookmarks are all one tap from Öva and still function correctly, and re-run a full click-through of every remaining route in the app (reuse Phase 01's route list minus what Phase 03 deleted) to confirm nothing 404s.

  **Completion note:** `npm run build` (tsc + vite) clean, `npm test` 14/14, both on the final source. A throwaway Playwright script (repo's own `playwright` devDependency, 390×844 with a 402px spot-check, same approach as Phase 03) ran **105 checks, 105 passed, zero console pageerrors**: (1) nav — 5 buttons in order Hem/Öva/Teori/Statistik/Profil, each clicked with correct route + single gold active tab + underline, no overflow/clipping at either width; (2) Teori → Ordbyggaren from both the Översikt and ORD-tab banners, back returns to `/theory`; (3) Profil — DOM-verified minimal (no STUDIEVERKTYG, no Teori & guider/Repetitionskö/Provsimulatorn/Ordbyggaren/HP-poängprediktor/Sparade frågor anywhere on the page; INSTÄLLNINGAR holds notif/fokus/replay, KONTO holds export/import/granular/reset; granular modal opens); (4) Öva — all three promoted cards visible on a fresh profile and functional: → `/srs` (dashboard renders), → `/exam-select` → Starta Full HP-dag → `/exam/full-hp` start page, → `/bookmarks` (empty state, back → `/practice`; with 2 bookmarks seeded the Öva card shows the live count and the page's "Öva alla sparade frågor" launched a real `/session`); (5) full route sweep — all **24 remaining routes** (Phase 03's list, unchanged by this phase) direct-loaded and render real content, nothing 404s/blanks. Screenshots `Working/07`–`10` visually inspected. Post-edit greps: no dangling `startBookmarkDrill`, no STUDIEVERKTYG string outside a provenance comment.

- [x] Do a final holistic check against the playbook's operating principles: pick 3-4 realistic user goals ("I want to practice my weakest section," "I want to learn how NOG works," "I want to see my saved questions," "I want to simulate a full exam") and count taps-from-Hem for each, comparing against Phase 01's original counts. Report the before/after tap-counts as evidence the redesign actually reduced friction, not just moved it around.

  **Completion note:** After-paths measured by real Playwright clicks from `/` (not estimated); before-counts from `Working/ia-map.md`. **(A) "Learn how NOG works":** before — NOG theory 3 taps (Hem → Profil → Teori & guider → NOG tab), the full `NogGuide` **unreachable** (∞, orphaned behind QuantHub); after — NOG theory **2 taps** (Teori → NOG), full guide **3 taps** (+banner). **(B) "See my saved questions":** before 2 taps via the Profil settings drawer; after 2 taps via Öva — count equal, but the entry now lives on the screen a studying user is already on (1 tap from Öva itself, with a live count visible at 0 taps from there) instead of behind the identity/settings tab. **(C) "Simulate a full exam":** before 3 taps to a started full-HP pass (Hem → Profil → Provsimulatorn → exam); after 3 taps (Öva → Provsimulatorn → Starta Full HP-dag) — equal count, relocated from settings-depth to the practice hub. **(D) "Practice my weakest section":** 1 tap (Home's weak-area card, when history exists) or 2 (Öva → Fokusträning/section card) — both before and after; already optimal, deliberately untouched. Structural counts behind these: `/theory` 2 → **1**, quant guides ∞ → **3**, NOG/quant theory 3 → **2**, Ordbyggaren 2 → 2 (via Teori Översikt), `/srs`//bookmarks//exam-select 2 → 2 but re-parented. Honest net: this phase's friction win is partly fewer taps (everything Theory-related, plus un-orphaning) and partly *where* the taps are — Phase 01's core finding was that every non-tab feature funneled through Profil, and that funnel is now gone: Profil contains zero study features, and no study goal requires entering a settings surface.

- [x] Report: confirm the 5-tab bar works and looks right at mobile width, confirm Profil is now minimal, confirm the promoted tools work from Öva, the before/after tap-count comparison, and confirmation build/tests pass.

  **Completion note:** Reported to the user: 5-tab bar verified working and comfortable at 390/402px (measured, not assumed — no overflow or clipping, correct gold active states on all five); Profil reduced to identity + goal + INSTÄLLNINGAR + KONTO with zero STUDIEVERKTYG remnants; SRS/Provsimulatorn/Sparade frågor all functional one-tap cards on Öva (drills and exams actually launched during verification); before/after tap-counts as above; build + 14/14 tests pass. This completes the playbook — all Phase 02 final decisions are implemented.
