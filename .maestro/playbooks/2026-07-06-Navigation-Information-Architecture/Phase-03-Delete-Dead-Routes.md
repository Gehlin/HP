# Phase 03: Delete Dead Routes & Salvage Unique Bits

Implements Phase 02's decisions (see the playbook `README.md`'s "Final decisions" section and `Working/ia-redesign-proposal.md` for full reasoning). **Critical constraint the user set explicitly: no knowledge/educational content may be lost.** `VerbalHub`/`QuantHub`/`Settings.tsx` were confirmed to contain no unique study material — they're dashboards/utilities over content that lives elsewhere — but verify this yourself against the actual current source before deleting anything, don't just trust the prior phase's characterization on a decision this consequential.

## Tasks

- [x] Read `VerbalHub.tsx`, `QuantHub.tsx`, and `Settings.tsx` in full. Confirm independently that none contain unique explanatory/theory text, unique question content, or any functionality not available elsewhere (per Phase 02's analysis: drills → Öva's Sektionsträning, guides → the individual guide pages, stats → Statistik, the whole-section exam pass is the one unique feature in the hubs, Settings' granular resets + replay-onboarding are the two unique bits there). If you find anything genuinely unique beyond what's already flagged, stop and note it rather than deleting it silently.

  **Completion note:** All three read in full (VerbalHub 313 lines, QuantHub 298, Settings 341) before touching anything. **No question content anywhere** — neither hub imports or embeds a single question; they only compute stats over `questions`/`loadHistory()`. The hubs' only text resembling educational content is the eight one-line `strategy` strings in `TYPE_META` (one per subtest). Checked each against Theory's tabs + the standalone guides: ORD (prefix/rot/etymologi) → Theory ORD strategy 3 + all of OrdGuide; MEK (signalord trots/eftersom) → Theory MEK strategies 1+4; ELF (läs frågan först, lokalisera stycket) → Theory ELF strategy 1; XYZ (rita figur / 60s-regeln) → Theory XYZ strategies 1+4; KVA (extremvärden → D) → Theory KVA strategy 2 + KvaGuide; NOG (behöver inte lösa, testa separat) → Theory NOG intro + trestegsmetoden; DTK (läs axlar/rubrik först) → Theory DTK strategy 1 + HP-insikt. **One tip had no home elsewhere:** LÄS "läs första meningen i varje stycke — bygg en karta" (paragraph-mapping) exists in neither Theory's LÄS strategies (which teach skim-questions-first) nor LasGuide. Per the no-knowledge-loss constraint it was **preserved before deletion** as a new "Bygg en styckeskarta" strategy card in Theory's LÄS tab. Everything else in the hubs is dynamic dashboard data (accuracy bars, due counts, bank sizes, HP-estimat — all in Statistik/SrsQueue/Practice) or duplicated facts (frågor/prov + s/fråga figures match Theory's tab headers and Practice's `TYPE_INFO`). `Settings.tsx` contains zero study material — its stats card, export/import (same `hp_*` keys), notif toggle, focus preference and nuclear reset are all duplicated in Profil (confirmed line-by-line against Profil's `DATA_KEYS`/handlers); its two genuinely unique bits are exactly the flagged ones: granular per-category resets (SRS / historik+XP / bokmärken / brickor) and "Visa introduktion igen". Nothing unique found beyond what Phase 02 flagged — safe to proceed.

- [x] Migrate the hubs' one unique feature — the whole-section exam pass (`/exam/verbal-random`, `/exam/quant-random`, or however it's actually wired — check the real route/handler) — into `ExamSelect.tsx`, so a user can still start a full verbal or quant exam pass from there. Migrate Settings' two unique bits (granular per-category data reset, replay-onboarding) into `Profil.tsx`'s existing settings section, matching its current UI conventions.

  **Completion note:** Checked the real wiring first: both passes are `ExamStart` route params (`/exam/verbal-random` → `getVerbalExamQuestions()`, `/exam/quant-random` → `getQuantExamQuestions()`, each a proper 4-section 55-min pass) — nothing in ExamStart needed touching. **The verbal pass was already half-migrated:** ExamSelect's Verbalt tab already carried a card to `/exam/verbal-random`; only the quant pass was hub-exclusive. Added a matching **"Kvantitativt provpass"** card at the top of ExamSelect's Kvantitativt tab (40 frågor · slumpmässigt urval · 55 minuter, XYZ 12 / KVA 10 / NOG 6 / DTK 12 pills from `SECTION_SIZES`, Starta → `/exam/quant-random`), mirroring the verbal card's markup exactly; renamed the verbal card "Verbalt delprov" → **"Verbalt provpass"** so the pair matches Phase 02's C-exam naming and distinguishes them from the real past-paper entries below. **Settings → Profil:** two new KONTO rows using Profil's existing `SettingsRow` convention — (1) **"Rensa specifik data"** opens a bottom-sheet modal (same overlay pattern as the existing focus/reset modals) with the four per-category resets carried over key-for-key from Settings (`hp_srs`; `hp_session_history`+`hp_current_session`+`hp_gamification`; `hp_bookmarks`; `hp_achievements`), including Settings' two-tap "Tryck igen" confirm with 4 s auto-disarm, its per-category toast messages, and live count badges (pass/bokmärken/brickor/att repetera) that refresh after a reset via a `dataVersion` bump re-running the count memos; (2) **"Visa introduktion igen"** clears `hp_onboarding_done` and hard-navigates to `/` (Settings' exact behavior — App reads the flag at mount). Rows sit between Importera data and the danger-styled Återställ all data, with two new 18px feather-style icons (`EraseIcon`, `ReplayIcon`) matching the existing icon set.

- [x] Delete `VerbalHub.tsx`, `QuantHub.tsx`, `Settings.tsx`, their routes in `App.tsx`, and every remaining reference to them anywhere in the codebase (grep for `VerbalHub`, `QuantHub`, `Settings`, `/verbalt`, `/kvantitativt`, `/settings` to catch stray imports/links Phase 01's map may not have caught).

  **Completion note:** Grepped `src/`, `index.html` and `public/` for all six patterns **before** deleting: the only references anywhere were the three lazy imports + three `<Route>` registrations in `App.tsx` itself — Phase 01's zero-inbound finding held, nothing else in the codebase (TopNav's `HIDDEN_PATHS` included) ever pointed at them. Removed the six `App.tsx` lines, deleted the three files (952 lines total). Post-delete re-grep is clean — the sole remaining textual match is a code comment in ExamSelect noting the quant pass card's provenance ("migrated from the retired QuantHub"), which is documentation, not a reference. `Settings`-the-word survives only as Profil's unrelated `SettingsRow` component. Playwright confirmed `/verbalt`, `/kvantitativt` and `/settings` no longer render their old screens (they fall through the router to an empty outlet like any unknown path — this SPA has no 404 route, pre-existing behavior).

- [x] Re-parent the 4 quant subtest guides (`KvaGuide`, `DtkGuide`, `NogGuide`, `XyzGuide`) under `Theory.tsx`: add "open full guide" banners/links mirroring however the 4 verbal guides are already surfaced there (check `Theory.tsx`'s existing pattern for the verbal guides and match it exactly, per Phase 02's finding that this pattern already exists and just needs extending).

  **Completion note:** Theory's existing pattern: each verbal tab ends with a dedicated banner component (`OrdGuideBanner` etc.) — a `card rounded-2xl p-4 mt-4` button with a 2px colored dot, title, one-line description and `ChevronRight`. Added four identical components: **`XyzGuideBanner`** (→ `/xyz-guide`, "Ämnesområden, strategier och formler för problemlösning", terracotta dot), **`KvaGuideBanner`** (→ `/kva-guide`, "Jämförelsemetoder, extremvärden och vanliga fällor", terracotta), **`NogGuideBanner`** (→ `/nog-guide`, "Svarsalternativen, trestegsmetoden och typuppgifter", green), **`DtkGuideBanner`** (→ `/dtk-guide`, "Diagramtyper, avläsningsfällor och procentberäkningar", gold-deep) — dot colors follow Theory's own `TYPE_COLOR` map so each banner matches its tab's accent. Placement: first banner in each quant tab, above the pre-existing `MathGuideBanner` (+`LiggandeStolenBanner` on XYZ), so the subtest's own guide leads and the cross-cutting math reference stays as the deeper second step — same altitude as the verbal tabs' guide banners. Every quant guide is now reachable at Teori → tab → banner, identical depth to the four verbal guides; descriptions were written from each guide's actual content (read/skimmed to confirm). Playwright clicked all four banners and verified each guide renders.

- [x] Fix the Home pre-selection bug Phase 02 found: `Home.tsx`'s DELPROV cards navigate with `{ state: { defaultType } }` but `Practice.tsx` reads `searchParams.get('type')` and never reads router state, so the pre-selection silently drops. Fix by navigating to `/practice?type=XYZ` (or whatever the real param name is — check `Practice.tsx`'s actual `searchParams` key) instead.

  **Completion note:** Confirmed the bug exactly as Phase 02 described: Practice reads `searchParams.get('type')` (validated against `TYPE_INFO` keys, seeds `selectedTypes` and auto-opens the Avancerat panel) and imports only `useSearchParams` — `location.state` is never read, so Home's `{ state: { defaultType: sec.type } }` was dead weight. One-line fix in `Home.tsx`: `navigate(\`/practice?type=${sec.type}\`)`. The `SECTIONS` `type` values are already exact `QuestionType` keys (incl. `LAS` for the "LÄS"-labeled card), so no mapping was needed. Playwright-verified end-to-end: expanded Home's delprov list, clicked the ELF card → landed on `/practice?type=ELF` with the ELF chip rendered selected (`border-2` + accent style) and the advanced panel auto-opened (screenshot `Working/01-practice-preselected-elf.png`).

- [x] Consolidate the SRS/bookmarks redundancy per Phase 02's decision: replace Öva's "Spaced Repetition" practice mode (which only duplicates SrsQueue's own "Alla →" launch) with a link into the SrsQueue dashboard instead of a separate mode; keep bookmarks reachable from both Öva and wherever else it already is, but remove the now-redundant Profil row once Phase 04 restructures Profil (if that's cleaner to do together, note it and defer explicitly to Phase 04 rather than half-doing it here).

  **Completion note:** `Practice.tsx`: the third mode-selector card ("Spaced Repetition") is replaced by a **"Repetitionskö" link card** — same visual shell (dot, refresh icon, title + due-count line) plus a trailing → to signal navigation, which goes straight to `/srs` instead of arming a parallel launcher; the label matches SrsQueue's own h1 so the destination is predictable. With the mode gone, all its dead logic went too: `Mode` narrowed to `'drill' | 'exam'`, the repetition branch in `start()`, the repetition-only due-by-type panel, the `dueByType` memo, the `mode !== 'repetition'` render guards, and the now-unneeded `ExamSession` type import — SrsQueue's dashboard (which Playwright confirmed still launches per-type and "Alla →" drills) is now the single home for repetition, reached from Öva in one tap. Bookmarks: **kept both existing entrances untouched** (Öva's Bläddra/Drill card and Profil's "Sparade frågor" row). **The Profil row's removal is explicitly deferred to Phase 04**, which demotes Profil to identity+settings and removes the whole STUDIEVERKTYG group in one pass — deleting one row now would half-do that restructure and churn the same lines twice.

- [x] Run `npm run build` and `npm test`. Use Playwright to verify: `ExamSelect` can still launch a full verbal/quant exam pass, `Profil` has the two migrated Settings features working, all 4 quant guides are reachable from `Theory` and render correctly, the Home pre-selection bug fix actually works (click a DELPROV card, confirm Practice opens with that type pre-selected), and no broken links/404s remain anywhere from the deletions (click through every remaining nav path).

  **Completion note:** `npm run build` (tsc + vite) exits clean, `npm test` 14/14 — both re-run after the final edit. A throwaway Playwright script (repo's own `playwright` devDependency, 402×874, same approach as prior playbooks) ran **55 checks, 55 passed**: (1) all 24 remaining routes render real content (`/`, `/practice`, `/theory`, `/progress`, `/exam-select`, `/exam/random|verbal-random|quant-random|full-hp`, `/matematik`, `/bookmarks`, `/srs`, `/liggande-stolen`, all 8 subtest guides, `/score`, `/ord-builder`, `/profil`) and the three deleted routes no longer render their old screens; (2) ExamSelect: quant tab shows "Kvantitativt provpass" → `/exam/quant-random` start page showing 40 frågor · 55 minuter; verbal tab's "Verbalt provpass" → `/exam/verbal-random` → **Starta provet clicked, session actually launched** into `/session`; (3) Profil: granular modal opens, "Rensa bokmärken" arms with "Tryck igen", second tap removes `hp_bookmarks` while other keys survive, toast "Bokmärken rensade" shows, count badge refreshes; "Visa introduktion igen" clears `hp_onboarding_done` and lands on `/` with the onboarding overlay up; (4) all four Theory quant banners clicked → each guide renders, verbal ORD banner spot-checked as regression; (5) Home ELF card → `/practice?type=ELF` with the chip pre-selected; (6) Öva's Repetitionskö card → `/srs`, no parallel SRS mode remains; (7) every top-nav tab (Hem/Öva/Statistik/Profil) clicked and renders. Screenshots `Working/01`–`06` (practice pre-selected, Öva mode cards, ExamSelect quant pass, Theory KVA banner tab, granular-reset modal, onboarding replay) all visually inspected. No console pageerrors during the run.

- [x] Report: confirm no knowledge content was lost (explicitly re-affirm this), confirm the 3 deletions are clean (no dangling references), confirm the migrated features work, confirm the bug fix works, and confirm build/tests pass.

  **Completion note:** Reported to the user. Summary of the confirmations: **no knowledge content lost** — verified against source, not the prior phase's summary; the one at-risk item found (VerbalHub's LÄS paragraph-mapping tip) was preserved into Theory's LÄS tab before deletion, everything else in the three files was dashboard data or duplicates of content that lives on; **deletions clean** — only App.tsx referenced the three components, post-delete grep finds zero dangling imports/links; **migrations work** — quant/verbal provpass launch from ExamSelect (verbal driven all the way into a live session), granular resets clear exactly their keys with two-tap confirm + toast, replay-onboarding restarts the wizard; **bug fix works** — Home delprov card → `/practice?type=X` with the type pre-selected on arrival; **build + tests pass** — tsc/vite clean, 14/14 vitest. Net for this phase: −3 routes, −952 lines of dead/divergent code, +4 quant guides reachable, 1 silent navigation bug fixed, SRS given a single home.
