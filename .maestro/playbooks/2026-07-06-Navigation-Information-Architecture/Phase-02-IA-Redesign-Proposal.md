# Phase 02: IA Redesign Proposal

**Decisions only — no implementation in this phase.** Using Phase 01's `Working/ia-map.md` and `Working/ia-findings.md` as ground truth, propose a concrete redesigned information architecture: what happens to every orphaned/redundant/buried route found, and how the app's navigation should actually be organized going forward. Read the playbook `README.md` (operating principles) first.

Phase 01 found: 6 effectively-dead routes (`VerbalHub`, `QuantHub`, `Settings`, and the 4 quant guides orphaned by `QuantHub`'s own orphaning), an incomplete reachable guide index (`Theory` missing all 4 quant guides), Profil confirmed as an undifferentiated catch-all for every non-tab feature, and several redundant/overlapping pairs (MathGuide vs XyzGuide, three unlabeled section-start entry points, SRS as both page and mode, bookmarks reachable two ways).

## Tasks

- [x] For each of the 6 dead/orphaned routes, decide and justify: wire it in properly, merge its useful parts into something else and delete it, or delete outright. Specifically address: `VerbalHub`/`QuantHub` (they're reportedly the best-built section indexes in the app but zero inbound links — is the right move to make them the real index and retire the incomplete `Theory` page, or fold their content into `Theory` and delete the hubs, or something else?), `Settings.tsx` (orphaned duplicate of Profil logic — confirm it's truly redundant by diffing against Profil's actual settings section before recommending deletion, don't assume from Phase 01's summary alone), and the 4 quant guides (once their gating page's fate is decided, make sure they have a real path too).

  **Completion note:** Read all six source files in full plus Profil/Theory/Practice/Home to decide (Part A of `Working/ia-redesign-proposal.md`). Corrected Phase 01 on two points after reading: (1) `VerbalHub`/`QuantHub` are **not** guide indexes — they're section *practice dashboards* mixing stats + drill-launch + guide-links + a section exam-pass on one screen (violates one-purpose-per-screen). **Decision: DELETE both**; their four jobs already have homes (drills→Öva Sektionsträning, per-type SRS→SrsQueue tiles, score→Statistik, guides→Theory); only unique bit, the whole-section exam pass (`/exam/verbal-random`, `/exam/quant-random`), moves into ExamSelect. Flagged the delete-vs-wire-in trade as Open Question 1 for the user. (2) `Settings.tsx` diffed against Profil's KONTO/settings: **not a pure duplicate** — core export/import/reset-all/notif/focus is duplicated (same `DATA_KEYS`), but Settings additionally has granular per-category resets + "replay onboarding" that Profil lacks. **Decision: DELETE** (orphaned + divergent copy = maintenance liability), migrating those two small bits into Profil first (Open Question 3). The **4 quant guides**: KEEP, re-parent under Theory — Theory already has XYZ/KVA/NOG/DTK tabs but (confirmed in source) no "open full guide" banners to `/xyz|kva|nog|dtk-guide`; add four banners mirroring the verbal ones so each is reachable once the gating hub is gone.

- [x] Decide what to do about each redundant/overlapping pair Phase 01 found: MathGuide vs XyzGuide (merge? keep both with clear differentiation? deprecate one?), the three unlabeled "start a section" entry points (Home's delprov cards, Practice's Sektionsträning grid, the hubs' own buttons — do these need consolidating, or do they serve genuinely different moments in the user's flow and just need clearer differentiation?), SRS as both a standalone page and a Practice mode, and bookmarks being reachable two ways. For each, the operating principle is "reduce before add" — default toward consolidation unless there's a real reason two paths serve different needs.

  **Completion note:** Part B of the proposal, each decided from source. **MathGuide vs XyzGuide — KEEP BOTH (corrects Phase 01):** they're layered, not redundant — XyzGuide is the XYZ subtest guide (light Ämnen/Strategi/Formler tabs, one of 8), MathGuide is a deep cross-cutting reference (10 LaTeX topic cards, worked examples, self-check) that XyzGuide deliberately links *into*; just clarify labels. **Three section-start surfaces — CONSOLIDATE to two + fix a bug:** the hub buttons vanish with A1; Home cards ("jump to my weak delprov from the dashboard") and Öva Sektionsträning ("one-tap timed drill while already in practice") serve different moments → keep both. New finding: Home's DELPROV cards are silently broken — `Home.tsx:288` passes `{ state: { defaultType } }` but Practice reads `searchParams.get('type')` and never reads `location.state`, so the pre-selection is dropped; fix = navigate to `/practice?type=XYZ`. **SRS page vs mode — KEEP page as SRS home:** SrsQueue is a real dashboard (due/overdue, per-type tiles, mastery breakdown, 7-day forecast); the Öva "Spaced Repetition" mode only duplicates its "Alla →" launch → replace that mode with a link into the SRS dashboard. **Bookmarks two ways — KEEP both entrances**, set primary home in Öva (practice-adjacent), drop the redundant Profil row.

- [x] Propose a restructured Profil: right now it's a flat list mixing daily-use tools (SRS, exam simulator, bookmarks) with one-time settings (export/reset data, notification toggle). Propose a grouping/hierarchy that separates "things you'll tap often" from "things you set once and forget," and decide whether any of the daily-use tools are important enough to deserve a shorter path than "inside Profil" at all (e.g. does bookmarks or SRS belong closer to Öva, given they're practice-adjacent, not profile-adjacent?).

  **Completion note:** Part C. **Profil is demoted to an identity + set-once screen:** keeps only the identity card, the goal/exam-date card, an INSTÄLLNINGAR group (notif toggle, focus priority — the only genuine "set once" toggles) and a KONTO group (export/import/granular-reset/reset-all). **Every daily-use tool in today's STUDIEVERKTYG is promoted out of Profil** to its logical home: Teori & guider → Öva (card) + Home; Repetitionskö → Öva (into the SRS dashboard); Provsimulatorn → Öva/Home; Ordbyggaren → Öva (learning); HP-poängprediktor → Statistik only (drop Profil copy); Sparade frågor → Öva (drop Profil row). Answered the task's explicit question: yes — SRS and bookmarks are practice-adjacent, not profile-adjacent, so they belong in the Öva orbit; the exam simulator likewise. Net effect: no daily feature is buried in a settings list; each promoted tool is reachable from the surface whose job it obviously is, rather than 2–3 taps deep in Profil. Full mapping table in the proposal.

- [x] Write the full proposal to `Working/ia-redesign-proposal.md`: for every route touched, its disposition (kept as-is / promoted / demoted / merged into X / deleted) with one-sentence reasoning tied back to the operating principles (does this reduce cognitive load, does it make the next step more obvious), plus a proposed new grouping/structure for Profil and for wherever the guide-index content ends up living. This is what the user will read and approve or redirect before any implementation phase touches code.

  **Completion note:** Written to `Working/ia-redesign-proposal.md`. Contains: Parts A–C with per-decision reasoning tagged to the operating principles ([P1]/[P2]/[P3]/[P5]); a full **disposition table** covering every touched route (delete ×3 incl. both hubs + Settings, keep/re-parent the 4 quant guides + MathGuide, keep+promote Theory/SrsQueue/ExamSelect, demote Profil, fix Home cards, replace Öva SRS mode), plus the routes explicitly left untouched; the new Profil structure (identity + goal + INSTÄLLNINGAR + KONTO) with the promotion mapping table; and the single-index plan (Theory completed + promoted, all standalone guides re-parented, hubs deleted). Corrections to Phase 01 are marked **[verify]** inline. Ends with three Open Questions flagged for the user.

- [x] Report back: a concise summary of the proposal (what's being cut, merged, promoted, and why), explicitly flagging any call that's genuinely a toss-up or that trades off two of the operating principles against each other (e.g. "fewer nav destinations" vs. "the next step being obvious") so the user makes that call, not the model.

  **Completion note:** Reported to the user: cuts (VerbalHub, QuantHub, Settings — all orphaned; their unique parts salvaged first), keeps-with-clarification (MathGuide/XyzGuide are layered not duplicate; 4 quant guides re-parented under a completed Theory), promotions (Theory/SRS/exam/bookmarks lifted out of Profil into Öva/Statistik), and Profil demoted to identity+settings. Three genuine trade-offs handed to the user, not decided by the model: (1) delete-and-distribute the hubs vs. wire them in as rich section landing pages — reduce/one-purpose vs. one-obvious-hub-per-section; (2) Teori as a 5th nav tab vs. surfaced in Öva — discoverability vs. 4-tab minimalism; (3) whether to migrate or drop Settings' two unique power-user features. Also surfaced one latent bug found while auditing (Home DELPROV cards pass router state Practice never reads).
