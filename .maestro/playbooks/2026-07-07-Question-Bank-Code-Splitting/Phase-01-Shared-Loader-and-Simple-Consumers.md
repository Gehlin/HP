# Phase 01: Shared Loader & Simple Consumers

Read the playbook `README.md` first — it explains why splitting by subtest doesn't work and what the actual fix is (convert all static imports to dynamic, via one shared loading utility).

## Tasks

- [x] Delete `src/components/BottomNav.tsx` (confirmed dead — zero imports anywhere, superseded by `AppHeader.tsx`). Confirm with a grep that nothing references it before deleting.

  **Completion note:** Ran `grep -rn "BottomNav" src/` before deleting — the only match was the file's own `export default function BottomNav()` declaration, zero import references anywhere else in `src/`. Deleted `src/components/BottomNav.tsx` (81 lines). This also removed one of the then-19 non-test static importers of `src/data/questions.ts`.

- [x] Read every one of the current static importers of `src/data/questions.ts` (grep `from '.*data/questions'` across `src/`, excluding `*.test.ts` files) and classify each by how it uses `questions`: does it need the full array (search/filter across all types), does it need to look up specific ids, does it need one subtest's worth, etc. This classification determines how each consumer should call the new loader.

  **Completion note:** Grepped both `from '../data/questions'` and `from './questions'` (needed the second form too — `src/data/exams.ts` imports it as `./questions` since it lives in the same directory, and was missed by a first pass that only searched for the `data/questions` substring). Found **19 non-test static importers** (18 originally listed plus `BottomNav.tsx`, now deleted): `src/utils/{achievements,analytics,dailyChallenge,readiness,session}.ts`, `src/data/exams.ts`, and 12 pages (`Bookmarks, Granska, Home, MathGuide, OrdBuilder, Practice, Profil, Progress, Resultat, ScorePredictor, Session, SrsQueue`). Classification:
  - **Full-array filter/find by id, needed synchronously at call time from a page's render body or `useMemo`** (not just inside a `useEffect`): `src/utils/analytics.ts` (all 8 exports — `Progress.tsx` and `ScorePredictor.tsx` call them directly in the component body, `Practice.tsx` in a `useMemo`), `src/utils/readiness.ts` (`computeReadiness()` — called directly in `Progress.tsx`'s render body, and transitively via `src/utils/pacing.ts` → `src/utils/notifications.ts`, also synchronous), `src/utils/session.ts` (used for passage-grouping and answer-key lookup inside `buildSession`/`finishSession`, called from 13 different files including the exam-taking critical path). These need real component-level restructuring (loading states, `useEffect`+`useState` instead of direct render-body calls) — out of scope for a "simple consumer" pass.
  - **Full-array filter/find by id, but the only usage is already inside a `useEffect` with exactly one call site**: `src/utils/achievements.ts` (`checkAchievements()` — sole caller `Resultat.tsx` line ~106, inside `useEffect`), `src/utils/dailyChallenge.ts` (`getDailyChallengeIds()` — sole caller `Practice.tsx` line ~123, inside `useEffect`). Clean async-conversion candidates.
  - **Page-level consumers using `questions` directly in render bodies, `useMemo`, and effects for filtering/search/passage-lookup** (`Bookmarks, Granska, Home, MathGuide, OrdBuilder, Profil, Progress, Resultat, ScorePredictor, Session, SrsQueue`, plus `src/data/exams.ts` for exam-section filtering): all still hold their own static import; each needs the same async-conversion treatment as a full page (loading state, `useQuestions()` hook), which is Phase 02's job.

- [x] Build a shared async loader — e.g. `src/data/questionsLoader.ts` exporting something like `loadQuestions(): Promise<Question[]>` that internally does `const mod = await import('./questions'); return mod.questions` but **caches the resolved promise** so repeated calls across different components don't re-trigger the dynamic import or show a loading flash after the first successful load. Consider also exporting a small React hook (e.g. `useQuestions()`) wrapping this in a `useState`/`useEffect` pattern with an explicit loading state, since most consumers are components that need to render *something* before the data resolves.

  **Completion note:** Created `src/data/questionsLoader.ts` with a module-level `let cached: Promise<Question[]> | null` and `loadQuestions()` that lazily assigns `cached = import('./questions').then(mod => mod.questions)` on first call and returns the same cached promise on every subsequent call (so concurrent callers within the same tick share one in-flight import, and later calls resolve off the cached array with no re-fetch). Also exported `useQuestions()`, a small hook wrapping it in `useState<Question[] | null>` + `useEffect` (with a `cancelled` flag to avoid a `setState` after unmount), returning `null` until the first resolution. Also updated `src/App.tsx`'s existing ad-hoc `import('./data/questions').then(...)` (added in an earlier session) to call `loadQuestions()` instead, so it shares the same cache as every other consumer rather than doing its own separate dynamic import.

- [x] Convert the simpler/leaf consumers first (utility files without their own rendering — `src/utils/achievements.ts`, `src/utils/analytics.ts`, `src/utils/dailyChallenge.ts`, `src/utils/readiness.ts`, `src/utils/session.ts` — check which of these actually need synchronous access at call time vs. can become async themselves) to use the new loader instead of a static import. For utility functions that are called synchronously from many places, converting them to async may have ripple effects — if a utility function's signature would need to become `async` and that breaks too many callers in ways that don't fit this phase's scope, note it and defer to Phase 02 rather than forcing a bad fit.

  **Completion note:** Of the 5 listed utils, **2 converted, 3 deferred** — only 2 turned out to be genuinely "leaf" in the sense that mattered (a single call site already inside a `useEffect`, not a render body):
  - **Converted `src/utils/achievements.ts`**: `checkAchievements()` → `async`, now does `const questions = await loadQuestions()` instead of a static import. Its one real caller, `src/pages/Resultat.tsx` (inside a `useEffect`), was updated from `const newlyEarned = checkAchievements()` to `checkAchievements().then(newlyEarned => { ... })` — a 4-line change, no restructuring needed since the rest of that effect doesn't depend on the result. Updated `src/utils/achievements.test.ts`'s 31 call sites to `await checkAchievements()` (all test callbacks were already `async`).
  - **Converted `src/utils/dailyChallenge.ts`**: `getDailyChallengeIds()` → `async`, same `await loadQuestions()` pattern. Its one caller, `src/pages/Practice.tsx` (inside a `useEffect` gated on `isDaily`), was updated to `getDailyChallengeIds().then(ids => { ... })`, wrapping the existing session-build/navigate logic. Updated all 7 call sites in `src/utils/dailyChallenge.test.ts` to `await`.
  - **Deferred `src/utils/analytics.ts`**: all 8 exported functions (`timeAnalyticsByType`, `accuracyByDifficulty`, `hpScoreHistory`, `typeAccuracyTrend`, `weakTypeSummary`, `buildWeakAreaSession`, `rollingHpScore`, plus the internal `buildByType` helper) use `questions` synchronously. `Home.tsx`'s calls are inside a `useEffect` (would've been convertible alone), but `Progress.tsx` calls 5 of them directly in the render body, `ScorePredictor.tsx` calls `hpScoreHistory()` directly in the render body, and `Practice.tsx` calls 2 of them inside `useMemo` (render-time, not effect-time). Since it's one shared module with one API, converting it means converting `Progress.tsx`/`ScorePredictor.tsx`/`Practice.tsx` too — real page-level restructuring, Phase 02's explicit scope (`Practice.tsx` is even named there). Converting only the `Home.tsx`-safe half would leave a half-async/half-sync module with an inconsistent API for no real win, so the whole file is deferred as one unit.
  - **Deferred `src/utils/readiness.ts`**: `computeReadiness()` is called directly in `Progress.tsx`'s render body (not an effect) and, transitively, via `src/utils/pacing.ts`'s `computePacing()` (also fully synchronous) from `src/utils/notifications.ts`. Two independent synchronous call chains outside a single `useEffect` — deferred alongside `Progress.tsx`.
  - **Deferred `src/utils/session.ts`**: `buildSession`/`finishSession`/`groupLinkedPassages` use `questions` for passage-grouping and answer-key lookup, and this module is imported by 13 different files (`App.tsx` and effectively every page in the exam-taking and results flow). It's the most central and highest-blast-radius consumer in the whole app — explicitly Phase 02 territory (README names `Session.tsx` first for exactly this reason).
  No page-level consumers (`Bookmarks.tsx`, `Granska.tsx`, `Home.tsx`, `MathGuide.tsx`, `OrdBuilder.tsx`, `Profil.tsx`, `Progress.tsx`, `Resultat.tsx`, `ScorePredictor.tsx`, `Session.tsx`, `SrsQueue.tsx`, `src/data/exams.ts`) were converted in this phase — all still hold a static `questions` import and are left for Phase 02.

- [x] Run `npm run build` after each conversion and check the bundle-size warning — confirm it's actually shrinking as consumers get converted (don't wait until the end to check). Run `npm test`.

  **Completion note:** Ran `npm run build` (`tsc && vite build`) after every conversion step, not just at the end:
  - Baseline (before any change): `dist/assets/index-*.js` **1,807.95 kB / gzip 458.99 kB**; warning listed all 17+ static importers.
  - After deleting `BottomNav.tsx` + wiring `App.tsx` to `loadQuestions()`: **1,807.91 kB / gzip 458.95 kB** — negligible (`BottomNav.tsx` itself was ~1KB of dead code, and its removal was the only thing that could move the needle here); warning unchanged in substance (still lists `exams.ts`, `Bookmarks.tsx`, `Granska.tsx`, `Home.tsx`, `MathGuide.tsx`, `...`).
  - After converting `achievements.ts` alone: **1,807.92 kB / gzip 458.95 kB** — effectively flat.
  - After converting `dailyChallenge.ts` as well (final state for this phase): **1,807.92 kB / gzip 458.95 kB** — still flat, and the `INEFFECTIVE_DYNAMIC_IMPORT` warning is still present, still listing the remaining static importers.
  This is the expected, honest result and matches the README's warning precisely: Rollup's decision to move `questions.ts` into its own chunk is **all-or-nothing** per module — as long as even one file still statically imports it, the dynamic import buys zero bytes, no matter how many *other* consumers get converted. Converting 2 of ~19 static importers (leaving ~17, all page-level or `Progress`/`analytics`/`session`-critical) was never going to move this build's number, and it didn't. The real payoff only appears once Phase 02 finishes the remaining page-level consumers and the very last static import is gone.
  `npm test` (`vitest run`): **213/213 tests passed**, 11 test files, no regressions — includes the updated `achievements.test.ts` (31 call sites now `await`ed) and `dailyChallenge.test.ts` (7 call sites now `await`ed).

- [x] Report: which consumers were converted, which were deferred to Phase 02 and why, the bundle size before/after this phase's changes (exact numbers from the build output), and confirmation build/tests pass.

  **Completion note:** Reported to the user/caller — summary below (see also each task's completion note above for full detail):
  - **Deleted**: `src/components/BottomNav.tsx` (confirmed dead via grep).
  - **Converted** (2): `src/utils/achievements.ts` (`checkAchievements`) and `src/utils/dailyChallenge.ts` (`getDailyChallengeIds`), both now `async` via the new `src/data/questionsLoader.ts` (`loadQuestions()` cached-promise loader + `useQuestions()` hook). Their single call sites (`Resultat.tsx`, `Practice.tsx`, both already inside a `useEffect`) were updated to `.then()`. `App.tsx`'s pre-existing ad-hoc dynamic import was also switched to the shared loader for cache consistency.
  - **Deferred to Phase 02** (all for the same underlying reason — synchronous use in a component's render body or `useMemo`, not just inside a `useEffect`, which needs real page-level restructuring): `src/utils/analytics.ts` (Progress.tsx/ScorePredictor.tsx render body, Practice.tsx `useMemo`), `src/utils/readiness.ts` (Progress.tsx render body + `pacing.ts`→`notifications.ts` chain), `src/utils/session.ts` (13 callers, core exam-flow critical path), and all 12 page-level consumers (`Bookmarks, Granska, Home, MathGuide, OrdBuilder, Profil, Progress, Resultat, ScorePredictor, Session, SrsQueue`, plus `src/data/exams.ts`) that still hold their own static `questions` import.
  - **Bundle size**: `dist/assets/index-*.js` **1,807.95 kB → 1,807.92 kB** (gzip **458.99 kB → 458.95 kB**) — essentially flat. This is expected, not a shortfall: Rollup only moves `questions.ts` into its own chunk once *every* static importer is gone, so converting 2 of ~19 (leaving ~17 remaining, all deferred above) cannot shrink the build yet. The `INEFFECTIVE_DYNAMIC_IMPORT` warning is still present after this phase's changes, listing the remaining static importers — the real bundle-size win is gated entirely on Phase 02 finishing the rest.
  - **Build/tests**: `npm run build` (`tsc && vite build`) passes clean at every step; `npm test` (`vitest run`) — **213/213 passing**, 11 test files, no regressions.
