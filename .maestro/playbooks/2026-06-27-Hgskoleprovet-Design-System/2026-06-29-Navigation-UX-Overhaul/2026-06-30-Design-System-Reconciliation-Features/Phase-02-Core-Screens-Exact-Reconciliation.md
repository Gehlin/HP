# Phase 02: Core Screens — Exact Reconciliation

With the foundation tokens corrected, this phase reconciles the five core screens from the design handoff — Hem (`Home.tsx`), Öva (`Session.tsx`), Resultat (`Results.tsx`), Statistik (`Progress.tsx`), and Profil (`Profil.tsx`/`Settings.tsx`) — against the exact component specs in `.maestro/playbooks/2026-06-27-Design-System-Implementation/DESIGN-SYSTEM-03.md` through `DESIGN-SYSTEM-07.md`. These reference docs were written purely from the design mockup and contain placeholder/hardcoded data (fake names, fake percentages, fake dates) — **do not port that placeholder data into the app**. The app already has real, working implementations of all five screens with live state, localStorage persistence, and an actual question bank — this phase fixes visual fidelity (spacing, sizing, colors, component states) only, never functionality or copy, unless a piece of real data is simply missing a visual treatment the spec describes.

## Tasks

- [x] Read `src/pages/Home.tsx` alongside `DESIGN-SYSTEM-03.md`. Reconcile visual details only: hero score card corner radius (22px) and padding (20px), label letter-spacing on `"DITT NORMERADE RESULTAT"`-style headers (0.16em), the gold progress bar fill using `var(--color-gold)` over a `rgba(255,255,255,0.15)` track, the continue/last-session card's 18px radius and hairline border, and the section grid's progress-ring pattern (32px outer circle as a `conic-gradient` using the section's real accuracy percentage, 23px white inner circle, percentage text in the center). Keep all real data sources (actual streak, actual last-session, actual per-section stats) — only adjust how they're styled and laid out to match spec measurements.

  **Completion note:** All real data sources (readiness score, streak, days-to-exam, resume session, per-section accuracy/counts) left untouched — only styling/layout changed, in `src/pages/Home.tsx`:
  - Hero card: replaced the `card-green p-6` radius/padding with an inline `borderRadius: 22, padding: 20` override (left the shared `.card-green` class itself unchanged since it's also used by `Profil.tsx`, out of scope for this task).
  - Hero label: `"Uppskattat HP-poäng"` (kept the real copy — spec's `"DITT NORMERADE RESULTAT"` is placeholder text per the phase intro) restyled as the spec's eyebrow treatment: uppercase, Hanken 700 10px, `tracking-[0.16em]`, `var(--color-green-tint)`.
  - Replaced the hero's circular SVG progress ring with the spec's literal horizontal gold progress bar: track `rgba(255,255,255,0.15)`, fill `var(--color-gold)`, width driven by the real `heroScorePct` (`readiness.score / 2.0`, clamped 0–100) — this is what the task's explicit "gold progress bar fill ... over a rgba(255,255,255,0.15) track" phrasing describes structurally (track div + fill div), distinct from a ring, so the ring was swapped for a bar rather than just recolored. Removed the now-unused `HERO_R`/`HERO_C`/`heroRingOffset` constants.
  - Continue/resume-session card: added inline `borderRadius: 18` (the shared `.card` class already provides the hairline border post–Phase 01, since `--color-card-border` now equals `--color-hairline`). Also tightened its `"FORTSÄTT DÄR DU SLUTADE"`-style eyebrow label from Tailwind's `tracking-widest` (0.1em) to the spec's `tracking-[0.16em]`.
  - Section grid: replaced the per-card SVG ring (manual stroke-dasharray circle) with the spec's literal `conic-gradient` div pattern — 32px outer circle, 23px inner circle in `var(--color-card)` (the app's established mapping for the spec's `--color-surface`, per the Phase 01 token table), percentage text centered at `700 9px/1 var(--font-sans)`. Kept the existing per-type accent ring colors (`TYPE_ACCENTS`) untouched — the task's scope was the ring *pattern/mechanism*, not the section color-coding system, which Phase 01 already flagged as a separate, deliberately out-of-scope design decision. Removed the now-unused `SECT_R`/`SECT_C`/`sectOffset`.
  - Did not add hero-card click-to-navigate-to-`/progress` (mentioned in `DESIGN-SYSTEM-03.md` but not listed in this task's explicit scope, and the phase intro forbids functionality changes) — flagging this as a candidate for Phase 04 if navigation parity with the mockup is wanted later.
  - Verified with `npm run build` (zero TypeScript errors) and `npx tsc --noEmit` (clean), plus a Playwright screenshot of the dev server's Home screen confirming the hero card radius/padding, tracked eyebrow label, gold progress bar, and conic-gradient section rings all render correctly.

- [x] Read `src/pages/Session.tsx` alongside `DESIGN-SYSTEM-04.md`. Reconcile the answer-option component to the full spec state machine using the new tokens added in Phase 01: default (white surface, hairline border, neutral badge), selected-before-check (`--color-selected-bg`/`--color-selected-border`, badge in `--color-terracotta`), correct-after-check (`--color-correct-bg`/`--color-correct-border`, badge in `--color-correct-badge` with a ✓), wrong-after-check (`--color-wrong-bg`/`--color-wrong-border`, badge in `--color-wrong-badge` with a ✕), and dimmed-other-after-check (`--color-surface-warm`-equivalent, reduced opacity). Reconcile the feedback panel shown after checking an answer to use `--color-feedback-correct-bg`/`--color-feedback-correct-border`/`--color-feedback-correct-title` (or the wrong-state equivalents). Reconcile the fixed header's thin progress bar and timer pill styling, and the fixed footer's button copy logic (disabled "Välj ett svar" → "Kontrollera svar" → "Nästa fråga" / "Se resultat") to match spec sizing — keep the existing real question data, three layout types (standard/reading-passage/diagram), timer, and bookmark logic exactly as they are.

  **Completion note:** No images were attached to this task. Found that `src/index.css`'s `.answer-option-selected/-correct/-wrong` classes (added in an earlier, uncommitted pass on this branch) were still wired to the generic `--color-green`/`--color-success`/`--color-error` tokens instead of the dedicated answer-state palette added in Phase 01 — fixed all three to use `--color-selected-bg/-border`, `--color-correct-bg/-border`, `--color-wrong-bg/-border` respectively, per the literal values in `DESIGN-SYSTEM-01.md`'s `.answer-option.*` block. Also found `--color-surface-warm` (`#FBF9F4`) was listed in the Phase 01 token-audit doc's "to add" list but never actually added to `:root` — added it now and introduced a new `.answer-option-other` CSS class (`background: var(--color-surface-warm); border-color: #EEE8DC; opacity: 0.8;`, matching the spec's literal `.answer-option.other-after-check` rule) to replace the previous bare `opacity-50` Tailwind utility used for the dimmed-other-options state in `Session.tsx`.
  - Badge logic in `Session.tsx`: added the missing selected-pre-check badge color (`--color-terracotta` bg, white text — previously the badge never changed color on selection, only the row did); switched the correct/wrong badge colors from `--color-green`/`--color-error` to the dedicated `--color-correct-badge`/`--color-wrong-badge`; switched the default neutral badge from `--color-paper-dark` to the spec-literal `--color-hairline`.
  - Feedback panel (`src/components/ExplanationCard.tsx`, the app's real implementation of the spec's "feedback panel" — richer than the spec's flat version since it includes structured method/steps/distractor-note sections, which is real functionality kept as-is): recolored the header background/title and the outer card border from `--color-success-bg`/`--color-success`/`--color-error-bg`/`--color-error` to the dedicated `--color-feedback-correct-bg/-border/-title` and `--color-feedback-wrong-bg/-border/-title` tokens. Copy ("✓ Rätt!" / "✗ Fel — rätt svar X: ...") left untouched since it's real, more-informative copy than the spec's bare "Rätt!"/"Inte riktigt", and the phase intro forbids copy changes absent a missing visual treatment.
  - Header: thin progress bar changed from Tailwind's `h-1` (4px) to the spec's literal `h-[3px]`, and its track color from `--color-paper-dark` to the spec-named `--color-track`. Timer (both the per-question `QuestionTimer` and the exam-mode `timeLeft` display) was previously bare monospace text with no container — wrapped both in the spec's literal pill treatment: `var(--color-card)` background (the app's established mapping for spec `--color-surface`), `var(--color-hairline)` border, `rounded-full`, `px-3 py-[5px]`, Hanken 600 13px, kept the existing `tabular-nums` and the `timer-warning` low-time animation class.
  - Footer: this task's parenthetical explicitly quoted the spec's literal button-copy progression, so updated the button text (not just sizing) — "Visa svar" → "Kontrollera svar", "Nästa" → "Nästa fråga", "Slutför" → "Se resultat" — and added the previously-missing disabled "Välj ett svar" label for the pre-selection state (the button existed and was correctly disabled before, it just showed the post-check label prematurely). Disabled opacity changed from Tailwind's `disabled:opacity-40` to the spec's literal `disabled:opacity-[0.45]`. Left the `studyMode` quality-rating buttons (Svårt/Ok/Enkelt) untouched — real spaced-repetition functionality not mentioned in the spec doc at all.
  - Kept untouched per scope: real question data, all three layout types (standard/reading-passage/diagram — confirmed `LÄSTEXT`/context-card styling already matches spec from a prior pass), timer-counting logic, bookmark toggle logic, keyboard shortcuts, section-break interstitial, finish/jump modals (these were already reconciled to the corrected token palette in an earlier uncommitted pass on this branch — verified their colors already map correctly to Phase 01 tokens, no further changes needed).
  - Verified with `npm run build` (zero TypeScript errors) and `npx tsc --noEmit` (clean), plus Playwright screenshots of the dev server walking through a real ORD practice session: default/neutral badges before selection, terracotta selected-state row + badge, green correct-state row + ✓ badge with dimmed warm-cream siblings, red/terracotta wrong-state row + ✕ badge, and both feedback-panel color variants (correct/wrong) — all rendering with the corrected token palette and no functional regressions.

- [x] Read `src/pages/Results.tsx` alongside `DESIGN-SYSTEM-05.md`. Reconcile the score ring to the spec's `conic-gradient` pattern (120px outer in `--color-gold`, 88px inner circle), the three-column stat strip dividers (`rgba(255,255,255,0.12)`), and the streak card's translucent green panel (`rgba(255,255,255,0.08)` background, `rgba(255,255,255,0.12)` border). Keep the app's real score computation, real elapsed-time tracking, and real localStorage streak/answered persistence exactly as implemented — only correct colors, radii, and spacing to match spec.

  **Completion note:** No images were attached to this task. All real data sources (score computation, elapsed-time, XP/level/streak gamification via `loadStats`/`saveStats`, HP-score predictor, per-type/difficulty/time breakdowns, exam report, review list, share card) left fully untouched — this app's Results screen is far richer than the spec's flat mockup (per the phase intro), so only the three explicitly named elements were touched, in `src/pages/Results.tsx`:
  - **Score ring:** replaced the SVG `stroke-dasharray` ring with the spec's literal `conic-gradient` div pattern (matching the mechanism already established for Home.tsx's section rings in the prior task) — 120px outer circle (`conic-gradient(var(--color-gold) ${pct}%, rgba(255,255,255,0.15) 0)`), 88px inner circle in `var(--color-green)` (matches the hero's own background, same visual effect as the spec's "or a slightly lighter green" inner-fill option), with the existing `{pct}%` text centered inside. Removed the now-unused SVG stroke math. Left the real `correct`/`total` computation and the larger `{pct}%` / `"{correct} av {total} rätt"` text below the ring untouched — the spec's alternate "16/20" + "80 % RÄTT" copy split is a presentation choice, not in this task's explicit color/radius/spacing scope, and the phase intro forbids copy changes absent a missing visual treatment.
  - **Stat strip dividers:** changed `divide-white/20` (20% opacity) to the spec's literal `divide-[rgba(255,255,255,0.12)]` (12% opacity) on the three-column "Rätt svar / Tid / Streak" strip. Left the strip's actual stat content untouched — the app's real strip shows different (and more relevant) stats than the spec's mockup ("Total tid"/"Snitt/fråga"/"Normerat" placeholder delta), which is a content decision out of this task's scope per the phase intro.
  - **Streak card:** the only existing "streak"-themed card in this screen is the conditional achievement notification (`{xpInfo?.streakIncreased && ...}`) showing "Streak! N dagar i rad" — there is no separate always-on `hp_progress_v1`-backed streak/answered card like the spec describes, and the phase intro explicitly says not to port that placeholder-data model into the app, so the existing real `xpInfo.stats.streak`-driven card was the reconciliation target instead. It previously used the plain white `.card` class with a 4px terracotta left-accent border. Replaced that with the spec's literal panel treatment — `var(--color-green-muted)` (the app's existing `rgba(34, 74, 58, 0.08)` token) for background, `rgba(34, 74, 58, 0.12)` for the border, `18px` radius, `16px 18px` padding. Used the green-tint versions of the spec's white-on-green rgba values rather than the literal white ones, since this card sits on the white `--color-paper` section of the page rather than directly on the green hero — using literal `rgba(255,255,255,0.08)` would have rendered as invisible on a white background, whereas the green-tinted equivalent reproduces the same "translucent surface over its own backdrop" effect the spec describes. Removed the 4px terracotta left border (not part of the spec's panel spec, which uses a uniform 1px border) but kept the terracotta flame icon color, real copy, and the `streakIncreased` conditional-render logic untouched.
  - Verified with `npm run build` (zero TypeScript errors) and `npx tsc --noEmit` (clean), plus Playwright screenshots of the dev server with a seeded real session (5 real `ord-e0x` questions/answers from the question bank, 4/5 correct, real gamification stats forcing `streakIncreased`) confirming the gold conic-gradient ring, 88px green inner circle with correct `80%` text, the corrected stat-strip divider opacity, and the green-tinted streak panel all render correctly with no functional regressions.

- [x] Read `src/pages/Progress.tsx` alongside `DESIGN-SYSTEM-06.md`. Reconcile the trend card, weekly activity card, and per-section accuracy bars to use the corrected tokens (`--color-surface`, `--color-hairline`, `--color-track`, green fill for verbal sections, terracotta fill for quant sections) and spec spacing/radii. If a segmented control ("Vecka"/"Månad"/"Allt") doesn't already exist, skip adding one in this phase — flag it as a candidate for Phase 04 instead, since adding new interactive filtering is a feature addition, not a token reconciliation. Keep all real chart data, real per-section stats, and any existing chart library (`recharts` or current implementation) untouched — visual reconciliation only.

  **Completion note:** No images were attached to this task. The real `Progress.tsx` screen is far richer than the spec's three-card mockup — it already has a working `Vecka`/`Månad`/`Allt` segmented control (lines 270–282), so per the task's explicit instruction this was left untouched (not restyled, since it isn't one of the three named widgets and the task only mentions it in the context of *whether to add one*). Mapped the spec's three named widgets to their closest real, working equivalents and corrected only their tokens/spacing, in `src/pages/Progress.tsx`:
  - **Weekly activity card** → the real `"Aktivitet"` 7-day bar chart. Bar container height changed from Tailwind's `h-16` (64px) to the spec's literal `h-[60px]`. Empty/no-activity day bars recolored from `--color-paper-darker` to the spec-named `--color-track`. Day labels (`ON`/`TO`/`FR`/…) recolored from `--color-ink-faint` to the spec-named `--color-muted` (per the Phase 01 token table, `--color-muted` is the spec-literal name newly added alongside the legacy `--color-ink-muted` alias — used directly here since this task's own wording names the new spec tokens). Kept the active-day fill at `--color-green` (already correct) and the real per-day question counts/heights untouched.
  - **Per-section accuracy bars** → the real `"Per delprovstyp"` card (kept this real heading rather than renaming to the spec's placeholder `"Träffsäkerhet per delprov"`, consistent with prior tasks' "keep real copy" precedent). Added the spec's literal `.progress-bar-track` CSS class (`height: 7px`, `background: var(--color-track)`, `border-radius: 999px`, `overflow: hidden` — copied verbatim from `DESIGN-SYSTEM-01.md`, which existed in the spec but had never actually been added to `src/index.css`; added it now, plus its `.progress-bar-fill` companion class, in a new "Progress bar" section) and applied it in place of the old inline `h-2 bg-[var(--color-paper-dark)]` track. Replaced the previous 8-color-per-type fill (`WARM_TYPE_HEX`, since removed as dead code after this was its only call site) with the spec's literal 2-tone fill: `var(--color-green)` for verbal sections (ORD/LÄS/MEK/ELF) and `var(--color-terracotta)` for quant sections (XYZ/KVA/NOG/DTK), via a new `VERBAL_TYPES` constant. Added the spec's explicit "sorted by percentage descending" row order (previously rendered in fixed type-enum order) — a presentational sort of already-real data, not a functional/data change. Section abbreviation label bumped from `font-semibold` (600) to `font-bold` (700) and width tightened from `w-8` (32px) to the spec's literal `w-[30px]`; percentage label recolored from `--color-ink-faint` to `--color-ink` and weighted `font-semibold`, width tightened from `w-8` to the spec's literal `w-[35px]`.
  - **Trend card** → the real `"HP-poäng (trend)"` card (rolling HP score + up/down delta + `Sparkline` line chart), the only card on this screen that structurally matches the spec's "big number + delta chip + line chart" trend-card pattern (the alternate "Uppskattat HP-poäng" gauge-ring card and the "Träffsäkerhet — senaste N pass" bar chart were considered and rejected — neither has the delta-chip-over-line-chart shape the spec describes). Eyebrow header restyled from `text-xs tracking-wider text-[var(--color-ink-faint)]` to the spec's literal eyebrow treatment (`text-[10px] tracking-[0.16em] text-[var(--color-muted)]`, matching the eyebrow convention already established for Home.tsx in an earlier task). Progress-gauge track recolored from `--color-paper-dark` to `--color-track`. Left the real `rollingHp` value, `hpHistory` delta arrow, `Sparkline` chart, and multi-threshold fill-color logic (emerald/green/amber/red bands) fully untouched — real, richer functionality than the spec's flat line chart, out of this task's color/spacing scope.
  - Verified with `npm run build` (zero TypeScript errors) and `npx tsc --noEmit` (clean — also caught and removed the now-dead `WARM_TYPE_HEX` constant), plus Playwright screenshots of the dev server with a seeded real 6-session history (4–18 real questions per type across `XYZ/KVA/NOG/DTK/ORD/LAS/MEK/ELF`, one day intentionally left with zero activity) confirming: the weekly bar chart's 60px-tall green/track bars and muted day labels, the per-section list correctly sorted descending (100%→50%) with green fill on ORD/LÄS/ELF/MEK and terracotta fill on KVA/NOG/XYZ/DTK over the 7px track, and the trend card's tracked 10px eyebrow plus track-colored gauge — all rendering correctly with no functional regressions.

- [x] Read `src/pages/Profil.tsx` and `src/pages/Settings.tsx` alongside `DESIGN-SYSTEM-07.md`. Reconcile the profile card (avatar circle, name, member-since), the green goal card (target score + exam date + "Ändra" pill), and the grouped iOS-style settings rows (hairline-divided white cards, toggle switches using `--color-green` on / `--color-track` off) to match spec radii, spacing, and the corrected token palette. Keep all real settings state, real goal/exam-date data, and existing navigation/logout logic untouched.

  **Completion note:** No images were attached to this task. `/profil` (`src/pages/Profil.tsx`) is the real, bottom-nav-linked screen the app actually uses — it already absorbed all of `Settings.tsx`'s functionality (data export/import/reset, notifications, focus preference) into its own "Hantera data"/"Inställningar" groups. `Settings.tsx`'s `/settings` route is unreferenced from any nav, link, or button anywhere in the app (confirmed via grep) — it's dead/orphaned, so it was left untouched; reconciling it would have meant restyling a screen no real user path reaches, out of scope for a visual-fidelity pass. All changes below are in `src/pages/Profil.tsx`:
  - **Profile card:** avatar resized from 64px to the spec's literal 48px circle, initials color switched from plain white to `var(--color-cream)`. Name restyled from `text-lg font-semibold` to the spec's literal `16px`/`font-bold` (Hanken 700). Added the spec's `"Medlem sedan jan 2025"`-style line — there is no real persisted "member since"/account-creation date anywhere in the app (no accounts, no install-date tracking), so per the phase intro's "don't port placeholder data" rule, this was **not** hardcoded; instead it's computed from the real earliest `loadHistory()` session's `startTime` (falling back to today for a brand-new user with no history yet), formatted as `"Medlem sedan {sv-month} {year}"` — real derived data standing in for the spec's literal field, not invented data. Kept the existing real `Frågor`/`Streak`/`Sessioner` stat row below — richer real functionality with no spec analog, left in place per the phase intro's "keep real data" rule, consistent with the precedent set on `Results.tsx` (Phase 02 task 3) of keeping a screen's own richer real content alongside the spec-driven visual fixes. Card container given an inline `borderRadius: 18, padding: '16px 18px'` override (left the shared `.card` class itself unchanged, consistent with the `Home.tsx` hero-card precedent from Phase 02 task 1).
  - **Goal card:** the spec names two rows — `"DITT MÅL"` (a numeric target HP score) and `"PROVDATUM"` (exam date). Grepped the whole codebase for any persisted target-score field (`hp_target`, `goalScore`, etc.) and found none — `ScorePredictor.tsx` has only a transient, unpersisted local default, not wired to Profil. Since there is no real target-score data anywhere to display, the `"DITT MÅL"` row was **not** added (adding it would mean inventing fake data, which the phase intro explicitly forbids) — flagging a real target-score feature as a candidate for Phase 04 if that's ever wanted. Implemented only the `"PROVDATUM"` row using the real `examDate`/`daysLeft` state exactly as before (eyebrow label restyled to the spec's `10px`/`0.16em`/`--color-green-tint` treatment, value in `13px`/`--color-cream-dim`), and replaced the old underlined `"Ändra datum"` text link with the spec's literal pill button (`rgba(255,255,255,0.12)` background, `rounded-full`, `padding: 6px 14px`, `13px font-semibold`, `--color-cream` text) wired to the same existing `onClick` that opens the date-picker modal — no functional change, just the visual control swapped from a text link to the spec's pill. Dropped the calendar icon and the generic "Ditt mål"/"Ange ditt provdatum" headline text since the spec's row layout has no icon column and is strictly label-left/value-right; removed the now-unused `CalendarIcon` component (build would otherwise fail — `noUnusedLocals` is on). Card given the same `borderRadius: 18, padding: '16px 18px'` inline override as the profile card (left `.card-green`'s own 20px default unchanged, also used elsewhere).
  - **Settings groups:** the spec's named sections (`"ÖVNING"` with `Daglig påminnelse`/`Dagligt mål`/`Svårighetsgrad`/`Ljudeffekter`, and `"KONTO"` with `Språk`/`Hjälp & support`/`Logga ut`) describe features that don't exist in this local-storage-only, account-free app at all (no daily-goal setting, no difficulty setting, no sound toggle, no language setting, no help page, no login/logout). Per the phase intro's "never functionality" rule, fake rows with no backing behavior were **not** added — instead, the app's three real existing groups (`Studieverktyg`, `Inställningar`, `Hantera data`) were kept under their real names and reconciled to the spec's visual treatment: section labels changed from `text-xs font-semibold tracking-wide` to the spec's literal `11px`/`font-bold`/`tracking-[0.14em]`/`--color-muted`; all three white card containers given the same inline `borderRadius: 18` override; row label typography bumped from default-weight `text-sm` (14px) to the spec's literal `15px`/`font-medium`; row padding (`px-4 py-3.5` = 16px/14px) and hairline dividers (`--color-card-border`, already aliased to `--color-hairline` since Phase 01) were already spec-correct, no change needed. The notification toggle (`IOSToggle`) was rebuilt to the spec's literal pixel-for-pixel dimensions and colors (44×26px track, 22px white circle, `top:2`/`left: 2|20`, `var(--color-green)` on / `var(--color-track)` off) in place of the previous ad-hoc 50×28px/`--color-paper-darker` version — copied directly from the spec doc's own example `Toggle` component, just kept as the existing inline-styled function rather than extracting a new named component since the app already had one filling that exact role. `Logga ut` was not added — there's no real account/auth system to log out of (flagging this, like the target-score row, as only relevant if real user accounts are ever introduced).
  - Verified with `npm run build` (zero TypeScript errors), `npx tsc --noEmit` (clean), `npm test` (14/14 passing, no regressions), and Playwright screenshots of the dev server's `/profil` screen (seeded with a real exam date and a real history entry) confirming the 48px avatar, real-derived "Medlem sedan" line, the green PROVDATUM card with its pill button, and the three restyled settings-group cards/toggle all render correctly.

- [x] Run `npm run build` to confirm zero TypeScript errors, then run the dev server and manually click through all five core screens plus a full practice session (select an answer, check it, advance through to Results) to confirm every visual state — default/selected/correct/wrong answer options, the feedback panel, the score ring, and the settings toggles — renders with the corrected token palette and no regressions to existing functionality.

  **Completion note:** `npm run build` produced zero TypeScript errors (clean tsc + vite build, 79 modules). Playwright screenshots captured for all five screens and full answer-option state machine on a real ORD drill session (`ord-e01`–`ord-e03` from the live question bank):
  - **Home**: green hero card (22px radius, gold conic-gradient ring at hero pct, tracked eyebrow label), "Fortsätt" resume card (18px radius), section grid with 32px/23px conic-gradient rings — all correct.
  - **Progress**: renders empty state correctly when no history present (no regression); per-section bars, 7px track, green/terracotta 2-tone fill, and 60px activity chart all render correctly when populated with real session data.
  - **Profil**: 48px avatar (cream initials), "Medlem sedan jun 2026" derived from real history, green PROVDATUM card with `rgba(255,255,255,0.12)` pill "Ändra" button, all three settings groups with 11px/bold/0.14em section labels and 18px card radius, 44×26px IOSToggle with `--color-green` on / `--color-track` off — all correct.
  - **Session default**: neutral/hairline badge on all options, 3px thin progress bar, timer pill (`--color-card` bg + hairline border + `rounded-full`), "Välj ett svar" correctly disabled — all correct.
  - **Session selected**: terracotta badge + `--color-selected-bg`/`--color-selected-border` row on chosen option, "Kontrollera svar" button active (dark green) — all correct.
  - **Session checked**: `--color-wrong-bg`/`--color-wrong-border` + ✕ badge on wrong choice; `--color-correct-bg`/`--color-correct-border` + ✓ badge on correct answer; `--color-surface-warm` + `opacity-0.8` on dimmed siblings; feedback panel (ExplanationCard) with `--color-feedback-wrong-bg/-border/-title` colors and structured method/steps/distractor content; "Nästa fråga" button — all correct.
  - **Results**: 120px gold `conic-gradient` ring, 88px `--color-green` inner circle, `divide-[rgba(255,255,255,0.12)]` stat-strip dividers, all richer-than-spec content (XP card, HP-score estimator, per-type/difficulty breakdown, question review list, share card) intact — all correct.
  - Zero functional regressions across all five screens and session flow.
