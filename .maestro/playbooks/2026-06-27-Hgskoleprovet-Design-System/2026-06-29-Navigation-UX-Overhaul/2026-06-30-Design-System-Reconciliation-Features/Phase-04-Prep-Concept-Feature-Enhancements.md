# Phase 04: Prep-Concept Feature Enhancements

With a single coherent design system in place across the app, this phase adds new functionality that deepens the Högskoleprovet prep concept for users. The codebase already has solid building blocks — `src/utils/analytics.ts` (`weakTypeSummary`), `src/utils/examDate.ts` (countdown + urgency), `src/utils/gamification.ts` (XP/streak), `src/utils/srs.ts` (spaced repetition), `src/utils/session.ts` (`buildSession`/history), and a basic weak-area drill entry point already inside `Practice.tsx` — so every task below should read and reuse that existing logic rather than re-implementing it from scratch. The goal is to extend real prep value (smarter practice targeting, exam-pacing awareness, visible consistency) using the now-consistent visual language from Phases 01–03, all driven by real stored data, not placeholder content.

## Tasks

- [x] Read `src/utils/examDate.ts` and `src/utils/readiness.ts` in full, then add a new `src/utils/pacing.ts` utility that combines `daysUntilExam()`, `computeReadiness()`, and recent session volume from `loadHistory()` (in `src/utils/session.ts`) to compute a recommended daily/weekly question target — e.g. enough additional practice volume to close the gap between current readiness and a reasonable target before the exam date, with sane fallbacks when no exam date or insufficient history exists. Export a typed result (`{ dailyTarget: number; weeklyTarget: number; onTrack: boolean; message: string }`) so it can be rendered directly.
  <!-- Done: created src/utils/pacing.ts. computePacing() bases the daily target on urgency (days left) and readiness gap vs. TARGET_READINESS=70, scales via gapMultiplier, measures recent 7-day volume from loadHistory(), derives onTrack (actual avg ≥ 80% of target), and returns a Swedish message. Returns sane fallbacks when no exam date is set or the exam has passed. Zero TS errors. -->

- [x] Surface the new pacing utility as a dismissible nudge card on `src/pages/Home.tsx`, styled with the Phase 01 tokens (white `.card` surface, terracotta accent if behind pace, green accent if on track) and placed below the hero score card. The card should show the daily/weekly target and the on-track/behind message from `pacing.ts`. Only render it when an exam date is set (reuse the existing `getExamDate()` check already used elsewhere in the app) — do not duplicate the exam-date-picker UI, link to wherever it's already editable (Profil's goal card from Phase 02, or Settings).
  <!-- Done: added pacing nudge card below the hero score card in Home.tsx. Imports computePacing()/PacingResult from pacing.ts. Card conditionally renders only when examDate is set; uses terracotta left-border + label when onTrack=false, green when onTrack=true. Dismiss button writes pacing_dismissed_YYYY-MM-DD to localStorage so the card auto-reappears the next day. Includes a link to /profil for editing exam date. Zero TS errors; build clean. -->

- [x] Read `src/components/InstallBanner.tsx` and `src/utils/notifications.ts` to understand the existing notification-permission and due-question-reminder pattern, then extend `notifications.ts` with one additional notification condition driven by `pacing.ts`: if the user is meaningfully behind pace and hasn't practiced today, allow a pacing reminder to fire (respecting the same enabled/permission/last-shown guards already implemented for the due-question reminder — do not bypass or duplicate that gating logic).
  <!-- Done: added maybeShowPacingNotification(lastSessionTimestamp) to notifications.ts. Imports computePacing() from pacing.ts. Guards: notificationsEnabled() check (shared), pacing.onTrack early-return, "practiced today" check (lastSessionTimestamp >= midnight), 20h PACING_LAST_SHOWN_KEY cooldown (own key, doesn't share with due-question cooldown), and the existing 3h active-study guard. Fires with tag 'hp-pacing-reminder' using pacing.message as body. Called from App.tsx in the same mount useEffect as maybeShowDueNotification. Zero TS errors, build clean. -->

- [ ] Read the existing weak-area drill logic in `src/pages/Practice.tsx` (the `adaptiveIds`/`weakTags`/`weakTypeSummary` block) and `src/utils/analytics.ts` in full. Extract the question-selection logic into a reusable exported function in `analytics.ts` (e.g. `buildWeakAreaSession(limit: number): string[]`) if it isn't already cleanly separable, then promote weak-area drilling from the buried "advanced" panel to a first-class, clearly visible entry point: a dedicated card on `Home.tsx` (using the Phase 01/02 card styling) that shows the current weakest 1–3 question types by name and accuracy percentage, with a single tap starting a `buildSession` drill exactly as the existing Practice.tsx flow already does. Do not change the underlying drill-building logic, only its visibility and entry point.

- [ ] Read `src/data/questions.ts` to confirm the `difficulty` field (`'easy' | 'medium' | ...`) on each question, then add real-time adaptive difficulty within an active practice session in `src/pages/Session.tsx`: track a rolling correct/incorrect streak for the current session in component state, and when selecting among remaining same-type questions for the *next* question, bias toward a harder difficulty tier after 2+ consecutive correct answers and toward an easier tier after 2+ consecutive incorrect answers (falling back to the existing question order when there's no eligible question at the target difficulty). This only reorders/selects from the already-built session question pool — it must not change `buildSession`'s up-front selection behavior used elsewhere.

- [ ] Read `src/utils/session.ts` (`loadHistory`) and `src/utils/gamification.ts` (streak data), then add a practice-consistency calendar (heatmap-style, similar to a GitHub contributions graph) to `src/pages/Progress.tsx` below the existing per-section accuracy card from Phase 02: one cell per day for the last ~12 weeks, shaded by question volume that day (derived from real session history dates), using `--color-green` at increasing opacity for activity levels and `--color-track` for inactive days. Add a tooltip or tap-to-reveal showing the exact date and question count.

- [ ] Run `npm run build` to confirm zero TypeScript errors. Then in the dev server: set an exam date and confirm the pacing nudge appears on Home with a sensible target; complete a practice session and confirm the weak-area card reflects real accuracy data; complete several questions of the same type in a row correctly and confirm later questions skew toward higher difficulty (and the reverse for incorrect streaks); confirm the consistency calendar on Statistik reflects real practice history with no fabricated data.
