# Phase 03: App-Wide Propagation & Consistency Sweep

The app is much bigger than the five core screens — it has subject hub pages (Verbal, Quant), a dozen theory guides, an SRS review queue, bookmarks, an Ord builder, a score predictor, the exam-select/start flow, and shared components like `ExplanationCard`, `AchievementToast`, `FormulaDrawer`, and `ChartView`. Most of these were already converted away from the old dark theme in earlier work, but they may still reference pre-Phase-01 token values, inconsistent card patterns, or component states that don't yet use the richer answer-state/feedback palette added in Phase 01. This phase sweeps every remaining page and shared component so the entire app — not just the five mockup screens — feels like one coherent system, and removes any duplicated or inconsistent card/button/badge styling along the way.

## Tasks

- [x] Before writing any new styling, grep the codebase for existing reusable patterns to avoid duplicating them: search for `.card`, `card-green`, `PageHeader`, `streak-pill`, `answer-option`, `btn-primary`, `btn-ghost` usages in `src/components` and `src/pages` to build a inventory of what's already shared vs. what's been re-implemented inline page-by-page. Reuse the existing `PageHeader.tsx` component (`src/components/PageHeader.tsx`) everywhere a secondary page needs a back-navigation header — do not create a second header component.

  <!-- COMPLETED 2026-06-30: PageHeader is already universally used across all 20+ secondary pages — no custom headers to replace. btn-primary/btn-ghost are used in most places; OrdBuilder, Progress, and Bookmarks have inline bg-[var(--color-green)] buttons that bypass .btn-primary. The main issues are (a) blue/violet Tailwind classes in Practice, ExamStart, ExamSelect, Results, ScorePredictor, DtkGuide for section accent colors, (b) hardcoded hex values in Theory.tsx, ChartView.tsx, and Progress.tsx heatmap, (c) text-white on green backgrounds that should be var(--color-cream). Full inventory saved to Working/phase03-inventory.md. -->

- [x] Reconcile the hub pages — `src/pages/VerbalHub.tsx` and `src/pages/QuantHub.tsx` — against the corrected tokens: paper background, white `.card`-style section tiles with hairline borders, green accent for verbal sections and terracotta accent for quant sections, `PageHeader` for navigation. Keep all real routing and section data.

  <!-- COMPLETED 2026-06-30: Replaced all off-palette Tailwind color classes in both files. VerbalHub: rose/pink/fuchsia/purple → green token family (--color-green, --color-green-muted, --color-green-light, --color-card-border). QuantHub: violet/blue/emerald/amber → terracotta token family (--color-terracotta, --color-terracotta-muted, --color-card-border). Hub-level badge pills now use green (verbal) / terracotta (quant) border/text tokens. SRS "due" repeat badges routed through --color-gold/--color-gold-deep/--color-gold-muted/--color-hairline. Accuracy bar semantic colors (good/medium/poor) use --color-green-light / --color-gold / --color-terracotta. PageHeader already in use; no routing or section data changed. Zero TypeScript errors. -->

- [ ] Reconcile all theory guide pages in one pass — `OrdGuide.tsx`, `MekGuide.tsx`, `LasGuide.tsx`, `ElfGuide.tsx`, `KvaGuide.tsx`, `DtkGuide.tsx`, `NogGuide.tsx`, `XyzGuide.tsx`, `MathGuide.tsx`, `LiggandeStolenGuide.tsx`, and the `Theory.tsx` index — to the corrected token set: paper background, white surface cards with hairline borders for content blocks, `--color-ink` headings, `--color-ink-soft`/`--color-muted` body and secondary text, `PageHeader` for back navigation. Grep first for any remaining hardcoded hex values or `slate`/`blue`/`violet` Tailwind classes across these files and replace them with token references.

- [ ] Reconcile the utility and exam-flow pages — `Bookmarks.tsx`, `SrsQueue.tsx`, `OrdBuilder.tsx`, `ScorePredictor.tsx`, `ExamSelect.tsx`, `ExamStart.tsx`, `Practice.tsx` — to use the corrected card/surface/hairline tokens, the shared `PageHeader`, and consistent button styling (`.btn-primary`/`.btn-ghost`). For `Practice.tsx` specifically, reuse the same progress-ring pattern already reconciled on the Home screen in Phase 02 rather than re-implementing it — check `Home.tsx` for the existing ring implementation first.

- [ ] Reconcile shared components — `ExplanationCard.tsx` (use the new `--color-feedback-correct-*` / `--color-feedback-wrong-*` tokens from Phase 01), `AchievementToast.tsx` (green background, cream text), `FormulaDrawer.tsx` (paper background, white formula tiles with hairline borders), `Onboarding.tsx` (paper background, `.btn-primary` CTAs), `InstallBanner.tsx`, and `ChartView.tsx` (chart series colors pulled from `--color-green`/`--color-terracotta`/`--color-gold` tokens instead of hardcoded hex). Also check `Math.tsx`/`MathText.tsx` to confirm KaTeX-rendered formulas use dark ink text and remain legible on the paper background.

- [ ] Run a final consistency sweep across all of `src/`: grep for any remaining raw hex colors in `.tsx` files (pattern like `#[0-9A-Fa-f]{6}` inside `style={{` or `className`) that aren't already routed through a CSS variable, and for any leftover `text-white` Tailwind usages that should instead be `var(--color-cream)` (on green backgrounds) or `var(--color-ink)` (on paper backgrounds). Fix every match found.

- [ ] Run `npm run build` to confirm zero TypeScript errors, then manually click through every remaining page (both hubs, at least 3 theory guides, Bookmarks, SRS queue, Ord builder, Score predictor, Exam select/start) in the dev server to confirm a single coherent warm paper aesthetic with no leftover dark-theme fragments anywhere in the app.
