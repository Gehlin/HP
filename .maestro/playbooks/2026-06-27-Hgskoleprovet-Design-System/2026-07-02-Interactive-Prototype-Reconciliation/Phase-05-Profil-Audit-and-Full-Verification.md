# Phase 05: Profil Audit & Full-App Verification Pass

Final phase: audit `src/pages/Profil.tsx` against the prototype's Profil screen (`Reference/Hogskoleprovet-Prototyp.dc.html` lines 452–517), then do a full click-through verification of every screen touched across Phases 01–04 together, the way the earlier reconciliation round closed with a build+UI verification pass (see `2026-06-29-Navigation-UX-Overhaul/.../Phase-04-Prep-Concept-Feature-Enhancements.md` task 7 for the pattern to follow).

## Tasks

- [x] Read `src/pages/Profil.tsx` in full and compare against the prototype: a profile card (avatar = initials in a green circle, name, member-since), a green "goal card" showing `DITT MÅL {goal}` and `PROVDATUM {date} (om N dagar)` side by side with an "Ändra" pill that re-enters onboarding at the goal step, a tappable "Sparade frågor" row with a live count linking to the Saved/Bookmarks screen, then two grouped iOS-style settings sections — **ÖVNING** (Daglig påminnelse toggle, Dagligt mål, Svårighetsgrad, Ljudeffekter toggle) and **KONTO** (Språk, Hjälp & support, Logga ut in terracotta). This app's Profil already has more rows than the prototype (Theory, Bookmarks, SRS queue, Exam simulator, Word builder, Score predictor under "STUDIEVERKTYG") — keep every existing row, just reconcile the visual treatment (grouped white card with hairline dividers between rows, consistent row height/padding/typography) and confirm the goal card and avatar match the prototype's exact styling.

  **Delta list (pre-fix state of `Profil.tsx`, 594 lines):**
  - **Profile card:** shell exact (white radius-18, 16px padding, 56px `#224A3A` circle, name 700/18, member-since 500/13 `#8B8478`), but three drifts: initials were `name.slice(0, 2)` where the prototype's `initials()` takes the first letters of the first *two words* ("Elin Andersson" → "EA", not "EL"); member-since used abbreviated months ("jul 2026") where the prototype writes the full month ("Medlem sedan februari 2026"); card gap 16px vs the prototype's 14px. The stats row (Frågor/Streak/Sessioner) is an app extra — kept.
  - **Goal card:** the biggest gap — the app's green card showed **only PROVDATUM + Ändra**; the prototype's entire left half (`DITT MÅL` eyebrow + goal in Newsreader 500/30 `#FBF7EE`) and the 1×46px `rgba(255,255,255,.14)` divider were missing, even though `hp_goal` is written by onboarding and read by Home. PROVDATUM block itself was exact (600/10 `.12em` `#9FB7A8` eyebrow, 700/17 value at 11px, 500/12 `#A9C0B2` "om N dagar" at 5px), as was the Ändra pill (8/13px, `rgba(255,255,255,.25)` border, 600/12) — but it opened a **local date-picker modal over `KNOWN_HP_DATES`** (`2026-04-04`/`2026-10-10`/`2027-04-10` — a list that contradicts the `EXAM_OPTIONS` onboarding now uses, the Phase-01 flagged item) instead of the prototype's `goalEdit` (re-enter onboarding at step 2).
  - **Sparade frågor row:** absent as the prototype's standalone tappable card (38px `#F3E0D5` radius-11 tile with the filled terracotta bookmark, 600/15 label, 700/14 `#8B8478` count, 8×14 chevron); the same destination existed only as a "Bokmärken" row buried inside STUDIEVERKTYG with a green count pill.
  - **Settings rows:** padding (16/14px) and label size (15px) right, but label weight 500 vs the prototype's 600; dividers were full-width `#EAE3D6` borders vs the prototype's 16px-inset `#F0EADD` hairline; chevron was a 16×16 ink-faint icon vs the prototype's 8×14 `#C3BBAC` stroke; row gap 12 vs 13px; value text (Fokusprioritet) was a green pill chip vs the prototype's plain 500/14 `#8B8478`; the toggle was 44×26 with `--color-track` off-state vs the prototype's 46×28 with `#E0D8C8`.
  - **Prototype rows this app has no backing feature for:** ÖVNING's Dagligt mål ("20 frågor"), Svårighetsgrad ("Adaptiv"), Ljudeffekter, and KONTO's Språk ("Svenska"), Hjälp & support, Logga ut. The app has no sound engine, no user-facing daily-goal or difficulty *setting* (adaptive difficulty is always-on in Session.tsx; the daily target is computed dynamically), no i18n and no accounts — adding those rows would be dead UI, so they were **not fabricated** (see task 4 flag). The app's real KONTO rows (Exportera/Importera/Återställ all data) stay; its "Återställ all data" gets the prototype's destructive-label treatment (`#BF4A2E`, the Logga ut convention). Leading row icons (an app convention across all three groups, not in the prototype) were kept — with 11 rows across three groups they carry real scanning value.

- [x] Fix any drift found in the previous task. If "Ändra" needs to jump back into the onboarding wizard at the goal step (per the prototype's `goalEdit` handler), confirm `src/components/Onboarding.tsx` (reconciled in Phase 01) supports being entered at an arbitrary step with a return-to-Profil behavior; wire it up if not already present.

  **Completion note:** all task-1 drift fixed in `Profil.tsx` (594 → 547 lines): prototype `initials()` logic (`initialsFor()`), full Swedish month names in member-since, 14px card gap; goal card rebuilt as the prototype's four-part row — DITT MÅL block (Newsreader 500/30, value from `hp_goal`, default `1,40`) · 1×46px divider · PROVDATUM block · Ändra pill; standalone "Sparade frågor" card added between the goal card and STUDIEVERKTYG with the prototype's exact anatomy and the live `getBookmarks().length` count (the duplicate "Bokmärken" row was removed from STUDIEVERKTYG — same feature, same `/bookmarks` destination, now in the prototype's position under the prototype's name, so no row was lost); `SettingsRow` reconciled (600-weight labels, 13px gap, inset `#F0EADD` hairline via an `after:` pseudo-element, 8×14 `#C3BBAC` chevron, plain `RowValue` text replacing the pill chip, count badges as 700/14 `#8B8478`); `IOSToggle` now 46×28/`#E0D8C8`; "Återställ all data" label `var(--color-wrong-badge)` (`#BF4A2E`) with no chevron, and the reset modal's off-palette `bg-red-600` confirm button moved to `var(--color-terracotta)`. **Ändra wiring:** `Onboarding.tsx` (199 → 202 lines) did *not* support arbitrary-step entry — added an optional `initialStep` prop (clamped 0–3) and made `goal`/`examISO` state preload from `hp_goal`/`hp_exam_date` (name already preloaded), so re-entry shows the user's current values exactly like the prototype's persistent state; `App.tsx`'s first-launch mount is unchanged (`initialStep` defaults to 0). Profil now renders `{showGoalEdit && <Onboarding initialStep={2} onClose={closeGoalEdit} />}` — the overlay *is* the return-to-Profil behavior (Profil stays mounted underneath; the prototype's `onboardReturn: 'profile'` equivalent), and `closeGoalEdit` re-reads `hp_goal`/exam date into state so the card updates immediately. **Exam-date reconciliation (Phase-01 flagged item):** `KNOWN_HP_DATES` deleted from `src/utils/examDate.ts` and replaced by the prototype's `EXAM_OPTIONS` (`2026-10-25`/`2027-04-10`/`2027-10-24`, `{iso,label,sub}`), which `Onboarding.tsx` now imports instead of its own local copy — one canonical list. Profil's separate date-picker modal (the only `KNOWN_HP_DATES` consumer) was **deleted outright**: Ändra now runs through onboarding steps 2→3, which covers both goal *and* date exactly like the prototype's `goalEdit`, so the app can no longer present two contradictory date lists. (Side effect, accepted: the modal's "Inget datum" clear option is gone — the prototype has no date-clearing affordance, and the wizard always confirms a real date.)

- [x] Do a full click-through of the app with Playwright covering: onboarding (all 4 steps) → Hem → start a practice session covering at least one question of each layout type available in the current question bank → finish → Resultat → Granska dina svar → back → Till hem → Statistik → Profil → Sparade frågor (via the Profil row) → back. Screenshot every screen visited. Save all screenshots to this phase's `Working/` folder.

  **Completion note:** one throwaway Playwright script (repo's own `playwright` devDependency, 402×874 viewport, same approach as Phases 01–04) runs the entire flow in a single browser session on a cleared profile: all four onboarding steps as "Elin Andersson" → Hem → a seeded in-progress 4-question mixed-layout session (`ord-e01` standard, `las-p01-q01`/`q02` linked passage, `dtk-e1` diagram — the same ids as Phase 02's pass, entered *genuinely* by tapping Hem's "FORTSÄTT DÄR DU SLUTADE" card) → select/Kontrollera svar on every question, bookmarking the DTK one → Se resultat → Resultat → Granska dina svar → Tillbaka till resultat → Till hem → Statistik tab → Profil tab → **Ändra** (onboarding wizard opens at the goal step with `1,40` preselected; changed to `1,60`, stepped through the date card, Sätt igång) → Profil showing the updated goal → Sparade frågor via the Profil row (1 saved DTK question) → back to Profil. Screenshots continue the shared numbering from Phase 04's 24: `25-verify-onboarding-step0` … `28-verify-onboarding-step3-date`, `29-verify-hem`, `30-verify-hem-continue-card`, `31/32-verify-session-ord-standard/-checked`, `33-verify-session-las-passage`, `34-verify-session-dtk-bookmarked`, `35-verify-resultat`, `36-verify-granska`, `37-verify-statistik`, `38-verify-profil`, `39-verify-goal-edit-step2`, `40-verify-profil-after-goal-edit`, `41-verify-sparade-fragor`, `42-verify-profil-back-from-sparade`, plus `43-verify-profil-bottom` (ÖVNING/KONTO groups scrolled into view).

- [x] Review every screenshot from the previous task side-by-side against the corresponding section of `Reference/README.md` and the prototype markup. Log any remaining discrepancy found (do not silently fix scope creep — if it's a small styling fix, fix it directly and note it; if it's a larger behavioral question, flag it back to the user).

  **Two real discrepancies found by the click-through and fixed:** (1) **"Se resultat" never reached Resultat** — on the last question it called `requestFinish()`, opening the finish-confirmation modal even when every question was already answered and checked, where the prototype goes straight to results. Fixed in `Session.tsx` (new `finishOrConfirm`): a fully-answered set finishes directly; the modal still guards sets with unanswered questions (the Phase-02 rationale for keeping it), for both the standard footer button and the study-mode Svårt/Ok/Enkelt buttons. (2) **Scroll position leaked across routes** — the app had no scroll reset on SPA navigation, so Resultat/Granska opened mid-scroll after a scrolled session (first captures of shots 35/36 proved it), while every prototype screen renders from its top. Fixed with a `ScrollToTop` component in `App.tsx` (scrolls to 0,0 on pathname change); re-ran the full click-through after both fixes — all 19 shots now match. **Verified correct against the reference:** onboarding steps identical to Phase 01's captures; Hem greeting/hero/grid (fresh-profile empty states per the Phase-01 decisions); all three question layouts and check states; Resultat's achievement toast + green summary + Detaljerad analys; Granska's Alla(4)/Fel(2)/Markerade chips and Rätt/Fel cards; Statistik's segmented control, per-delprov 2-color bars, and today-highlighted week bar; Profil per tasks 1–2 including the goal-edit round trip (`1,40` → `1,60` visible in shot 40) and the Sparade-frågor round trip. **Flagged, not fixed (behavioral, needs a product call):** the prototype's ÖVNING rows *Dagligt mål / Svårighetsgrad / Ljudeffekter* and KONTO rows *Språk / Hjälp & support / Logga ut* have no backing features in this app (no sound engine, no configurable goal/difficulty, no i18n/accounts) — rendering them would be dead controls, so Profil keeps its real rows instead; adding any of those features is new scope beyond reconciliation. Also noted as deliberate app-over-prototype differences (consistent with Phases 01–04 decisions): TopNav/BottomNav chrome on Profil/Sparade frågor, leading row icons, the stats row in the profile card, and the finish-confirmation modal for *unfinished* sets.

- [x] Run `npm run build` one final time and confirm zero TypeScript errors. Run through this entire playbook's phase files and confirm every task checkbox is checked with a completion note — if any is not, either finish it or explicitly note why it was intentionally skipped.

  **Completion note:** final `npm run build` (`tsc && vite build`) exits 0 with zero TypeScript errors (only the pre-existing chunk-size warning), re-run after every change this phase. Checkbox sweep across the playbook: Phase 01 — 5/5 checked, Phase 02 — 6/6, Phase 03 — 5/5, Phase 04 — 4/4, Phase 05 — 5/5 (this file), zero unchecked boxes, every task carrying a completion note. Nothing was skipped. Files touched this phase: `src/pages/Profil.tsx`, `src/components/Onboarding.tsx`, `src/utils/examDate.ts`, `src/pages/Session.tsx`, `src/App.tsx`. The playbook is complete: the app's Onboarding, Hem, practice engine (all three layouts), Resultat/Granska, Statistik, Sparade frågor, and Profil now match the interactive prototype's markup and behavior exactly where they overlap, with every pre-existing app feature beyond the prototype preserved and restyled into the same design language.
