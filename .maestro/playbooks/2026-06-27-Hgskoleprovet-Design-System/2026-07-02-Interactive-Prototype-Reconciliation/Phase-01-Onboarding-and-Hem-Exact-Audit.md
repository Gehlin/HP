# Phase 01: Onboarding & Hem Exact Audit

This repo has already been through two rounds of design-system reconciliation against a *prose* description of this spec (`.maestro/playbooks/2026-06-27-Design-System-Implementation/DESIGN-SYSTEM-01.md`–`08.md`). We now have something better: an actual working interactive prototype (`Reference/Hogskoleprovet-Prototyp.dc.html`, source-of-truth markup starting at the `<!-- ═══════════ ONBOARDING ═══════════ -->` comment, state logic in the `<script type="text/x-dc" data-dc-script>` block at the bottom) plus its handoff doc (`Reference/README.md`). Colors already match exactly (confirmed: `#224A3A`, `#BF5A33`, `#F1ECE3`, `#E4C66A` are all live in `src/index.css`), so this phase is **not** a rebuild — it's a line-by-line audit of `src/components/Onboarding.tsx` and `src/pages/Home.tsx` against the prototype's literal markup, catching drift in copy, spacing, states, and interaction sequencing that the earlier prose-based rounds could only approximate. Read `Reference/README.md` in full before starting — it has the full design-token table and per-screen behavior notes referenced throughout this phase.

## Tasks

- [x] Read `src/components/Onboarding.tsx` in full and compare it against the prototype's onboarding block (`Reference/Hogskoleprovet-Prototyp.dc.html` lines 24–92, plus the onboarding-related state/handlers in the script block: `onbNext`, `onbBack`, `finishOnboard`, `onbDots`, `onbCta`, `goalChips`, `examCards`, `EXAM_OPTIONS()`). Write a short delta list (in this file, under this task, as a completion note) covering: is it a 4-step wizard (Welcome → Name → Goal → Exam date) with progress dots that elongate the active dot? Does step 2 use the exact goal chips `1,20 / 1,40 / 1,60 / 1,80 / 2,00`? Does step 3 use radio cards with the exact live "om N dagar" countdown per `EXAM_OPTIONS()` (`2026-10-25`, `2027-04-10`, `2027-10-24`)? Does the CTA label change per step (`Kom igång` / `Fortsätt` / `Fortsätt` / `Sätt igång`)? Is there a back button from step 1 onward? Does completion write to a single localStorage key equivalent to `hp_onboard_v1` (`{ name, goal, examISO }`) — note the actual key name(s) this app already uses (survey found `hp_onboarding_done`, `hp_exam_date`, `hp_user_name`, `hp_goal` as separate keys; decide whether to consolidate or leave split, favoring the smallest safe diff).

  **Delta list (pre-fix state of `Onboarding.tsx`, 189 lines):**
  - **4-step wizard?** No. It was a 5-slide *centered modal overlay* (bottom sheet on mobile), not a full-screen flow, with entirely different content: Welcome ("Välkommen till HP Träning" + section pills) → "Åtta delproven" info → focus-preference picker (Kvantitativt/Verbalt/Hela provet) → "Smart repetition" → "Följ dina framsteg". No Name, Goal, or Exam-date steps existed at all.
  - **Progress dots:** 5 dots, 4px tall, active elongates to 24px green, past dots green/40 — right *idea*, wrong count (5 vs 4), geometry (4px vs 7px tall, 24px vs 22px active), and fill rule (prototype fills `#224A3A` for `i <= step`, inactive `#D8CFBE`).
  - **Goal chips `1,20`–`2,00`:** absent.
  - **Exam-date radio cards with live "om N dagar":** absent. (Note: `examDate.ts` `KNOWN_HP_DATES` has `2026-04-04` / `2026-10-10` / `2027-04-10` — used by Profil's own picker — which do *not* match the prototype's `EXAM_OPTIONS()` `2026-10-25` / `2027-04-10` / `2027-10-24`. Onboarding now uses the prototype's exact three; `KNOWN_HP_DATES` is Profil territory, left for Phase 05.)
  - **CTA labels:** "Nästa →" / "Fortsätt med {fokus} →" / "Kom igång →" (last slide) — wrong strings and sequence vs `Kom igång / Fortsätt / Fortsätt / Sätt igång`; CTA could also be disabled (focus slide), which the prototype never does.
  - **Back button from step 1 onward:** none — a "Hoppa över" skip link instead, which the prototype doesn't have.
  - **localStorage:** wrote only `hp_onboarding_done='1'` + `hp_focus_preference`. Crucially, `hp_user_name` and `hp_goal` are *read* by `Home.tsx` and `Profil.tsx` but were written **nowhere** in the codebase — a real user could never see their name in the greeting. **Decision: keep the split keys** (`hp_onboarding_done`, `hp_user_name`, `hp_goal`, `hp_exam_date`) rather than consolidating into one `hp_onboard_v1` blob — all four already have live consumers (`isOnboardingDone()` in App.tsx, Home greeting/goal, `examDate.ts` util feeding Home hero + Profil), so writing them from onboarding completion is the smallest safe diff; consolidation would touch 4+ files for zero visual gain.
  - **Extra app feature not in prototype:** the focus-preference slide. Removed from onboarding (the prototype's 4 steps are exact), but the feature itself is preserved — focus is still fully settable in both Settings ("Fokus") and Profil, and `hp_focus_preference` simply stays unset until chosen there (same as tapping "Hoppa över" before).

- [x] Fix every gap found in the previous task's delta list, matching the prototype's copy, spacing (62px top padding, 24px side padding on the onboarding screen; 28px gap before the CTA), and radius values (14–18px) exactly. Keep the existing component structure and styling approach (Tailwind + CSS variables) — do not introduce inline pixel styles to mimic the prototype's raw `style=` attributes, translate them to the app's existing conventions instead.

  **Completion note:** Rewrote `src/components/Onboarding.tsx` (189 → 205 lines) as the prototype's full-screen 4-step wizard, keeping the same public surface (`isOnboardingDone`/`markOnboardingDone` exports and the `{ onClose }` prop that `App.tsx` mounts against — App.tsx unchanged). All copy is verbatim from the prototype markup: step 0 "H" tile (62px, radius 18) + "Plugga smartare inför provet." (Newsreader 38/1.08, −0.015em) + the "Öva på alla åtta delprov…" paragraph; step 1 "Vad ska vi kalla dig?" + "Ditt namn visas på startsidan och i dina resultat." + 58px-tall radius-16 input (border 1.5px `#E0D8C8`, placeholder "Ditt namn"); step 2 "Vilket resultat siktar du på?" + "Snittet för många utbildningar ligger runt 1,30–1,50…" + the five chips `1,20/1,40/1,60/1,80/2,00` (13×21px padding, radius 14, selected = solid green/cream); step 3 "När skriver du provet?" + "Vi räknar ner dagarna åt dig på startsidan." + three radio cards from the prototype's exact `EXAM_OPTIONS` with live `"{sub} · om {N} dagar"` countdown (`Math.max(0, Math.ceil(…/86400000))`, same formula) and the 22px radio that fills via a 7px green border when selected. Spacing/radius translated to Tailwind arbitrary values + existing tokens (`pt-[62px] px-6 pb-9`, dots `h-[7px]`/active `22px` with 250ms transition, `mt-7` = 28px before the name input and goal chips, `mt-[26px]` before exam cards, CTA `h-14 rounded-2xl` = 56px/16px green with `Kom igång/Fortsätt/Fortsätt/Sätt igång`); back button (34px, radius 11, `#6f6859` chevron) renders from step 1 on, exactly like `onbCanBack`. Colors route through existing CSS variables where a token matches exactly (`--color-green`, `--color-cream`, `--color-ink`, `--color-ink-muted`, `--color-ink-faint` = `#A89F90`, `--color-ink-soft` = `#33302A`, `--color-card-border` = `#EAE3D6`); the four prototype-only hexes with no token (`#E0D8C8` input border, `#E5DDCE` chip border, `#D8CFBE` inactive dot/radio, `#EEF3EF` selected card bg) are used literally rather than inventing unauthorized tokens. Completion writes `hp_user_name` (`name.trim() || 'Elin'`, the prototype's own fallback), `hp_goal` (default `1,40`), `hp_exam_date` via the existing `setExamDate()` util (default `2026-10-25`), and `hp_onboarding_done` — per the task-1 split-keys decision. Verified with `npm run build` — zero TypeScript errors.

- [x] Read `src/pages/Home.tsx` in full and compare against the prototype's Home block (lines 94–173) and its `renderVals()` fields (`greet`, `todayLabel`, `gridCells`, hero card values). Check specifically: greeting format is `"{God morgon/dag/kväll}, {förnamn}"` with the time-of-day cutoffs at 10:00 and 18:00; the streak pill uses the exact terracotta soft styling (bg `#F3E0D5`, border `#ECCFBE`) with a small rotated-square glyph; the hero score card has a `▲ delta` chip, the gold gradient progress bar (`linear-gradient(90deg,#C9A24A,#E4C66A)`), and the footer row "Mål X" / "Provet om N dagar · DD mmm"; the continue card shows the correct in-progress section abbreviation/name/count; the section grid is a 2-column layout with VERBAL/KVANTITATIV column headers and 8 cards in the exact order ORD, XYZ, LÄS, KVA, MEK, NOG, ELF, DTK (left column verbal top-to-bottom, right column quant top-to-bottom) each with a conic-gradient ring. Note every drift found as a completion note.

  **Drift found (pre-fix state of `Home.tsx`, 272 lines):**
  1. **Greeting cutoffs:** `hour < 12` / `hour < 17` instead of the prototype's `< 10` / `< 18` (`greetPrefix()`).
  2. **Greeting name:** used the full stored `hp_user_name` string; prototype's `firstName()` takes the first whitespace-separated word only.
  3. **`todayLabel`:** raw `toLocaleDateString('sv-SE', …)` output, which is lowercase ("onsdag 2 juli"); prototype capitalizes the first letter ("Onsdag 2 juli").
  4. **Hero `▲ delta` chip:** missing entirely (label row had only "DITT NORMERADE RESULTAT").
  5. **Hero score value — real bug:** displayed `computeReadiness().score`, which is a **0–100 composite**, formatted as `"{score.toFixed(2)} / 2,0"` with bar width `score/2.0*100` capped at 100 — e.g. readiness 45 rendered as "45,00 / 2,0" with a 100% bar. The prototype's field is a normerat 0–2,0 estimate.
  6. **Top padding:** 56px vs the prototype's 58px.
  7. **Card order:** the weak-area drill card sat *between* the hero and the continue card, breaking the prototype's hero → continue rhythm.
  8. **Continue-card tile:** ORD tile text was `fontSize: 14`; prototype is `font:800 15px`.
  9. **Section-ring geometry:** 38px ring / 27px inner / 10px ink-colored number on track `#E6DFD4`, and `conic-gradient(c ${pct}%, …)` missing the `0` start stop; prototype `gridCells` is 32px / 23px / 8.5px with the *number colored by the section accent* (green/terracotta) on track `#E7E0D3` via `conic-gradient(accent 0 pct%, #E7E0D3 0)`.

  **Already exact (no change):** streak pill (bg `#F3E0D5`, border `#ECCFBE`, 9px rotated square `#BF5A33`, number 700/13 `#A84A26`); hero card shell (green `#224A3A`, radius 22, padding `18px 20px 17px`, Newsreader 50 score, gold `linear-gradient(90deg,#C9A24A,#E4C66A)` bar, footer "Mål X" / "Provet om N dagar · DD mmm", tap → Statistik/`/progress`); continue card layout (46px tile `#E7EDE7`, "FORTSÄTT DÄR DU SLUTADE" eyebrow, 40px green play circle with the same polygon SVG); VERBAL/KVANTITATIV headers (700/12, `#224A3A`/`#BF5A33`, margin `20px 2px 11px`); grid order ORD, XYZ, LÄS, KVA, MEK, NOG, ELF, DTK with identical names.

  **App-specific behavior noted and deliberately kept (real data > prototype's static mocks):** ring percentages computed from session history (prototype hardcodes 62/41/54/…); continue card only renders when an unfinished session actually exists (prototype always shows a static ORD card); hero footer falls back to an "Ange provdatum →" link when no exam date is set. Also: the task-4 mention of a "pacing nudge card" — pacing was implemented as a *notification* (`maybeShowPacingNotification` in `App.tsx`/`notifications.ts`), not a Home card, so only the weak-area card needed placement.

- [x] Fix every gap found in the Home audit. Preserve any extra Home.tsx content that isn't in the prototype (the pacing nudge card and weak-area drill card added in the earlier features phase) — place them so they don't break the prototype's card order/rhythm (hero → continue → [pacing/weak-area cards, if kept] → VERBAL/KVANTITATIV grid), and confirm with the user only if their placement relative to the new exact-match cards is ambiguous.

  **Completion note:** All nine drift items from the task-3 list fixed in `src/pages/Home.tsx`: greeting cutoffs now 10:00/18:00; greeting uses the first name only (`userName.trim().split(/\s+/)[0]`); `todayLabel` first-letter capitalized; top padding 56 → 58px (safe-area `max()` wrapper kept); continue tile 14 → 15px; ring 32px/23px/8.5px with the accent-colored number, track `#E7E0D3`, and the gradient rewritten as `conic-gradient(ringColor 0 pct%, #E7E0D3 0)`. The hero score bug (item 5) fixed by replacing `computeReadiness()` (0–100 composite, wrong scale) with `estimateSectionedScore()` from `src/utils/hpScore.ts` over the same per-type accuracy map the grid already builds — the hero now shows the real normerat estimate (1,00–2,00) matching the "/ 2,0" scale and bar; when no history exists it shows "–" with an empty bar (the app has no seed data, unlike the prototype's hardcoded 1,15). The `▲ delta` chip (item 4) added with the prototype's exact styling (`rgba(255,255,255,.12)` pill, 700/11, `#D6E4D9`) — its value comes from `hpScoreHistory()` in `src/utils/analytics.ts` (last minus oldest tracked session, `▲`/`▼` + comma-formatted absolute value, e.g. "▲ 0,12"), rendered only when ≥ 2 sessions of history exist since the app has no mock baseline. **Placement (item 7):** the weak-area drill card moved to *after* the continue card — the task's own hero → continue → [extra cards] → grid order was explicit, so no user confirmation was needed; the pacing nudge required no placement (it's a notification, not a card, per the task-3 note). Verified with `npm run build` — zero TypeScript errors.

- [x] Run `npm run build` (zero TypeScript errors) then `npm run dev` and use Playwright (the project's local devDependency, same approach as the earlier phases' `Working/*.png` screenshots) to capture the onboarding flow (all 4 steps) and the Hem screen. Save screenshots to this phase's `Working/` folder. Visually diff against the prototype descriptions above and fix anything still off.

  **Completion note:** `npm run build` exits clean (zero TypeScript errors; only the pre-existing chunk-size warning). Dev server was already running on `localhost:5173`; a throwaway Playwright script (local `playwright` devDependency, 402×874 viewport matching the prototype's iPhone frame) clears localStorage, walks all four onboarding steps, finishes to Hem, and captures `Working/01-onboarding-step0-welcome.png` … `04-onboarding-step3-examdate.png` + `05-hem.png`. Visual diff confirmed: dots elongate/fill correctly with the back button appearing from step 1; goal chips render `1,20–2,00` with `1,40` pre-selected solid green; exam cards show live countdowns ("Höstens prov · om 115 dagar" for 2026-10-25 as of 2026-07-02) with the 7px-border filled radio; Hem greets "God dag, Elin" (first name from "Elin Andersson", 10–18h window) over a capitalized "Torsdag 2 juli", with streak pill, green hero, and the 2-col grid in exact ORD/XYZ/LÄS/KVA/MEK/NOG/ELF/DTK order with 32px accent rings. Two things *were* still off on the first capture and got fixed: (1) the hero showed "Ange provdatum →" immediately after onboarding because `Home.tsx` read the exam date into state on mount — which fires while Home sits *under* the onboarding overlay — so the just-written date was stale; switched to a render-time `getExamDate()` read (Home re-renders when the overlay unmounts), and the footer now correctly shows "Provet om **115 dagar** · 25 okt". (2) sv-SE `toLocaleDateString(month:'short')` emits "okt." with a trailing period, unlike the prototype's `examShort` "25 okt" — stripped it. Fresh-profile empty states (hero "– / 2,0" with no delta chip, 0% rings, no continue card) are expected per the task-3/4 decisions to use real data instead of the prototype's seeded mocks (1,15 / streak 12 / ORD 12-of-20).
