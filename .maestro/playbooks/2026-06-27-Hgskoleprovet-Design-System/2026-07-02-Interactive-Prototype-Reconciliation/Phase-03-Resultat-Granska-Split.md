# Phase 03: Resultat / Granska Split

**User decision (confirmed 2026-07-02):** split the current combined `src/pages/Results.tsx` (which mixes score summary + XP/achievements/level-up + question-by-question review in one screen) into two screens matching the prototype exactly: a **Resultat** screen (score summary only) and a separate **Granska** screen (review list only), reached via a "Granska dina svar" button. Keep all existing XP/achievement/level-up logic — relocate it into the new Resultat screen's layout, do not delete it. The prototype's markup for these two screens is `Reference/Hogskoleprovet-Prototyp.dc.html` lines 253–329; behavior notes are in `Reference/README.md` under "3. Resultat" and "4. Granska (Review)".

## Tasks

- [x] Read `src/pages/Results.tsx` in full and identify every distinct piece of content it currently renders (score/percentage, XP breakdown by difficulty, level-up animation, achievement toasts, personalized feedback card, HP score estimate, the filterable all/wrong/flagged question review list). Decide, and note as a completion note, which pieces belong on the new Resultat screen (score summary + gamification) vs. the new Granska screen (question review), and where the app's routing currently sends the user after finishing a session (grep for wherever `Results` or a `/results` route is navigated to).

  **Inventory (pre-split `Results.tsx`, 1011 lines) and allocation.** Distinct pieces found, → where each went: (1) green hero with 120px conic ring `{pct}%` + "{correct} av {total} rätt" → **Resultat** (replaced by the prototype's 172px `{correct}/{total}` ring); (2) hero "Dela" quick-share button + `handleShare` share-text builder → **Resultat**, consolidated into the share card as a "Dela resultat" button (function preserved, hero kept prototype-clean); (3) "Nytt personbästa!" pill + `hp_personal_best` tracking → **Resultat** hero (above the eyebrow); (4) 3-stat strip Rätt svar/Tid/Streak → **Resultat**, replaced by the prototype's Total tid / Snitt per fråga / Normerat (streak moves to the prototype's dedicated streak card, correct-count is in the ring); (5) conditional `streakIncreased` streak card → **Resultat**, merged into the prototype's always-shown streak card; (6) XP-award side effects (one-shot `session.xpEarned` guard, `saveStats`, level-up detection, notification-permission ask, personal-best write) → **Resultat** `useEffect`, copied verbatim; (7) XP earned card (+N XP, per-difficulty breakdown, level label + progress bar) + level-up banner → **Resultat**, restyled for the green screen (see task-2 note); (8) `AchievementToast` + `checkAchievements()` → **Resultat** overlay, unchanged; (9) HP score predictor card ("Estimerat HP-betyg" + Betygsskala toggle) → **Resultat** analysis section, and its combined score also feeds the strip's gold "Normerat" stat; (10) full-HP-day pass-1 break card (with the "Starta kvantitativt pass" flow driver) and pass-2 combined-score card → **Resultat** analysis section; (11) Per delprovstyp bars, (12) Tidanalys card, (13) Resultat per svårighetsgrad, (14) exam-only report (Avsnittsresultat table, Jämförelse med medel, "Vad du bör öva mer på") → **Resultat** analysis section, markup carried over verbatim; (15) share preview card → **Resultat** analysis section; (16) filterable all/wrong/flagged review list (accordion cards with option grid, `ExplanationCard`, SRS/time/source pills, bookmark toggle) → **Granska** (rebuilt as the prototype's flat cards, filter kept — see task-3 note); (17) "Öva på N fel från detta pass" drill button → **Granska**, below the list (it's a review-context action). There was no separate "personalized feedback card" beyond the pieces above — the closest match is the score-dependent heading, which the prototype's own `resTitle` covers. **Routing before the split:** finishing a session went `Session.tsx:191` `navigate('/results')` (after `finishSession()`); the only other references to the route were the `/results` route registration in `App.tsx:140` and `HIDDEN_PATHS` in `TopNav.tsx:47` — no history/progress page linked to it (`Progress.tsx` renders its own history inline).

- [x] Build the Resultat screen to match the prototype exactly: full-screen green (`#224A3A`) background, "PASS SLUTFÖRT · {SECTION}" eyebrow, a heading that varies by score (`"Bra jobbat, {name}"` ≥80%, `"Bra kämpat, {name}"` ≥50%, else `"Fortsätt kämpa, {name}"`), a gold conic-gradient score ring (`{correct}/{total}` + `{pct} % RÄTT`), a 3-up stat strip (Total tid / Snitt per fråga / Normerat, the last in gold), a streak card (`"{streak} dagars svit!"` + `"{answered} frågor besvarade totalt."`), then three actions: a solid cream "Granska dina svar" button (primary, routes to the new Granska screen) and a row of two outline buttons "Öva igen" / "Till hem". Fold the existing XP/achievement/level-up content into this screen without breaking this card order — if there's a natural slot (e.g. inside or just below the stat strip) use it; if placement is genuinely ambiguous, flag it back to the user rather than guessing.

  **Completion note:** new `src/pages/Resultat.tsx` (883 lines). The first viewport is the prototype's card stack verbatim: `min-h-[100dvh]` flex column on `--color-green`, `pt-16 px-5` (= the prototype's `64px 20px 0`), eyebrow 700 11px `.16em` `--color-green-tint` (`#9FB7A8`) "PASS SLUTFÖRT · {resSec}", heading Newsreader 400 32px/1.1 `--color-cream` with the exact thresholds (≥80 "Bra jobbat" / ≥50 "Bra kämpat" / else "Fortsätt kämpa") + prototype `firstName()` semantics (first whitespace-separated word of `hp_user_name`; if no name stored, the comma+name is omitted rather than inventing a fallback). Ring: 172px `conic-gradient(var(--color-gold) 0 {pct}%, rgba(255,255,255,.13) 0)`, 140px green inner, Newsreader 500 46px/.9 `{correct}` with 22px `#8FA89A` `/{total}`, "{pct} % RÄTT" 600 11px `.1em` at 7px gap. Stat strip: `bg-white/[.07]`, 1px `white/10` border, radius 18, mt-24px, cells `py-[15px]` with 700 18px tabular-nums values + 500 11px labels, 1px dividers; "Snitt / fråga" = `round(duration/total) s` (prototype's `resAvg` formula). **Normerat decision:** the prototype's `normDelta` is placeholder math (`'+' + (correct*0.01)`), so the gold stat instead shows the app's real `estimateSectionedScore().combined` formatted Swedish-style (`1,71`) — truthful data in the prototype's exact slot. Streak card: `rgba(191,90,51,.16)` bg / `rgba(217,128,89,.35)` border / radius 16 / `13px 15px` padding, 34px terracotta tile with the 13px rotated `#FBE7D9` square, "{streak} dagars svit!" 700 14px cream + "{answered} frågor besvarade totalt." 500 12px `#D9B7A6` — streak from `loadStats()`, answered = sum of `Object.keys(answers).length` across `loadHistory()` (the finished session is already in history). **Gamification placement (not ambiguous — the task's suggested slot fit):** level-up banner + XP card sit directly below the streak card, restyled to the dark surface (`bg-white/[.07]` card, gold progress fill, gold-tinted level-up banner) with all data intact (+N XP, lätt/medel/svår × 10/15/25 lines, 20 XP sessionbonus, "Nivå N — label", XP-to-next progress); `AchievementToast` mounts as before; the one-shot XP-award effect (guarded by `session.xpEarned`) was copied unchanged so back-navigation from Granska can't double-award. Then `flex-1` spacer → actions pinned at the viewport bottom exactly per prototype: cream 54px radius-16 700/16 "Granska dina svar" → `/granska`; two 50px outline buttons (1.5px `rgba(255,255,255,.22)`, 600 15px `#D6E4D9`) — "Öva igen" now **restarts the same set** (`buildSession` with the session's own `questionIds`/`timeLimitSeconds`/`instantFeedback`/`type`/`studyMode`) per the README's "restarts the set", where the old screen just went to `/practice`; "Till hem" → `/`. The remaining relocated analysis cards (HP predictor, full-day pass 1/2, per delprovstyp, Tidanalys, svårighetsgrad, exam report, share card — task-1 items 9–15) live **below the fold** in a `--color-paper` section headed "DETALJERAD ANALYS", markup carried over verbatim, so the initial viewport stays prototype-exact and scrolling reveals the app's richer analytics (placement decision noted in the final report). Also fixed in passing: the pass-1 "Starta kvantitativt pass" button was ink-on-blue with no text color (illegible on white card) — given explicit `text-white`.

- [x] Build the Granska screen to match the prototype exactly: paper background, header with a back arrow (→ Resultat) and "Granska dina svar" (Newsreader 22px), then a scrollable list of cards — one per answered question — each with a Rätt/Fel pill (green `#E4EDE6`/`#2E6A4C` or terracotta `#F6E3DC`/`#A84A26`), "Fråga N", the question's `main` text (Newsreader 18px), "Ditt svar: …" (colored to match right/wrong), "Rätt svar: …" shown only when the user answered wrong, a hairline divider, and the explanation text. Reuse the existing filter functionality (all/wrong/flagged) from the old `Results.tsx` if it still makes sense here — the prototype's Granska has no filter UI, so decide whether to keep it as an enhancement (recommended, since it's existing functionality) or drop it to match exactly; note the decision.

  **Completion note:** new `src/pages/Granska.tsx` (152 lines). Header per prototype: `pt-14` (56px), 38px back button (white, 1px `--color-card-border`, radius 12, the exact `M7 2 2 8l5 6` chevron stroked `#6f6859` at 2.2 — same literal-hex precedent as Phase 01's onboarding back button) → `navigate('/resultat')`, title Newsreader 400 22px/1.05 `--color-ink`. Cards exactly per the prototype's `reviewItems` mapping: white radius-16 `15px 16px` cards at 12px gap; self-start pill `4px 10px` radius-999 700 10px `.06em` — Rätt `#E4EDE6`/`#2E6A4C`, Fel `#F6E3DC`/`#A84A26`; "Fråga N" 500 12px `#A89F90` at 11px gap; question text Newsreader 500 18px/1.3 ink at 5px gap (via `MathText`, since this bank's questions carry `$math$`/`**bold**`); "Ditt svar: {option text}" 600 13px/1.4 colored `#2E6A4C`/`#A84A26` (skipped/unanswered → "—", the prototype's own null-handling); "Rätt svar: {option text}" 600 13px `#2E6A4C` only when not right; 1px `#F0EADD` divider (`12px 0 10px`); explanation 500 13px/1.55 `#6f6859` from `q.explanation`. **Filter decision: kept** (the task's recommended path) — the Alla/Fel/★ Markerade chips carry over with their existing token styling, sitting between header and list; "Fråga N" numbering uses the question's position in the session, so filtering to "Fel (1)" still shows "Fråga 2" (the old screen renumbered filtered lists from #1 — fixed). **Also kept:** the "Öva på {N} fel från detta pass →" drill button (restyled to a 54px radius-16 green primary, below the list) and an empty-state line when a filter matches nothing. **Deliberately dropped from Granska to match the prototype's flat cards** (nothing lost app-wide): the accordion option grid + `ExplanationCard` (the full A–E grid and structured Metod/steps explanation are already shown at answer time in Session's check step, and the correct answer is on this card as "Rätt svar"), the SRS/time/source pills (SRS state lives in `/srs` and Statistik; per-question times feed Resultat's Tidanalys card), and the per-card bookmark toggle (bookmarking is in-session via the Phase-02 54×54 footer toggle and managed in Sparade frågor). All session data reads (`loadSession`, `skipped`, `flagged`, `answers`) are unchanged, so Granska works for any historical current-session including exam mode.

- [x] Update routing/navigation so "finish a session" → Resultat, and "Granska dina svar" → Granska → back arrow → Resultat. Grep the codebase for any other place that links directly to the old combined Results screen (e.g. from history/progress pages) and update those links to point at Resultat (or Granska, if the link's intent was specifically "view past answers"). Remove `src/pages/Results.tsx` only once both new screens are confirmed to cover everything it did — check for other imports of it first.

  **Completion note:** a full grep for `Results`/`/results` found exactly three referencing files, all updated — no history/progress page ever deep-linked to the old screen. (1) `App.tsx`: lazy `Results` import replaced with `Resultat` + `Granska`, routes `/resultat` and `/granska` registered, and `/results` kept as a `<Navigate to="/resultat" replace />` redirect (costs one line, protects any stale PWA/bookmark entry — the app is an installable PWA); (2) `Session.tsx:191` `handleFinish` now navigates to `/resultat`; (3) `TopNav.tsx` `HIDDEN_PATHS` swaps `/results` for `/resultat` + `/granska`, so both remain tab-bar-free full-screen flows per the README ("Practice, Result, Review, and Saved are full-screen flows"). Flow verified end-to-end: finish → `/resultat`, "Granska dina svar" → `/granska`, back arrow → `/resultat` (URLs asserted in the task-5 Playwright run). `src/pages/Results.tsx` (1011 lines) **deleted** after confirming zero remaining imports and that every task-1 inventory item has a home (items 1–15 + XP side effects on Resultat, 16–17 on Granska); the old screen's `pt-topnav` padding was vestigial (TopNav was already hidden on `/results`) and was not carried over.

- [x] Run `npm run build` (zero TypeScript errors), then use Playwright to complete a practice session end-to-end, screenshotting Resultat and Granska. Save to this phase's `Working/` folder and fix anything still off, including confirming the XP/achievement content relocated cleanly and isn't visually clipped or duplicated.

  **Completion note:** `npm run build` exits clean (zero TypeScript errors, re-run after every fix below). A throwaway Playwright script (repo's own `playwright` devDependency, 402×874 viewport, same approach as Phases 01/02) seeds the profile (`hp_user_name: 'Elin Andersson'`) plus a fresh 4-question ORD session (`ord-e01`–`e04`, `instantFeedback: false`), answers 3 of 4 correctly through the real UI (option → Kontrollera svar → Nästa fråga ×3 → Se resultat → finish modal), and walks Resultat → Granska → back. Screenshots continue the shared `Working/` numbering: `15-resultat-achievement-toast.png` (achievement toast firing over the green screen — relocation working, "Igångsättaren" 1/2), `16-resultat-top.png` (toast dismissed: "PASS SLUTFÖRT · ORD", "Bra kämpat, Elin" at 75%, gold 3/4 ring + "75 % RÄTT", strip 3:07 / 47 s / gold 1,71, "1 dagars svit! / 4 frågor besvarade totalt.", "⭐ Ny nivå uppnådd! Nivå 2 — Grundnivå", "+55 XP" card with 2×10+1×15+20 breakdown and 10/200 XP bar — everything fits the first viewport above the pinned actions, nothing clipped or duplicated), `17-resultat-analysis.png` (below-fold paper section: Tidanalys, svårighetsgrad, share card), `18-granska.png` (header + chips + Rätt/Fel cards with Ditt svar/Rätt svar/divider/explanation), `19-granska-filter-fel.png` ("Fel (1)" filter keeping "Fråga 2" numbering + the green drill button). URL assertions: finish → `/resultat`, Granska button → `/granska`, back arrow → `/resultat`. **One real pre-existing bug found and fixed** — the very first run could not finish a session at all: `Session.tsx`'s finish-confirmation modal overlay was `z-30` (and the jump-to-question sheet `z-20`, keyboard guide `z-30`) while the fixed footer and header are `z-40`, so on mobile-height viewports the footer sat *on top of* the bottom-sheet modal and swallowed clicks on "Visa resultat →" (Playwright surfaced it as "subtree intercepts pointer events"; a real thumb tap would hit the same dead zone). All three overlays raised to `z-50`. Final line counts: `Resultat.tsx` 883, `Granska.tsx` 152, `Results.tsx` deleted; `App.tsx`, `Session.tsx`, `TopNav.tsx` touched for routing/z-index only.
