# Phase 02: Öva (Practice) Engine Reconciliation

The prototype's practice screen (`Reference/Hogskoleprovet-Prototyp.dc.html` lines 175–251, plus the option/feedback/footer logic in `renderVals()`) describes one engine that renders three question layouts depending on optional fields present on the question object: **standard** (lead + main + 4 options), **reading passage** (`passage` field — a white "LÄSTEXT" card, and consecutive questions sharing the same `passage` string stay on screen together as linked questions, e.g. "Text 1 · Fråga 1 av 2"), and **diagram** (`chart` field — a bar chart card with title/bars/labels, terracotta bars, height = `value / max(values) * chartHeight`). This repo's practice flow is split across `src/pages/Practice.tsx` (mode/section selection, filters) and `src/pages/Session.tsx` (the actual per-question screen) — that split is a deliberate enhancement beyond the prototype (mode selector, exam simulator, SRS) and should stay. This phase is only about reconciling **Session.tsx's per-question rendering** against the prototype's three layouts and exact interaction states.

## Tasks

- [x] Read `src/pages/Session.tsx` in full, plus the question data types it consumes (find the type definition — likely in `src/data/` or `src/types/`, grep for the question interface used by `loadSession()`/`buildSession()`). Confirm whether the data model already supports (a) a `passage` field with multiple consecutive questions sharing the same passage, staying on-screen together, and (b) a `chart`/diagram field for DTK questions. Report findings as a completion note: which of the three layouts (standard, reading-passage, diagram) are already correctly distinguished in the UI today, and which are missing or rendered generically.

  **Findings (pre-fix state; `Session.tsx` was 819 lines, types in `src/types.ts`):** The **data model already supports both optional fields** — `Question.passageId` (resolving via `getPassage()` to `src/data/passages.ts`, 19 passages with 79 linked questions, 4–5 questions per passage) and `Question.chartData` (`ChartData`: bar/line, multi-series; 37 of the 131 DTK questions carry it, the rest use `tableData` — richer than the prototype's single-series `chart.bars`). Layout status per the prototype's three: **standard = correct** (chips, prompt, options — fine). **Reading-passage = rendered generically**: the passage went into the shared "context box" as sans-serif 14px `--color-ink-muted` text (not Newsreader 15px/1.7), the label was 9px, and — the bigger gap — **linked questions were never grouped**: every session builder (20 `buildSession()` call sites across `Practice.tsx`, hubs, `Bookmarks.tsx`, etc.) shuffles ids, so questions sharing a `passageId` land scattered through the set, and no "Text N · Fråga X av Y" lead existed anywhere. **Diagram = rendered generically**: `ChartView.tsx` drew an SVG axis/legend chart with a green-first palette and honored per-series `color` hexes hardcoded in the data (21 series carry raw Tailwind hexes like `#3b82f6`/`#8b5cf6` from before the design system) — not the prototype's white card with terracotta HTML bars, value labels above and axis labels below.

- [x] For any layout found missing or incorrect in the previous task, implement it to match the prototype exactly: reading-passage layout is a `LÄSTEXT`-labeled white card (radius 16px, padding 15px/16px) with the passage text in Newsreader 15px/1.7 line-height, sitting above the lead/main/options, and grouped questions must show the "Text N · Fråga X av Y" lead format and keep the same passage card visible while paging between the linked questions. Diagram layout is a white card with a bold title, then bars laid out `align-items:flex-end; justify-content:space-between` with a value label above each bar and an axis label below, terracotta (`#BF5A33`) bar fill, using the same `value/max*height` scaling as the prototype. If the underlying question bank/content in this repo's DTK section doesn't yet carry chart data, flag this back to the user rather than inventing exam content — this phase is about rendering plumbing, not writing new question content.

  **Completion note — reading passage:** grouping fixed at the source: `buildSession()` in `src/utils/session.ts` (100 → 126 lines) now runs the ids through a new `groupLinkedPassages()` — a stable reorder that pulls each passage group together at its first occurrence, leaving everything else untouched — so all 20 call sites (drills, exam mode, bookmarks, SRS, weak-area) get consecutive linked questions for free; the adaptive-difficulty swap in `Session.tsx` got a guard so it never swaps a passage-linked question out of (or into) its group. The passage card now matches the prototype exactly: `card` (radius 16) with `px-4 py-[15px]`, label 600 10px `.12em` `--color-ink-faint` with 10px gap, passage in `--font-serif` (Newsreader) 15px/1.7 `--color-ink-soft` (`#33302A`), 18px below-margin, plus `whitespace-pre-line` so the bank's multi-paragraph passages keep their breaks. The label reads "Lästext" (uppercased by CSS → LÄSTEXT) for both LAS and ELF passages — the prototype hardcodes LÄSTEXT even for its English ELF passage — while non-passage `context` on other types keeps the "Kontext" label. The **"Text N · Fråga X av Y" lead** is computed at render time (N = ordinal of the passage among the session's distinct passages, X/Y from the consecutive run) and renders as a 500 13px/1.4 `--color-ink-muted` line above the question text, exactly the prototype's `qLead` slot; the card stays on screen across the linked questions since each renders the same `passageId`. **Diagram:** `ChartView.tsx` (127 → 154 lines) got a new `SimpleBarChart` used for every single-series bar chart — prototype-exact: card padding `14px 16px 12px`, bold 12px/1.3 ink title with 16px gap, a 118px `flex items-end justify-between gap-3` row, per-bar column with value label above (700 11px `#8B8478` tabular-nums), bar `height = round(value/max × 92px)` radius `6px 6px 0 0` fill `--color-terracotta` (`#BF5A33`), axis label below (600 11px `#A89F90`), card margin-bottom 18px. Multi-series bar and line charts (26 of 37 — the app's DTK data is *richer* than the prototype's single-series `chart.bars`) keep the existing SVG axis renderer as a deliberate enhancement, but its palette is now terracotta-first design tokens and it **ignores the 21 raw Tailwind hexes (`#3b82f6`, `#8b5cf6`, `#ef4444`…) hardcoded in question data** — fixed in the renderer so the 18k-line data file stays untouched. **Nothing was flagged as missing:** all 131 DTK questions already carry `chartData` or `tableData`; no chart fields had to be invented.

- [x] Audit the answer-option rendering (letter badge A–E + text) and the 4 reveal states against the prototype's exact values in `renderVals()`'s `practiceOpts` mapping: pre-check unselected (bg `#FFFFFF`, border `1px solid #EAE3D6`), pre-check selected (bg `#F4E7DD`, border `1.5px solid #C97A52`, badge bg `#BF5A33`), post-check correct (bg `#E4EDE6`, border `1.5px solid #3E7A5E`, badge bg `#2E6A4C` with a `✓`), post-check wrong-selected (bg `#F6E3DC`, border `1.5px solid #C0573A`, badge bg `#BF4A2E` with a `✕`), post-check other/dimmed (bg `#FBF9F4`, border `1px solid #EEE8DC`, muted text `#A89F90`). Fix any state whose colors or badge glyphs drift from these values.

  **Already exact (via the Phase-01-era tokens in `src/index.css`):** all five row backgrounds and border *colors* (`#FFFFFF`/`#EAE3D6`, `#F4E7DD`/`#C97A52`, `#E4EDE6`/`#3E7A5E`, `#F6E3DC`/`#C0573A`, `#FBF9F4`/`#EEE8DC`), the three badge fills (`#BF5A33`/`#2E6A4C`/`#BF4A2E`), and the `✓` glyph. **Drift found and fixed:** (1) selected/correct/wrong borders were **2px, prototype is 1.5px** (`.answer-option-*` in `index.css`); (2) row radius 12px → **15px**, padding `14px 16px` → **15px** (prototype `boxStyle`); (3) wrong badge glyph was `✗` U+2717 → **`✕` U+2715**; (4) the letter badge was a 24px *circle* with `--color-hairline` fill and ink text → now the prototype's **27×27, radius 8** (`rounded-lg`), default **transparent with 1.5px `#E0D8C8` border and `#A89F90` letter**, solid-filled with white glyph in the three active states; (5) the dimmed state relied on `opacity: 0.8` with full-ink 14px text → opacity removed, text now explicit **`#A89F90`** with badge letter `#C3BBAC` (prototype's dimmed pair); (6) option text was 14px ink in every state → now **16px/1.3, weight 600** default `#33302A` (`--color-ink-soft`), **weight 700** + per-state text colors when active (`#23201A` selected, `#1E3A2E` correct, `#7A3A28` wrong — all via the existing `--color-*-text` tokens), row gap 12px → 13px. Kept: `items-start` alignment + `min-h-[52px]` (app options run multi-line, prototype's short strings center-align trivially) and the reveal pulse animation.

- [x] Audit the footer: a bookmark toggle button (54×54px, filled terracotta bg `#F3E0D5`/border `#E0B79F`/icon `#BF5A33` when saved, plain white otherwise) to the left of the primary action button, which cycles through exactly three label states — `"Välj ett svar"` (disabled, bg `#DAD3C5`) while nothing is selected, `"Kontrollera svar"` (bg `#224A3A`) once an option is picked but not yet checked, then `"Nästa fråga"` or `"Se resultat"` (only on the final question) after checking. Confirm the feedback panel appears only after checking, with the exact copy `"Rätt!"` / `"Inte riktigt"` and the correct/wrong background+border+title colors from the design tokens table in `Reference/README.md`. Fix any drift.

  **Label cycle was already exact** (`Välj ett svar` disabled → `Kontrollera svar` → `Nästa fråga` / `Se resultat` on the last question), and the feedback panel already appeared only after checking. **Drift found and fixed:** (1) the bookmark toggle was a borderless 44px icon button whose only saved-state signal was the filled glyph → now the prototype's `bmBtnStyle`: **54×54, radius 16**, saved = bg `#F3E0D5` (`--color-terracotta-soft`) + **1.5px border `#E0B79F`** + icon fill/stroke `#BF5A33`, unsaved = white card bg + 1px `#EAE3D6` border + `#A89F90` stroke, 19px icon, `.15s` transition; (2) the primary button was `btn-primary` (radius 12, weight 600) hugging its text at the right edge, with disabled rendered as *green at 45% opacity* → now the prototype's `footerBtnStyle`: **flex-1 (fills the row beside the bookmark), 54px tall, radius 16, 700 16px**, disabled **bg `#DAD3C5` / text `#9A9183`**, active bg `--color-green` (`#224A3A`) / text `--color-cream` (`#FBF7EE`); the study-mode Svårt/Ok/Enkelt buttons (app enhancement, kept) share the same 54px/radius-16 row. (3) Feedback panel (`ExplanationCard.tsx`, 135 → 133 lines): the bg/border/title colors were already the exact `--color-feedback-*` tokens, but the copy drifted — correct read `"✓ Rätt!"` at 18px-black and wrong read `"✗ Fel — rätt svar B: …"` → header now prototype-exact: **9px rotated-square dot** (`#2E6A4C`/`#BF4A2E`) + **`"Rätt!"` / `"Inte riktigt"`** at 700 14px. The "rätt svar" info isn't lost — the correct option is simultaneously highlighted green with a `✓` badge in the list, exactly as the prototype does it; `correctAnswer`/`correctAnswerText` props kept optional for caller compatibility (`Results.tsx` also mounts this card — its review accordion still shows the correct answer via its own highlighted option grid; full Resultat/Granska reconciliation is Phase 03's scope). Kept as enhancements: structured Metod/steps/Vanliga fällor explanation body and the "Lär dig mer" deep-link.

- [x] Audit the fixed header: back button → Hem, a thin progress bar that fills proportionally to `(qIndex + (checked ? 1 : 0)) / total`, and a live mm:ss timer pill that only counts while the practice view is active and resets when a new set starts. Confirm bookmark state persists per-question (keyed like `"<SECTION>:<index>"` or equivalent) and survives a page reload via localStorage, matching the prototype's `hp_bookmarks_v1` behavior — note the actual key name this app already uses.

  **Progress bar — real drift fixed:** the fill was `current / total`, missing the prototype's checked term, so the bar never reached 100% (a finished 20-question set showed 95%); now `(current + (checked ? 1 : 0)) / total` exactly per `progFrac`. Kept the app's edge-to-edge 3px bar under the header row (vs the prototype's inline 7px pill-row bar) — the header *layout* is an established app pattern (✕ / counter / timer), only the math was wrong. **Timer — real drift fixed:** `QuestionTimer` reset to 0:00 on *every question*, but the prototype's `elapsed` counts the **whole practice set** and resets only when a new set starts; the pill now shows set-elapsed mm:ss (700 13px tabular-nums, and the prototype pill's 7px terracotta dot was missing — added), counting only while `Session` is mounted and resetting on remount (= new set). The per-question elapsed is still tracked internally so the Phase-04 two-minute pacing warning (pill turns `--color-error` and pulses) keeps working; per-question ms are separately persisted via `saveQuestionTime` refs, untouched. The exam-mode countdown pill (`timeLeft`) is an app enhancement, unchanged. **Back button — deliberate difference, kept:** the app's ✕ opens a finish-confirmation modal (answered/markerade/ej besvarade counts) instead of the prototype's silent back → Hem; the modal protects an in-progress set and was kept per the phase-doc instruction to preserve enhancements. **Bookmarks — confirmed, no change:** persisted under the localStorage key **`hp_bookmarks`** (`src/utils/bookmarks.ts`) as an array of stable question ids (e.g. `"dtk-e1"`) — the equivalent of the prototype's `hp_bookmarks_v1` `"<SECTION>:<index>"` keys, and strictly more robust since ids survive shuffled sets; verified via Playwright that toggling writes `["dtk-e1"]` and survives reload.

- [x] Run `npm run build` (zero TypeScript errors), then use Playwright to walk through one full practice set covering all three layouts if the current question bank has them (a standard ORD question, a LÄS reading-passage question, a DTK diagram question), screenshotting each layout plus one of each answer-reveal state. Save to this phase's `Working/` folder and fix anything still visually off.

  **Completion note:** `npm run build` exits clean (zero TypeScript errors). A throwaway Playwright script (local devDependency, 402×874 viewport, same approach as Phase 01) seeds a 4-question session — `ord-e01` (standard), `las-p01-q01`/`las-p01-q02` (linked passage), `dtk-e1` (single-series bar diagram) — with `instantFeedback: false` to exercise the full check cycle, then walks it end to end. Screenshots continue Phase 01's numbering in the shared `Working/` folder: `06-ord-standard-unselected.png` (disabled `#DAD3C5` "Välj ett svar"), `07-ord-selected-precheck.png` (terracotta selected row + solid badge + green "Kontrollera svar"), `08-ord-checked-wrong.png` (all four post-check states at once: green `✓` correct, red `✕` wrong-selected, two dimmed rows, "Inte riktigt" panel), `09`/`10`/`11-las-passage-*.png` (LÄSTEXT Newsreader card staying on screen across "Text 1 · Fråga 1 av 2" → "Fråga 2 av 2"), `12-dtk-diagram.png` (terracotta bars, values above, years below), `13-dtk-bookmarked.png` (filled terracotta 54×54 bookmark), `14-dtk-checked-correct-se-resultat.png` ("Rätt!" panel + "Se resultat" on the last question). **One bug found via the first capture and fixed:** the scroll position persisted across questions — after scrolling to an explanation, "Nästa fråga" opened the next question mid-scroll (the LAS passage appeared without its top/label in view); added a scroll-reset effect on question change (`mainRef.scrollTo(0,0)` + `window.scrollTo`), matching the prototype's implicit per-question reset. Re-captured after the fix; all states verified against the prototype values. Final line counts: `Session.tsx` 882, `ChartView.tsx` 154, `ExplanationCard.tsx` 133, `session.ts` 126, `index.css` unchanged in size (values only).
