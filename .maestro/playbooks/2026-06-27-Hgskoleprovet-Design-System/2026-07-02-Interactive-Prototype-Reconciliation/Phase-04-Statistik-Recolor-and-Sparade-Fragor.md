# Phase 04: Statistik Recolor & Sparade Frågor Reskin

Two independent, user-confirmed changes (confirmed 2026-07-02):

1. **Statistik recolor:** `src/pages/Progress.tsx` currently colors its 8 per-section accuracy bars with 8 distinct Tailwind colors (violet/blue/emerald/amber/rose/pink/fuchsia/purple). Switch to the prototype's 2-color convention (green `#224A3A` for the 4 verbal sections — ORD, LÄS, MEK, ELF — terracotta `#BF5A33` for the 4 quant sections — XYZ, KVA, NOG, DTK), matching the verbal/quant color language already used everywhere else in the app (Home's section grid, the answer-state palette).
2. **Sparade frågor reskin:** keep `src/pages/Bookmarks.tsx`'s existing richer functionality (filter by type, expandable answers, drill CTA) but restyle its cards/spacing/typography to match the prototype's "Sparade frågor" screen visually.

Prototype references: Statistik markup at `Reference/Hogskoleprovet-Prototyp.dc.html` lines 369–450; Saved markup at lines 331–367. Behavior/token notes in `Reference/README.md` under "6. Statistik" and "5. Sparade frågor (Saved)".

## Tasks

- [x] Read `src/pages/Progress.tsx` in full. Replace the 8-color per-section palette in the "Träffsäkerhet per delprov" (or equivalently named) bar list with the 2-color verbal/quant scheme, sorted by percentage like the prototype (highest first). Keep the existing 12-week consistency calendar and any other content added in the earlier features phase — this task only touches the per-section bar colors, not layout or other cards.

  **Completion note (verified, not re-done):** this task's change was already in the working tree when this session resumed — **a prior agent run was interrupted by a session limit partway through this phase, after making this edit but before checking anything off**; this note records verification of that work against the phase doc's wording rather than new work. Verified line-by-line against the prototype's Stats markup (`Reference/Hogskoleprovet-Prototyp.dc.html` lines 428–439): the "Träffsäkerhet per delprov" card renders all 8 sections from a single `.sort((a, b) => b.pct - a.pct)` (highest first, exactly like the prototype's ELF 70 → DTK 19 ordering), fill color is `#224A3A` for the 4 verbal types (`VERBAL_TYPES = ['ORD', 'LAS', 'MEK', 'ELF']`, declared at the top of the file) and `#BF5A33` for the 4 quant types, on the prototype's exact row anatomy — 34px 800/13 type label (with `LAS → LÄS` display mapping), 8px radius-999 track in `#EFE8DA`, 32px right-aligned 700/12 `#6f6859` percentage, 11px row gap, inside a radius-18 card headed 700/13 "Träffsäkerhet per delprov" with 14px bottom margin. Sections with 0 answered show `—` and a 0-width fill (graceful degradation the prototype's static markup didn't need). The old 8-color `TYPE_COLORS` map still exists but now only styles *other* cards (per-type grid cards, sparklines, history pills) — out of this task's stated scope. The 12-week "Träningskonsistens" calendar sits directly below the card, untouched and confirmed rendering in `Working/21-statistik-per-delprov.png`. The same interrupted run also restyled the segmented control (Vecka/Månad/Allt: `#E6DFD0` track, radius 12/9, 3px padding, active white + `0 1px 2px rgba(0,0,0,.06)` shadow + 700 `#224A3A`) and the "Den här veckan" card to prototype-exact values — verified correct except one real bug fixed under task 2's pass (see below: the today-highlight comparison could never match).

- [x] Compare the rest of `Progress.tsx` against the prototype's Statistik screen: a segmented control (Vecka / Månad / Allt), a "NORMERAT (ESTIMAT)" trend card with a line/area chart and a `▲ delta sedan {month}` chip, and a "Den här veckan" 7-day bar card (mån–sön, with the current day highlighted in green/bold). Note which of these exist already vs. are missing as a completion note, then add whichever are missing using real data from the app's existing stats/analytics utilities (`loadStats()`, `loadHistory()`, etc. per the earlier codebase survey) — do not invent placeholder numbers.

  **Existed already (from the interrupted run):** the title/subtitle block (Newsreader 26 "Statistik" + 500/13 `#8B8478` "Din utveckling över tid"), the prototype-exact segmented control wired to the existing `timeRange` state, the "Den här veckan" card (real `questionsByDay` counts, `#CBD7CD`/`#E4DCCC`/`#224A3A` bars, 86px row, "{N} frågor" total), and a *static* "NORMERAT (ESTIMAT)" card. **Missing / wrong, fixed this session** (`Progress.tsx` now 1248 lines): (1) the card's big number rendered `readiness.score` — a 0–100 readiness index — as e.g. "45,00" where the prototype shows a 0–2 normerat value; replaced with the already-computed `combinedScore` from `estimateSectionedScore(byType)` (respects the segmented control, `—` when null), Swedish comma formatting. (2) Added the trend chart + delta with real data: a `monthBuckets` aggregation groups the full `loadHistory()` by calendar month, scores each month via `estimateSectionedScore`, and keeps the last ≤6 months; if history spans <2 months it falls back to per-session points (last 15 sessions) so new users still get a truthful trend. Chip is `▲ {delta}` 700/12 `#2E6A4C` (or `▼` in `#A84A26` if declining, hidden at exactly 0), delta = last − first trend point; the right-hand caption becomes the prototype's `sedan<br>{first month, full sv-SE name}` (falls back to the old "{n} sessioner / {x}% rätt" when no trend). Chart is the prototype's exact SVG: `viewBox 0 0 300 120`, 104px tall, gridlines at y=39/74 in `#EFE8DA`, area polygon filled with a `#224A3A` 0.16→0 gradient (`id="normTrendArea"`), 2.5px `#224A3A` polyline, end dot r4.5 + r8 halo at 0.16 opacity; x spans 14→289, y maps min→98/max→39 (the prototype's own first/last coordinates), month labels 600/10 `#B3AA9B` justify-between below (≤6 labels, sampled when in per-session mode). (3) Fixed the today-highlight bug in "Den här veckan": the old check compared a 2-char uppercase label (`'TO'`) to a 3-char lowercase one (`'tor'`) so `isToday` was *never* true — now the loop marks index 6 (today, by construction) and labels render as the prototype's lowercase 3-char forms (`mån`…`tor`). Verified in `Working/20-statistik-top.png`: "1,83 ▲ 0,47 / sedan mars", mars→juli labels, bold green "tor" bar.

- [x] Read `src/pages/Bookmarks.tsx` in full. Restyle its cards to match the prototype's Saved screen: white cards (radius 15px, padding 14/15px) with a section-code eyebrow (Hanken 800 11px, green) above the question `main` text (Newsreader 16px/1.35), and a 34×34px terracotta bookmark-remove button on the right. Keep the existing filter-by-type and expand-to-see-answer functionality — layer the prototype's visual treatment on top rather than removing behavior. If there are saved questions, keep/add a primary "Öva alla sparade frågor" button (green, full-width, routes into a practice session built from just the bookmarked questions) above the list; if none, use the prototype's empty state (bookmark icon in a terracotta-soft tile, "Inga sparade frågor än", helper copy about tapping the bookmark during practice).

  **Completion note:** rewritten from scratch (218 lines; the interrupted run had only touched a padding class). Header follows the prototype's Saved header (and the Phase-03 Granska precedent): 38px white radius-12 back tile with the `M7 2 2 8l5 6` chevron (stroke `#6f6859`, 2.2) → `navigate('/profil')` per the README ("Entered from the Profil row"), title Newsreader 400 22px/1.05 `#23201A` **"Sparade frågor"** (was "Bokmärken"; the old duplicated `PageHeader` + h1 pair is gone). Cards exactly per prototype lines 346–352: white, 1px `#EAE3D6`, radius 15, `14px 15px` padding, 12px flex gap, 10px list gap; eyebrow 800 11px `.08em` `#224A3A` section code (`LAS → LÄS`), question text Newsreader 500 16px/1.35 `#23201A` at 6px gap (via `MathText`, `line-clamp-2` while collapsed); right side the prototype's 34×34 radius-10 remove button (`#F3E0D5` bg, 1px `#E0B79F` border, 16px filled bookmark in `#BF5A33`, shared `BOOKMARK_PATH` const) wired to the existing `handleRemove`. **Behavior kept:** type filter chips (unchanged token styling, now showing `LÄS`), tap-card-to-expand with the full existing expanded block — correct-answer-highlighted A–E options, Kontext box, explanation — now opening under a prototype-style 1px `#F0EADD` divider, with the collapsed card's old `{source} · #{number}` metadata moved into the expanded block (the prototype's collapsed card is sec + main only) and the redundant "Ta bort bokmärke" text link dropped (the 34px button is the remove affordance). **CTA moved above the list** per the prototype: 52px radius-16 `#224A3A` button, 700 15px `#FBF7EE`, labelled "Öva alla sparade frågor" (when a type filter is active it drills the filtered subset and says "Öva {TYP} · {n} frågor" — preserving the old filtered-drill behavior truthfully); same `buildSession(..., 'drill')` flow as before. **Empty state** is the prototype's verbatim: 60×60 radius-18 `#F3E0D5`/`#ECCFBE` tile with the 26px `#BF5A33` outline bookmark, Newsreader 400 22px/1.2 "Inga sparade frågor än", Hanken 500 14px/1.6 `#8B8478` "Tryck på bokmärket under en fråga när du övar för att spara den och repetera senare." at 9px gap, centered with `0 30px 60px` padding (the old "Öva nu →" button dropped to match). Added a small "Inga sparade frågor matchar filtret." line for the filter-empty edge case. Unused `TYPE_PILL` map deleted; `pt-topnav`/`pb-bottomnav` chrome kept (TopNav/BottomNav still show on `/bookmarks` — changing nav visibility is Phase 05's Profil-flow territory).

- [x] Run `npm run build` (zero TypeScript errors), then use Playwright to screenshot the reconciled Statistik and Sparade frågor (Bookmarks) screens, in both the empty and populated bookmark states. Save to this phase's `Working/` folder and fix anything still off.

  **Completion note:** `npm run build` (`tsc && vite build`) exits clean, zero TypeScript errors, re-run after every change. A throwaway Playwright script (repo's own `playwright` devDependency, 402×874 viewport, same approach as Phases 01–03) parses the real id→answer map out of `src/data/questions.ts` (1291 questions) and seeds `hp_session_history` with 12 sessions spanning **five calendar months** (mars→juli, accuracy trending 40%→80%, all 8 section types covered, 4 sessions inside the current week) plus `hp_bookmarks` with 5 questions across XYZ/ORD/MEK/LÄS/NOG — real question IDs and real correct answers, no invented numbers anywhere in the app code. Screenshots continue the shared `Working/` numbering from Phase 03's 19: `20-statistik-top.png` (header, Vecka-active segmented control, NORMERAT card showing "1,83 ▲ 0,47 / sedan mars" with the area chart and mars–juli labels, "Den här veckan · 47 frågor" with today "tor" as the bold `#224A3A` bar), `21-statistik-per-delprov.png` (all 8 accuracy bars sorted 100%→67%, ORD/MEK/ELF/LÄS green vs XYZ/NOG/DTK/KVA terracotta, with the untouched 12-week Träningskonsistens calendar directly below), `22-sparade-populated.png` (green "Öva alla sparade frågor" CTA above the 5 white eyebrow+Newsreader cards with terracotta remove buttons), `23-sparade-expanded.png` (ORD card expanded: divider, source line, ✓-highlighted answer options, explanation — expand behavior intact), `24-sparade-empty.png` (prototype empty state, fresh context with no bookmarks). Two things found "still off" during the run and fixed: the never-true `isToday` comparison (caught because shot 1 had no bold green bar — see task 2) and the first 21 capture duplicating 20 (the bars card already fit the first viewport; retaken with a centered scroll so the 8th bar and the calendar are visible).
