# Phase 02: Supporting Systems Tests

Add test coverage to the remaining six untested utility files: `src/utils/achievements.ts`, `src/utils/gamification.ts`, `src/utils/pacing.ts`, `src/utils/notifications.ts`, `src/utils/bookmarks.ts`, `src/utils/dailyChallenge.ts`. Read the playbook `README.md` first, and Phase 01's completion notes for the established testing conventions/style used in this playbook (should match `srs.test.ts`'s existing style too).

## Tasks

- [x] Read all six files in full. Also check where each is called from (`Profil.tsx`, `Home.tsx`, `Session.tsx`, notification-triggering code) to understand each function's real contract before testing it.

  **Completion note:** `gamification.ts` (73 lines) is a pure `hp_gamification` localStorage record (`{xp, streak, lastPracticeDate, longestStreak}`) with `loadStats`/`saveStats`/`awardXP`/`updateStreak`/`getLevel`. `updateStreak()` itself is only ever called from `session.ts`'s `finishSession()` (not directly from any page) — `Resultat.tsx` computes XP/level-up itself inline (not via `awardXP`) and just reads `loadStats()`/`getLevel()` afterward to render; `AppHeader.tsx` and `Profil.tsx` also just read `loadStats().streak` for display. `achievements.ts` (131 lines) is a `hp_achievements` id-array unlocked by `checkAchievements()`, called only from `Resultat.tsx` after a session finishes; it cross-reads `loadHistory()` (session.ts), `loadStats()` (gamification.ts), `getSrsStats`/`getStats` (srs.ts), `getBookmarks()` (bookmarks.ts) and `estimateHpScore()` (hpScore.ts) — so achievements are a derived/computed view over the other five systems, not their own independent state. `pacing.ts` (73 lines) computes `computePacing()` from `daysUntilExam()` (examDate.ts), `computeReadiness()` (readiness.ts) and `loadHistory()`; notably **no page currently renders `computePacing()`'s output directly** — its only consumer is `notifications.ts`'s `maybeShowPacingNotification()`. `notifications.ts` (81 lines) wraps the browser `Notification` API behind three localStorage keys (`hp_notif_enabled`, `hp_notif_last_shown`, `hp_notif_pacing_last_shown`); `Profil.tsx` calls `requestNotificationPermission`/`disableNotifications` from its settings toggle, `Resultat.tsx` calls `requestNotificationPermission()` once automatically 3s after a user's first-ever completed session. `bookmarks.ts` (25 lines) is a flat `hp_bookmarks` string-array behind `getBookmarks`/`isBookmarked`/`toggleBookmark`/`clearBookmarks`, read by `Bookmarks.tsx`, `Practice.tsx`, `Progress.tsx`, `Profil.tsx`, and toggled from `Session.tsx`'s footer flag button. `dailyChallenge.ts` (33 lines) picks `CHALLENGE_SIZE` (10) question ids via a seeded LCG keyed on the calendar date (`getDailyChallengeIds`) and tracks same-day completion via a `hp_daily_done` ISO-date string (`isDailyChallengeCompleted`/`markDailyChallengeCompleted`), consumed by `Practice.tsx`. jsdom (the vitest environment here) has no built-in `Notification` global, so `notifications.test.ts` installs a small fake `Notification` class on `globalThis` per test rather than mocking the module.

- [x] Write `src/utils/gamification.test.ts`: streak calculation logic (the exact rule for incrementing/resetting a streak based on last-practiced date — this exact logic was documented carefully during earlier design work, check it's implemented correctly against that spec), XP tracking.

  **Completion note:** 27 tests. Checked `updateStreak()`'s current implementation against the design-reconciliation spec (`2026-07-02-Interactive-Prototype-Reconciliation/Reference/README.md`'s "Progress persistence logic": same-day → no change; exactly one day since last practice → increment; more-than-one-day gap → reset to 1; first-ever completion → seed). The current `hp_gamification` implementation **matches the rule's shape exactly** (`diffDays===0` no-op, `===1` increment, else reset-to-1) — verified with dedicated same-day (double-call), exactly-one-day, multi-day-gap, and a fake-timer 3-day sequential-streak-build test. One deliberate divergence from the literal old-prototype spec, called out explicitly rather than silently assumed: the *old* `hp_progress_v1` prototype format defaulted to a seed streak of 12 and said "first ever completion keeps seed"; the *current* `hp_gamification` key has no seed concept at all (`loadStats()` defaults to `streak: 0`), so "first ever" here means `updateStreak()` sets `streak = 1` from a true zero-state — functionally the same intent (day one of practice counts as a 1-day streak) but a different literal default. Also covered: `longestStreak` correctly keeps its high-water mark after a reset (doesn't get dragged back down), and an edge case not in the original spec — a `lastPracticeDate` in the future (clock skew) produces a negative `diffDays`, which falls into the `else` branch and resets to 1 rather than crashing or incrementing (documented, not a bug, but worth locking in). `awardXP` tests hand-verified the `correct*rate+20` formula per difficulty (10/15/25 for easy/medium/hard) and found it silently ignores its `_total` parameter entirely (dead parameter — a caller passing the wrong total has zero effect) and falls back to the easy rate for any unrecognized difficulty string. `getLevel` boundary tests hit one-XP-below vs. exactly-at each threshold (99→still level 1, 100→level 2), and confirmed the top-level (10) case where `nextLevelXp` collapses to equal `currentLevelXp` (no level 11 exists) — verified both `Progress.tsx` (`isMaxLevel` check) and `Resultat.tsx` (`level===10` check) already guard this to avoid a 0/0 progress-bar division, so it's a real edge case but not currently a live bug.

- [x] Write `src/utils/achievements.test.ts`: badge-unlocking conditions — construct scenarios that should and shouldn't unlock each achievement, including boundary cases (exactly at the threshold, one below it).

  **Completion note:** 30 tests. Mocked `../data/questions` with a controlled 120-question pool (same rationale/pattern as `readiness.test.ts`: mastery thresholds up to `mastered_100` are impractical to hit by hand against the real ~1446-question bank), and used real `saveStats`/`toggleBookmark`/direct `hp_srs`/`hp_session_history` localStorage writes for everything else, matching this playbook's established convention. Covered every threshold with an exact-boundary + one-below pair: `streak_3/7/30` (2 vs 3, 6 vs 7, 29 vs 30), `sessions_10/50/100`, `bookmarks_10`, and `mastered_10/50/100` (SRS interval 20 vs the actual 21-day mastery cutoff — note this achievement's mastery bar is *stricter* than `readiness.ts`'s own mastery definition, which uses interval ≥7, not ≥21 — a real inconsistency between two features both called "mastery" in this codebase, worth knowing about even though not a bug in either file individually). `perfect_session` confirmed its `total>=5` minimum is a real gate, not just the 100% check (4/4 correct does *not* unlock it). `hp_*` thresholds were verified by independently calling the real `estimateHpScore()` on the test's own pct before asserting unlock/non-unlock, rather than guessing, and confirmed all four (`hp_150/175/190/200`) fire simultaneously on a single 100% session since they're independent `if`s, not an else-if chain. Two real findings surfaced: **(1) `speed_demon`** is declared in `ALL_ACHIEVEMENTS` (rarity silver, "Svara snabbare än HP-standard i alla typer") but `checkAchievements()` has no code path that ever unlocks it — confirmed via a maximally-favorable scenario (streak 30, 100 mastered, 10 bookmarks, perfect session) that it still never appears in `getEarnedIds()`; this achievement is permanently unreachable dead content today. **(2) `allrounder`'s** description reads "≥70% i alla 4 delproven" (≥70% in all 4 parts, i.e. all 4 quant *or* all 4 verbal types), but the actual check is only `types.length === 4 && types.every(...>=0.7)` — it doesn't verify the 4 types come from the same section. A test session mixing 2 verbal + 2 quant types (e.g. ORD, LÄS, XYZ, KVA), each at exactly 70%, unlocks `allrounder` just as readily as a true single-section sweep would — a genuine mismatch between the Swedish description and the implementation, not just an untested edge case.

- [x] Write `src/utils/pacing.test.ts`: daily/weekly target calculation across different days-until-exam and current-progress scenarios (plenty of time left, exam very soon, already on track, badly behind).

  **Completion note:** 10 tests. Mocked `./readiness`'s `computeReadiness` (via `vi.mock` + `vi.mocked(...).mockReturnValue`) so each scenario supplies an exact readiness score instead of deriving one from a constructed history against the real question bank; used real `setExamDate`/`clearExamDate` (examDate.ts) and real `hp_session_history` writes for the recent-volume/on-track math. Hand-verified the `base × gapMultiplier` formula for all four days-bucket boundaries (exactly 30 days → base 20, not 15; exactly 14 days → base 30, not 20; 13 days → base 40) and both scenarios named in the task: plenty of time + already at target readiness (90 days, score=70, gap=0 → daily=15, the "Bra jobbat!" message branch specifically, since `readiness.score >= TARGET_READINESS` uses `>=` not `>`) and badly-behind-with-exam-soon (10 days, score=20, gap=50, zero recent volume → daily clamps at the 50 ceiling, `onTrack=false`, and the deficit message reports the exact rounded gap). Also covered the "already on track" case at a moderate 45-day/gap-15 scenario (daily=22, both the target-readiness-not-yet-met branch and the on-track sub-message). **Finding:** the daily target's `Math.max(10, ...)` floor is dead code — `base` is never lower than 15 (the ≥60-day bucket) and `gapMultiplier` is never below 1 (gap can't go negative), so `base×gapMultiplier` can never fall below 15; the 10-question floor can never actually fire. Also noted (not a bug, just a fact worth recording): no page currently renders `computePacing()`'s output directly — its only real consumer today is `notifications.ts`.

- [x] Write `src/utils/notifications.test.ts`, `src/utils/bookmarks.test.ts`, `src/utils/dailyChallenge.test.ts`: cover each file's actual exported logic — notification trigger/cooldown conditions, bookmark add/remove/persistence, daily challenge selection logic (is it deterministic per day, does it avoid repeats appropriately).

  **Completion note:** `notifications.test.ts` — 24 tests. jsdom has no built-in `Notification` global, so installed a small fake `Notification` class (`static permission`, `static requestPermission`, and an `instances` array recording every constructed notification) onto `globalThis` in `beforeEach`/removed it in `afterEach`; mocked `./pacing`'s `computePacing` for the pacing-notification tests so `onTrack`/`message` can be set exactly per scenario. Covered `notificationsSupported`/`notificationsEnabled`'s full 3-way gate (supported × granted × user-toggle), `requestNotificationPermission`'s grant/deny persistence, and both `maybeShow*Notification` functions' guards with exact millisecond boundary tests around their cooldown windows (just-under vs. just-over the 20-hour repeat cooldown, just-under vs. just-over the 3-hour active-study guard) rather than only "long ago" vs. "just now". Confirmed the due-notification and pacing-notification cooldowns use genuinely separate localStorage keys (`hp_notif_last_shown` vs. `hp_notif_pacing_last_shown`) — a recent due-notification does not suppress a pacing notification, and vice versa. No bugs found here; every guard behaves exactly as its inline comment describes.

  `bookmarks.test.ts` — 13 tests. Straightforward add/remove/persist/order coverage against the real `hp_bookmarks` key: insertion order preserved on add (new items appended), removal of a middle item leaves the others in original relative order (array `filter`, not resorted), toggling one id never disturbs another, and `clearBookmarks()` is a no-op-safe full reset. No surprises.

  `dailyChallenge.test.ts` — 10 tests. Used `vi.useFakeTimers`/`vi.setSystemTime` to control "today" for the LCG seed (`year×10000 + month×100 + day`). Confirmed `getDailyChallengeIds()` is genuinely deterministic within the same calendar day (two calls hours apart on 2026-07-07 return the identical 10-id array) and differs across a day boundary (2026-07-07 vs. 2026-07-08 produce different selections) — both checked by actually running the function twice and comparing, not assumed from reading the seed formula. Also verified the selection is always exactly `CHALLENGE_SIZE` (10) ids, contains no duplicates (the sort-based shuffle is always a permutation of the input, so duplication is structurally impossible — "avoids repeats *within* a day" holds trivially), and every returned id exists in the real question bank. `isDailyChallengeCompleted`/`markDailyChallengeCompleted` correctly key off today's UTC ISO date string and reset to "not completed" the moment the calendar day rolls over, including a garbage-value-in-storage case (treated as not-completed rather than throwing).

- [x] Run `npm test` and confirm all new and existing tests pass (should now be ~7-8 test files total across both phases). Run `npm run build`. Report per-file test counts, total test count before/after this whole playbook, and any bug the testing process surfaced.

  **Completion note:** `npm test` → **11 test files, 206 tests, all passing** (up from Phase 01's 5 files / 92 tests). New Phase 02 files: `achievements.test.ts` 30, `gamification.test.ts` 27, `pacing.test.ts` 10, `notifications.test.ts` 24, `bookmarks.test.ts` 13, `dailyChallenge.test.ts` 10 — **114 new tests** (92 + 114 = 206, confirmed via `vitest run --reporter=json`). `npm run build` succeeds (`tsc && vite build`) with only the same pre-existing chunk-size/dynamic-import warnings from Phase 01, unrelated to this work. The gamification streak logic (`updateStreak()`) was checked line-by-line against the design-reconciliation spec and **matches it correctly** — same-day/one-day/gap-reset all behave as intended, with only a cosmetic difference from the old prototype's seeded-12 default (current implementation has no seed, defaults to 0, and a first-ever session sets streak to 1). The most significant findings from this phase are in `achievements.ts`: (1) the `speed_demon` achievement is fully unreachable dead content — declared but never checked by `checkAchievements()`; (2) the `allrounder` achievement's implementation (`types.length === 4`) doesn't actually verify the 4 types share a section, so it can unlock on a mixed 2-verbal/2-quant session despite its Swedish description implying a same-section sweep; (3) `achievements.ts`'s SRS "mastery" bar (interval ≥21 days) is stricter than — and inconsistent with — `readiness.ts`'s own "mastery" definition (interval ≥7 days) elsewhere in the same codebase. A smaller finding in `pacing.ts`: its daily-target `Math.max(10, ...)` floor is dead code, since the formula's minimum possible output is 15.
