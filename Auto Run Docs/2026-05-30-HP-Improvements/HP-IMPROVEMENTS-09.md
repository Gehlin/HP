# HP Improvements — Phase 09: Theory Expansion — Deep Dives Per Topic

Expand the Theory section with detailed method guides for every major HP topic, complete worked examples, and common mistake warnings.

---

- [ ] **Add an "Algebra" deep-dive tab to `src/pages/Theory.tsx`**: Add a new section `algebra` to the SECTIONS array and create an `AlgebraSection` component. Content must cover: (1) Solving linear equations step by step with one worked example; (2) Expanding brackets — the FOIL method with $(a+b)(c+d)$ example; (3) Factoring — common factor, difference of squares, trinomials; (4) The three conjugate/square identities with examples; (5) Solving quadratic equations using the PQ formula with a worked numerical example; (6) A "Vanliga misstag" box listing: forgetting to flip the inequality sign when dividing by a negative, sign errors when expanding $-(a-b)$, confusing $x^2 = 4$ having two solutions.

- [ ] **Add a "Sannolikhet & Kombinatorik" deep-dive tab to `src/pages/Theory.tsx`**: Create a `ProbabilitySection` component. Content: (1) Basic probability definition with a dice example; (2) Complement rule $P(\text{not A}) = 1 - P(A)$ — when to use it (easier to count the complement); (3) Independent events: $P(A \text{ and } B) = P(A) \times P(B)$; (4) Permutations vs. combinations — when order matters; (5) "At least one" problems — always use complement; (6) Conditional probability concept (without formula — intuitive explanation with examples); (7) Three worked HP-style examples with full solutions.

- [ ] **Add a "Geometri" deep-dive tab to `src/pages/Theory.tsx`**: Create a `GeometrySection` component. Content: (1) All key area/perimeter/volume formulas with diagrams described in text; (2) Pythagoras — identifying hypotenuse, the 3-4-5 and 5-12-13 triples; (3) Similar triangles — when to use them, ratio of areas vs. ratio of sides; (4) Circle theorems — inscribed angle, tangent properties; (5) Coordinate geometry — slope formula, distance formula, midpoint; (6) A "Snabbtest" at the bottom: 5 short questions (no answer submission, just show answers inline) to self-test.

- [ ] **Add a "NOG Strategibok" (strategy workbook) tab to `src/pages/Theory.tsx`**: Create a `NOGStrategySection` component with 8 worked NOG examples covering every common pattern. For each example: show the full question, walk through the reasoning for statement (1) alone, then (2) alone, then combined, then state the answer and why. Patterns to cover: direct algebra, indirect algebra (equation gives value of expression, not variable), ordering with 3+ people, geometric sufficiency, "yes/no" questions (answer is still unique even if "no"), counting/set problems, percentage sufficiency, and the tricky pattern where both statements together are still insufficient (answer E).
